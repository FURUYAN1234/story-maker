## What's New / 更新内容

* **Fix**: Fixed a 404 error caused by missing favicon during initial load by adding `favicon.svg` and updating the link in `index.html`. / 初期表示時にブラウザが `/favicon.ico` を要求して404エラーになる問題を修正（`favicon.svg` の追加およびリンク指定）。
* **Fix**: Synchronized and corrected internal version references in files like `src/data.js`. / `src/data.js` などの内部バージョン表記のズレを同期・修正。
