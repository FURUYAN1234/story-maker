# HANDOFF (Story Maker)

## Codex Update - 2026-06-08 JST - v4.9.6 README repair and release follow-through

- User reported the GitHub README was still mojibake and main/tag still showed `v4.9.5`, so local v4.9.6 changes were not enough. This release pass must commit, push, create an English/Japanese annotated tag, deploy GitHub Pages, and run the requested full workspace backup.
- Replaced `README.md` with clean UTF-8 English/Japanese release documentation. The public feature list now covers only the 14 non-long output modes; long-novel mode is explicitly treated as suspended until quality meets the public release bar.
- Fixed the visible package description in `package.json` so the repository metadata no longer carries mojibake text.
- Before release, check untracked `scratch_*`, `output_*`, and `scripts/inapp_browser_uia.ps1` as generated/local verification artifacts. Do not stage them into the public release.

## Codex Update - 2026-06-08 JST - v4.9.6 public deploy safety guard

- Added a production-build guard in `vite.config.js` that strips the local QA API-session persistence helper and debug globals from public bundles, then fails the build if public chunks still contain `qaApiSession`, `QA API keep`, `__sm_qa_api_test`, Story Maker diagnostic globals, `localStorage`, or `sessionStorage`.
- Rationale: the local QA helper did not expose real API keys, but its code and labels were present in the generated public JS, so the old claim that the developer/API-save mode could not be found in a deployed bundle was not true enough for release.
- Verification passed: `node --check vite.config.js`, `node --check src/main.js`, `npm run build`, `npm run lint --if-present`, `git diff --check -- . ':!dist'`, and `dist` scans for QA persistence strings, storage APIs, key/token patterns, and scratch/output artifacts.
- Deployed with `npm run deploy`. Remote/live verification passed: `origin/gh-pages:index.html` and `https://furuyan1234.github.io/story-maker/` show `Story Maker v4.9.6` with `assets/index-CsM3w162.js`; remote and live JS scans returned no hits for QA persistence strings, storage APIs, or API-key/token patterns.
- Security note: deploy publishes only `dist` via `gh-pages -d dist`. User-entered story inputs and API keys are sent only to the selected provider endpoint during generation; they are not saved to this app's server. URL import/proxy features may send the requested URL/content to their configured external fetch service.

## Codex Update - 2026-06-08 JST - v4.9.6 all-random mode and paragraph density

- Implemented `v4.9.6` in `src/main.js` and synced version metadata in `package.json`, `package-lock.json`, `index.html`, `src/data.js`, and `README.md`.
- Root cause: `全項目ランダム` skipped output-mode rerolling after a mode chip click because `modeSource === "selected"` made `br()` treat the mode as protected. v4.9.6 overrides the full-random handler so output mode is rerolled whenever the mode section is not locked, while still excluding sealed long-novel mode.
- Added a non-long paragraph-density contract to the shared standard prompt path used by Gemini and OpenAI. Prose-style modes (`short_short`, `novel`, `medium`, `essay`, `fairy`) also receive final display cleanup through `Q()` so long unbroken prose is split with blank lines at section and paragraph boundaries.
- Verification passed: `node --check src\main.js`, `npm run lint --if-present`, `npm run build`, `git diff --check -- . ':!dist'`, HTTP 200 on port 5179, and in-app browser UI verification on `http://127.0.0.1:5179/`. The browser test selected `medium` manually, clicked `全項目ランダム` 120 times, saw all 14 non-long modes, saw zero `long`, and reported zero warn/error logs.
- Follow-up in-app browser real API matrix passed using the already-entered masked keys in the same tab: Gemini 14/14 non-long modes and ChatGPT/OpenAI 14/14 non-long modes. Checked footer `Generated By AI Story Maker V4.9.6`, no visible API errors, required mode markers, and prose paragraph counts. Gemini `medium` produced 3,593 visible chars / 22 paragraphs; ChatGPT `medium` produced 1,844 visible chars / 14 paragraphs.

## Codex Update - 2026-06-07 JST - v4.9.5 non-long output-mode contracts

- Fresh Gemini QA for the requested all-mode sweep found a real standard-mode bug before the full matrix: `4koma` returned ordinary prose instead of the required four-panel structure.
- Implemented `v4.9.5` on the standard generation prompt path by wrapping `lp()` with a shared non-long output-mode contract. All non-long modes now receive explicit completed-output templates: 4koma, AI 4koma scenario, short-short, novel, medium, scenario, manga, essay, poem, fairy tale, letter, diary, documentary, and radio drama.
- The contract explicitly keeps selected genre content in control and forbids flattening every genre into comedy. Long-novel mode remains sealed and excluded.
- Next required step: run `node --check`, lint, diff-check, build, reload port 5179, then run the fresh Gemini/OpenAI all-non-long-mode API matrix before deploy.

## Codex Update - 2026-06-07 JST - v4.9.4 long-novel mode sealed

- Final v4.9.3 Gemini long-novel QA failed. The run saved chapters 1 and 2, then chapter 3 repeatedly entered ledger rebuilds for off-route material and chapter-boundary replay; it was aborted at progress `2 / 10` after `[Rescue] Chapter 3 ... (4/8)`.
- Implemented `v4.9.4` as a seal, not another prompt patch. Long-novel mode is disabled in the mode chip UI, removed from random mode selection, and blocked at generation start when `mode` or `mode-custom` still contains a long-novel request.
- Wrapped long-novel chapter entry points so direct calls to the long-novel generator or next-chapter path reset instead of starting generation.
- Do not continue patch-loop attempts for long-novel mode unless the user explicitly reopens it. Do not touch other apps while working on Story Maker.
- Verification passed: `node --check src\main.js`, `npm run lint --if-present`, `git diff --check -- . ':!dist'`, `npm run build`, HTTP 200 on port 5179 with `Story Maker v4.9.4`, and in-app browser DOM showing `data-long-novel-sealed="true"` with zero `data-v="long"` mode chips.

## Codex Update - 2026-06-07 JST - v4.9.3 chapter-ledger drafting / next full QA decisive

- Implemented `v4.9.3` in `src/main.js` and synced visible/runtime metadata from `4.9.2` to `4.9.3`.
- Root correction: long-novel prompts now add a chapter-design ledger before prose. Chapter 1 must internally decide a compact whole-novel ledger, and each later chapter must fix start state, genre drive, chapter-end state, foreshadowing/prop usage, character entry/exit, and next-chapter handoff from saved canon before drafting.
- Quality-gate retries are no longer presented as `[Stop]` / two-attempt failure for repairable chapter-quality issues. They are logged as ledger rebuilds with a larger rebuild budget.
- User directive: run one fresh full long-novel QA after local checks. If that next full QA cannot complete, stop trying to patch long-novel mode and disable/non-activate long-novel mode instead. Do not touch other apps while working on Story Maker.
- Next required step: run local checks, reload the in-app browser at a fresh v4.9.3 QA URL, confirm saved API key presence by input length only, restore the intended chips, then run the decisive full Gemini long-novel QA. Do not deploy/tag/release/backup before the decisive QA passes.

## Codex Update - 2026-06-07 JST - v4.9.2 analog-store digital false-positive closure / API QA pending

- Implemented `v4.9.2` in `src/main.js` and synced visible/runtime metadata from `4.9.1` to `4.9.2`.
- Added a final quality-issue filter so old `v4.6.1 story-means ledger` `digitalLab` and old POS fallback warnings do not reject ordinary register/shelf/receipt/invoice/package/customer/storefront prose in the local-comedy QA profile.
- Digital/lab material is still rejected when it functions as the actual cause, proof, solution, clue, or chapter hook.
- Next required step: run local checks, reload the in-app browser at a fresh v4.9.2 QA URL, confirm saved API key presence by input length only, restore the intended chips, rerun Gemini full long-novel QA, then run OpenAI only after Gemini passes cleanly. Do not deploy/tag/release/backup before both APIs pass.

## Codex Update - 2026-06-07 JST - v4.9.1 genre-dominance guard / API QA pending

- Implemented `v4.9.1` in `src/main.js` and synced visible/runtime metadata from `4.9.0` to `4.9.1`.
- Root correction: local-comedy QA constraints no longer activate merely because the setting contains convenience-store, shopping-street, or daily-life axes. They now activate only when the selected genre itself is comedy/dotabata/gag, so other genres keep their own structure and payoff engine.
- Added a final quality-issue filter for ordinary store-work false positives: old `v4.6.1 story-means ledger` `cultFacility` warnings are removed when the prose is clearly register/shelf/receipt/invoice/package/customer/storefront work and no hard cult/ritual route exists. The v4.9.0 completion rescue warning is also filtered when off-route material is only incidental rather than serving as cause/proof/solution/hook.
- Next required step: run local checks, reload the in-app browser at a fresh v4.9.1 QA URL, confirm saved API key presence by input length only, restore the intended chips, rerun Gemini full long-novel QA, then run OpenAI only after Gemini passes cleanly. Do not deploy/tag/release/backup before both APIs pass.

## Codex Update - 2026-06-07 JST - v4.9.0 selected-genre payoff engine and completion rescue / API QA pending

- Implemented `v4.9.0` in `src/main.js` and synced visible/runtime metadata from `4.8.9` to `4.9.0`.
- Root fix: long-novel generation now has a selected-genre payoff engine. Every draft, next-chapter, repair, and continuation path must turn the chapter's setup into a genre-appropriate payoff, punchline, cost, or relationship update. Quality detection is treated as a signal to rebuild the chapter into a complete manuscript, not as the goal.
- For the local-comedy QA profile, promotional/limited-product, secret-inventory, past-event, legend-item, digital/lab, and grand-template routes are detected as the wrong story engine when they become cause, proof, solution, or hook. Retries must finish through register misunderstandings, shelf labels, paper receipts, cash-count slips, paper invoices, package labels, complaint slips, shift notes, testimony, apologies, refunds, and visible cleanup.
- Next required step: run local checks, reload the in-app browser at a fresh v4.9.0 QA URL, confirm saved API key presence by input length only, restore the intended chips, rerun Gemini full long-novel QA, then run OpenAI only after Gemini passes cleanly. Do not deploy/tag/release/backup before both APIs pass.

## Codex Update - 2026-06-07 JST - v4.8.9 selected-genre native pull and campaign-price stall closure / API QA pending

- Formalized the already-added `v4.8.9` hardening block in `src/main.js` and synced visible/runtime metadata from `4.8.8` to `4.8.9`.
- Root fix focus: every long-novel draft/next/repair/continuation path now reasserts the selected genre's own pull instead of flattening all stories toward workplace crisis, local comedy, or suspense. For the local-comedy QA profile, campaign/discount/limited-price explanations cannot become the cause of price gaps, shelf-label contradictions, cash differences, or trust crises.
- Continuation prompts now reject stagnant repeats and require new current-scene prose only; the save gate rejects campaign/discount price routes when they function as cause, proof, solution, trust crisis, or hook.
- Local checks and fresh full Gemini/OpenAI QA still need to run after this metadata sync. Do not deploy/tag/release/backup before both APIs complete cleanly.

## Codex Update - 2026-06-07 JST - v4.8.8 chapter-start replay and secret-shelf route preemption / API QA pending

- Fresh in-app Gemini QA on `v4.8.7` was stopped after chapter 4. It saved chapters 1-4, but chapter 3 used a full regeneration for previous-chapter opening replay and chapter 4 consumed two more regenerations from secret-shelf/prize/smuggling-style drift. This is not good enough for the 100% completion / retry-budget preservation target.
- Implemented `v4.8.8` in `src/main.js`: local-comedy long-novel prompts now force each chapter opening to start with fresh current store action, customer interruption, manager speech, or visible paper evidence rather than reusing previous prose. Backroom/shelf scenes are constrained to ordinary inventory, cleanup, delivery, shelf-label, cash-count, complaint-slip, shift-note, package-label, or testimony work.
- Added a save gate for secret shelves, mystery paper, prize/phantom-item, smuggling, and code phrasing when those become the chapter cause, proof, solution, trust crisis, or hook.
- Version metadata synced to `4.8.8` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with functional changes only; no local API persistence/key-save details were added.
- Next required step: run local checks, reload the in-app browser at a fresh v4.8.8 QA URL, confirm saved API key presence by input length only, restore the intended chips, rerun Gemini full long-novel QA, then run OpenAI only after Gemini passes cleanly. Do not deploy/tag/release/backup before both APIs pass.

## Codex Update - 2026-06-07 JST - v4.8.7 analog-store digitalLab false-positive closure / API QA pending

- Fresh in-app Gemini QA on `v4.8.6` was stopped during chapter 1. The prose had moved to shelf labels, receipts, refunds, register lines, and visible store work, but the old `v4.6.1 story-means ledger` still rejected it as `kinds:digitalLab` despite no visible USB/POS/data/lab route in the monitored body.
- Implemented `v4.8.7` in `src/main.js`: a final analog-store false-positive filter removes old `v4.6.1 story-means ledger` digitalLab warnings, old digital-prop warnings, and old POS data fallback warnings when the chapter is clearly grounded in register/shelf/receipt/cash/invoice/package/customer/storefront work and has no hard digital, device, data, camera, app, server, or lab route.
- Version metadata synced to `4.8.7` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with functional changes only; no local API persistence/key-save details were added.
- Next required step: run local checks, reload the in-app browser at a fresh v4.8.7 QA URL, confirm saved API key presence by input length only, restore the intended chips, rerun Gemini full long-novel QA, then run OpenAI only after Gemini passes cleanly. Do not deploy/tag/release/backup before both APIs pass.

## Codex Update - 2026-06-07 JST - v4.8.6 continuation delta and legacy promotion-paper closure / API QA pending

- Fresh in-app Gemini QA on `v4.8.5` was stopped. It saved chapter 1 only after two full regenerations, then chapter 2 returned to old campaign/stamp-rally material through paper bundles, campaign POP, and a box-open scatter scene. This is not acceptable for the 100% completion target.
- Implemented `v4.8.6` in `src/main.js`: continuation joining now attempts to append only the new delta when a continuation response repeats the already-known chapter body. Large repeated paragraph/body blocks are collapsed before audit/save, and `zn()` is made repeat-aware so repeated bodies do not falsely satisfy completion while the collapsed body is still under the chapter minimum.
- Added a legacy promotion-paper closure: old shelf-label bundles, faded flyers, campaign POP, stamp-rally instruction sheets, and past-promotion remnants are treated as the same forbidden unselected route when used as cause, evidence, solution, or chapter hook. The old local repair text that mentioned card bundles/stamps is sanitized if it fires.
- Re-clarified the selected-genre direction in prompts: comedy maximizes comedy, serious drama maximizes seriousness, suspense maximizes tension, and practical crisis/trust/deadline pressure is used only when it fits the selected genre.
- Version metadata synced to `4.8.6` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with functional changes only; no local API persistence/key-save details were added.
- Next required step: run local checks, reload the in-app browser at a fresh v4.8.6 QA URL, confirm saved API key presence by input length only, restore the intended chips, rerun Gemini full long-novel QA, then run OpenAI only after Gemini passes cleanly. Do not deploy/tag/release/backup before both APIs pass.

## Codex Update - 2026-06-07 JST - v4.8.5 hidden-paper box and continuation growth / API QA pending

- Fresh in-app Gemini QA on `v4.8.4` stopped the campaign-card/stamp route, but chapter 1 still failed before saving. The draft used ordinary store-work evidence for a while, then drifted toward a hidden-paper-from-box hook; the continuation loop also repeated the same 2,617-character body length several times instead of growing the chapter.
- Implemented `v4.8.5` in `src/main.js`: cardboard/boxes/cans/packaging remain allowed as ordinary store-work objects, but paper, notes, photos, keys, cards, or old objects may not fall from inside/under/behind them as a mystery hook. Evidence must be visible receipts, paper delivery invoices, shelf labels, complaint slips, shift notes, packaging labels, or testimony.
- Added a continuation growth contract for the local-comedy profile: continuation calls must output only new prose after the last situation, must not repeat existing paragraphs, headings, memos, or word-count notes, and must add current-scene action/dialogue/work evidence.
- Version metadata synced to `4.8.5` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with functional changes only; no local API persistence/key-save details were added.
- Next required step: run local checks, reload the in-app browser at a fresh v4.8.5 QA URL, confirm saved API key presence by input length only, restore the intended chips, rerun Gemini full long-novel QA, then run OpenAI only after Gemini passes cleanly. Do not deploy/tag/release/backup before both APIs pass.

## Codex Update - 2026-06-07 JST - v4.8.4 local-comedy ledger alignment / API QA pending

- Fresh in-app Gemini QA on `v4.8.3` was aborted during chapter 1. The save gate correctly rejected the draft, but the retry was avoidable: older prompt ledgers still listed campaign-card/stamp material as allowed store evidence, while newer v4.8.1+ gates rejected campaign/card/stamp/prize material as the chapter engine.
- Implemented `v4.8.4` in `src/main.js`: campaign-card/stamp/prize routes are removed from the local-comedy allowed ledger and runtime beat plans. The selected QA axis is now fixed to shelf labels, paper receipts, cash differences, paper delivery invoices, packaging/cardboard, complaint slips, shift notes, customer/shopkeeper testimony, face-to-face apologies, and small refunds.
- Version metadata synced to `4.8.4` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with functional changes only; no local API persistence/key-save details were added.
- Next required step: run local checks, reload the in-app browser at a fresh v4.8.4 QA URL, confirm saved API key presence by input length only, clear mode custom if needed, activate the long-novel mode chip, restore the intended chips, rerun Gemini full long-novel QA, then run OpenAI only after Gemini passes cleanly. Do not deploy/tag/release/backup before both APIs pass.

## Codex Update - 2026-06-07 JST - v4.8.3 analog store-work false-positive filter / API QA pending

- Fresh in-app Gemini QA on `v4.8.2` still hit older digital/POS gates on analog store-work scenes involving shelf labels, POP sheets, cardboard, paper memos, and packaging. The run was aborted during chapter 1 after old digital false positives and a repeated-opening repair failure.
- Implemented `v4.8.3` in `src/main.js`: for the local-comedy long-novel profile, old v4.4/v4.5/v4.6 digital/POS warnings are filtered only when the prose has no hard digital route terms (USB, POS logs/data, sales/register data, system logs, electronic records, cameras, analysis, research facilities, secret plans, AI weapons) and is clearly grounded in analog store work.
- Version metadata synced to `4.8.3` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with functional changes only; no local API persistence/key-save details were added.
- Next required step: run local checks, reload the in-app browser at a fresh v4.8.3 QA URL, confirm saved API key presence by input length only, clear mode custom if needed, activate the long-novel mode chip, restore the intended chips, rerun Gemini full long-novel QA, then run OpenAI only after Gemini passes cleanly. Do not deploy/tag/release/backup before both APIs pass.

## Codex Update - 2026-06-07 JST - v4.8.2 repair length preservation / API QA pending

- Fresh in-app Gemini QA on `v4.8.1` stopped using stamp/card/campaign/prize material as the chapter engine, but chapter 1 had a repeated-opening contradiction. The AI repair removed too much prose, leaving the chapter at 2,710 chars against the 4,800-char save minimum, which triggered a full regeneration. The run was aborted.
- Implemented `v4.8.2` in `src/main.js`: repair, continuation, next-chapter, and initial prompts now require length preservation. If a repeated opening is removed, the model must replace that removed material with new current-scene action, dialogue, physical evidence, apology/refund, or shelf/packaging/invoice work rather than summarizing the chapter down.
- Version metadata synced to `4.8.2` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with functional changes only; no local API persistence/key-save details were added.
- Next required step: run local checks, reload the in-app browser at a fresh v4.8.2 QA URL, confirm saved API key presence by input length only, clear mode custom if needed, activate the long-novel mode chip, restore the intended chips, rerun Gemini full long-novel QA, then run OpenAI only after Gemini passes cleanly. Do not deploy/tag/release/backup before both APIs pass.

## Codex Update - 2026-06-07 JST - v4.8.1 no stamp/card engine for local-comedy QA / API QA pending

- Fresh in-app Gemini QA on `v4.8.0` still returned to old campaign-card/card-pile/stamp-style plot engines in chapter 1. The save gate rejected the draft and triggered regeneration; the run was aborted before anything was saved.
- Implemented `v4.8.1` in `src/main.js`: for the local-comedy QA profile, newer prompts explicitly override earlier carrier prompts and remove stamps/cards/campaigns/prizes from the chapter engine. They may appear only as incidental background, not as cause, mystery, proof, solution, or hook. The new carriers are shelf labels, paper receipts, cash differences, paper delivery invoices, packaging/cardboard, complaint slips, shift notes, customer/shopkeeper testimony, apologies, refunds, and confessions.
- Added a matching save gate that rejects stamp/card/campaign/prize saturation when it functions as cause, truth, solution, proof, or hook.
- Version metadata synced to `4.8.1` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with functional changes only; no local API persistence/key-save details were added.
- Next required step: run local checks, reload the in-app browser at a fresh v4.8.1 QA URL, confirm saved API key presence by input length only, clear mode custom if needed, activate the long-novel mode chip, restore the intended chips, rerun Gemini full long-novel QA, then run OpenAI only after Gemini passes cleanly. Do not deploy/tag/release/backup before both APIs pass.

## Codex Update - 2026-06-07 JST - v4.8.0 ordinary register POS surface repair / API QA pending

- Fresh in-app Gemini QA on `v4.7.9` entered long-novel mode, saved chapter 1 after one contradiction repair, then chapter 2 was rejected by older POS/data gates because the ordinary shelf-label/register scene contained the surface word `POS`. The run was aborted after the first retry was triggered.
- Implemented `v4.8.0` in `src/main.js`: for the local-comedy long-novel profile, harmless surface terms such as `POSレジ`, `POS端末`, `POSシステム`, and standalone `POS` are locally rewritten to `レジ` / `レジ設定` before save-time quality gates. Hard digital routes such as `POSログ`, `POSデータ`, sales/register data, system logs, electronic records, scan history, and product-master data remain blocked.
- Version metadata synced to `4.8.0` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with functional changes only; no local API persistence/key-save details were added.
- Next required step: run local checks, reload the in-app browser at a fresh v4.8.0 QA URL, confirm saved API key presence by input length only, clear mode custom if needed, activate the long-novel mode chip, restore the intended chips, rerun Gemini full long-novel QA, then run OpenAI only after Gemini passes cleanly. Do not deploy/tag/release/backup before both APIs pass.

## Codex Update - 2026-06-07 JST - v4.7.9 hidden-box lexical closure / API QA pending

- Fresh in-app Gemini QA on `v4.7.8` correctly entered long-novel mode after the mode chip was reactivated, saved chapters 1-2 with no full regenerations, then chapter 3 live output returned to the same forbidden route using lexical variants: `木の箱`, `裏キャンペーン`, last year's stamp-rally prizes, and `スタンプの迷宮`. The run was aborted before chapter 3 was saved.
- Implemented `v4.7.9` in `src/main.js`: local-comedy prompts and save gates now treat `木箱`, `木の箱`, `木製の箱`, hidden/back campaign language, last-year prize hunts, legendary/phantom stamp prizes, and stamp/card maze language as one forbidden hidden-box/legendary-prize route. Ordinary cardboard, packaging, product tins, and equipment cases remain allowed only as mundane objects, not as the mystery/cause/solution/proof/hook.
- Version metadata synced to `4.7.9` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with functional changes only; no local API persistence/key-save details were added.
- Next required step: run local checks, reload the in-app browser at a fresh v4.7.9 QA URL, confirm saved API key presence by input length only, clear mode custom if needed, activate the long-novel mode chip, restore the intended chips, rerun Gemini full long-novel QA, then run OpenAI only after Gemini passes cleanly. Do not deploy/tag/release/backup before both APIs pass.

## Codex Update - 2026-06-07 JST - v4.7.8 selected-genre mastery / API QA pending

- User clarified that "interestingness" means maximizing the selected genre, not forcing every story toward workplace crisis or comedy. Gag should stay gag, serious should stay serious, suspense should stay suspense, and the app should strengthen the selected genre's pull while still targeting 100% completion.
- Fresh in-app Gemini QA on `v4.7.7` improved over earlier runs but still used regeneration budget: chapter 3 needed regeneration after a stamp/card-centered route plus previous-chapter replay, and chapter 4 hit an opening-replay quality stop. That means v4.7.7 is not yet a clean full-QA candidate.
- Implemented `v4.7.8` in `src/main.js`: added a selected-genre mastery contract across long-novel shared, initial, next-chapter, continuation, and repair prompts. Regeneration now strengthens both the desired genre target and the negative prompt, explicitly discarding the failed opening/route and unselected high-power templates instead of renaming them.
- Added retry-budget preservation: if a non-final chapter fails only because its opening replays previous-chapter prose, the save-path repair hook can trim the repeated opening and avoid spending a full regeneration. For the local-comedy QA profile, chapter-3-and-later card/stamp saturation is detected earlier and redirected to the assigned carrier.
- Version metadata synced to `4.7.8` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with functional changes only; no local API persistence/key-save details were added.
- Next required step: run local checks, abort/reload the in-app browser at a fresh v4.7.8 QA URL, confirm saved API key presence by input length only, restore the intended 11 chips, rerun Gemini full long-novel QA, then run OpenAI only after Gemini passes cleanly. Do not deploy/tag/release/backup before both APIs pass.

## Codex Update - 2026-06-07 JST - v4.7.7 local-comedy carrier rotation / API QA pending

- Fresh in-app Gemini QA on `v4.7.6` improved the retry profile: chapter 1 saved at 5,545 chars and chapter 2 saved at 5,634 chars with zero full regenerations. The previous ordinary-register/POS false-positive no longer blocked chapter 2.
- The run was aborted in chapter 3 because a new saturation pattern appeared: old stamp cards, cardboard/card piles, phantom prize language, and stamp-card maze language kept becoming the chapter engine. The v4.7.6 mundane stamp gate correctly rejected it, but this still spent chapter-3 retries.
- Implemented `v4.7.7` in `src/main.js`: added local-comedy carrier rotation. Each chapter now receives an assigned proof/trouble carrier and the prompt forces rotation from stamp/card material into shelf labels, paper receipts, cash differences, delivery invoices, packaging/cardboard mix-ups, complaint slips, shift notes, customer/shopkeeper testimony, apology, refund, and confession.
- Added a chapter-3-and-later save gate for local-comedy stamp saturation. It rejects old card boxes, card piles, phantom prizes, stamp-card maze/franchise escalation, and high-frequency stamp/card repetition when it becomes the chapter engine instead of the assigned carrier.
- Version metadata synced to `4.7.7` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with functional changes only; no local API persistence/key-save details were added.
- Next required step: run local checks, reload the in-app browser at a fresh v4.7.7 QA URL, confirm saved API key presence by input length only, restore the 11 chips, and rerun Gemini full long-novel QA from scratch before OpenAI.

## Codex Update - 2026-06-07 JST - v4.7.6 ordinary register precision / API QA pending

- Fresh in-app Gemini QA on `v4.7.5` confirmed the chapter-1 wooden-box / legendary-stamp drift was fixed: chapter 1 saved cleanly at 4,997 chars, no full regeneration, and no hidden-box/legendary-stamp/USB/POS/data/amnesia/lab terms in the monitored output.
- The run was aborted in chapter 2 because normal store-work phrases such as register screen, scan, receipt printer, and price confirmation were treated as `v4.5.9 POS data fallback` / `v4.6.1 story-means ledger digitalLab`, consuming retries even though the visible prose was mundane register/customer/shelf-label work. A second retry also lacked a visible shopping-street anchor.
- Implemented `v4.7.6` in `src/main.js`: local-comedy quality filtering now removes POS/data false positives when the text contains ordinary register operations but no hard POS/log/sales-data/digital-evidence terms. Real POS logs, sales data, register logs, app/server/system/log/data/camera routes remain blocked.
- Added a v4.7.6 shopping-street anchor prompt to shared long prompt, initial plan, next chapter, continuation, and repair paths so every chapter includes at least one visible local storefront/shopkeeper/arcade/street/customer/shop-association anchor without turning it into history/legend lore.
- Version metadata synced to `4.7.6` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with functional changes only; no local API persistence/key-save details were added.
- Next required step: run local checks, reload the in-app browser at a fresh v4.7.6 QA URL, confirm saved API key presence by input length only, restore the 11 chips, and rerun Gemini full long-novel QA from scratch before OpenAI.

## Codex Update - 2026-06-07 JST - v4.7.5 mundane campaign-stamp lock / API QA pending

- Fresh in-app Gemini QA on `v4.7.4` was started with the intended 11 chips and saved Gemini key present by masked length only. It did not show USB/POS/data/amnesia/lab drift, but chapter 1 still drifted from an ordinary campaign-card problem into a wooden-box / legendary-stamp / phantom-prize / old-stamp-origin route. The run was aborted after the failure was observed; one chapter had already been saved in that browser attempt, so reload before retesting.
- Implemented `v4.7.5` in `src/main.js`: added a local-comedy mundane campaign-stamp lock across shared long prompt, initial long plan, next-chapter, continuation, and repair prompts. For convenience-store / shopping-street / comedy long novels, stamps/cards/prizes/shelf labels/receipts/packaging/cardboard must stay as today's store-work mistakes: wrong stamp position, mixed-in cards, expired cards, old ink pads, missed explanations, shelf-label mismatch, customer confusion, or staff report omissions.
- Added a matching save-time gate for this profile. It rejects wooden boxes, hidden boxes, legendary stamps, phantom prizes, impossible stamps, historical stamp-origin investigations, and old-manager-special-order lore before they can enter saved canon.
- Version metadata synced to `4.7.5` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with functional changes only; no local API persistence/key-save details were added.
- Next required step: run local checks, reload the in-app browser on port 5179 at a fresh v4.7.5 QA URL, confirm saved API key presence by input length only, restore the 11 chips, rerun Gemini full long-novel QA, then run OpenAI only after Gemini passes cleanly. Do not deploy/tag/release/backup before both APIs pass.

## Codex Update - 2026-06-07 JST - v4.7.4 selected-genre pull contract / API QA pending

- User clarified that "interestingness" does not mean comedy-only. The generator must maximize the selected genre itself: gag stays gag, serious stays serious, suspense stays suspense, and each selected genre should pull the reader with its own pressure while targeting 100% completion.
- Implemented `v4.7.4` in `src/main.js`: added a selected-genre pull contract for long novels. It detects the selected genre family and tells the model how to drive chapters for comedy, suspense, mystery, serious drama, romance, horror, action, human drama, fantasy, and SF.
- The v4.7.4 contract is prompt-first rather than a broad new hard save gate, to avoid spending regeneration budget unnecessarily. It still explicitly blocks unselected strong templates such as hidden boxes, legendary items, USB/POS/data, amnesia, research labs, world-scale conspiracies, cult rituals, smuggling/code routes, and AI weapons from becoming the core mystery, proof, solution, or chapter hook unless those axes were selected.
- Version metadata synced to `4.7.4` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with functional changes only; no local API persistence/key-save details were added.
- Next required step: run local checks, reload the in-app browser on port 5179, confirm saved Gemini/OpenAI key presence by input length only, restore the intended 11 long-novel chips, then rerun fresh Gemini and OpenAI full long-novel QA before any deploy/tag/release/backup.

## Codex Update - 2026-06-07 JST - v4.7.3 Japanese-only local-comedy QA hardening / API QA pending

- Fresh in-app Gemini QA on `v4.7.2` completed all 10 chapters, but it is not deployable quality: final scan found the English internal label `awkward` in the manuscript, Gen-4 editorial evaluation skipped due response JSON parsing failure, and late chapters required many retries (chapter 7 reached 5/5, chapter 9 retried twice, chapter 10 retried once after a repair produced a too-short damaged body).
- Implemented `v4.7.3` in `src/main.js`: replaced the v4.7.2 English entertainment-spine prompt wrapper with a Japanese-only local-comedy contract connected to the pre-v4.7.2 base prompt functions, so the old English contract is no longer prepended to generation/continuation/repair prompts.
- Added retry-conserving leak repair: minor English/internal labels such as `awkward`, `laugh beats`, `store-work`, or `local comedy` are converted to manuscript-safe Japanese during save-path cleanup instead of immediately spending a regeneration attempt. Only unrepaired internal-instruction blocks or hard management markers still fail closed.
- Added retry-budget preservation for common nonfatal quality failures: chapter 1 now explicitly shows shopping-street grounding in the opening, and preview/teaser-style non-final chapter endings can be rewritten to a concrete store-work ending without spending a full-chapter regeneration when the body is otherwise long enough.
- Version metadata synced to `4.7.3` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with the functional QA hardening only; no local API persistence/key-save details were added.
- Next required step: run local checks, reload the in-app browser on port 5179, confirm saved API key presence by input length only, restore the intended 11 long-novel chips, then rerun fresh Gemini and OpenAI full long-novel QA before any deploy/tag/release/backup.

## Codex Update - 2026-06-07 JST - v4.7.2 local-comedy entertainment spine / API QA pending

- User asked to improve the story's interestingness so an AI/human review can honestly call the long novel entertaining, and to aim for 100% completion across both APIs.
- Implemented `v4.7.2` in `src/main.js`: added a local-comedy entertainment spine for convenience-store / shopping-street / dotabata long novels. Each chapter now receives concrete laugh beats (customer interruptions, campaign-card mistakes, shelf-label contradictions, cardboard mix-ups, awkward public apologies, visible store cleanup) before drafting.
- Added a moderate save-time fun gate for this selected axis. It rejects only severe failures where local store action disappears, a chapter becomes heavy truth-summary melodrama with too little comedy, late chapters reopen already-settled envelope/debt/repayment threads as a new mystery, or endings rely on abstract future/truth/unity language without funny human action.
- Version metadata synced to `4.7.2` in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`. README changelog updated with the v4.7.2 functional change only; no local API persistence/key-save details were added.
- Local verification completed for v4.7.2: `node --check src\main.js`, `npm run lint --if-present`, `git diff --check -- . ':!dist'` (LF-to-CRLF warnings only), `npm run build`, and HTTP 200 on port 5179.
- Next required step: reload the in-app browser on port 5179, confirm saved Gemini/OpenAI key presence by input length only, restore the intended 11 long-novel chips, then run fresh full Gemini and OpenAI long-novel QA. Do not deploy/tag/release/backup until both fresh API runs complete and final manuscript quality is acceptable.

## Codex Update - 2026-06-07 JST - v4.7.1 Gemini progress-meta cleanup / deploy held

- Resumed the interrupted in-app Gemini full long-novel QA for `v4.7.0`. The run completed all 10 chapters with saved chapter lengths: 1=4,883, 2=8,680, 3=9,198, 4=12,112, 5=8,185, 6=11,482, 7=5,653, 8=8,902, 9=6,812, 10=5,653.
- Runtime gates behaved correctly but Gemini remained costly in late chapters: chapter 7 rejected a previous-chapter replay, chapter 9 repeatedly reopened resolved envelope/debt/repayment threads and one off-ledger route before repair/regeneration finally saved it, and chapter 10 rejected a chapter-9 opening replay before saving.
- Final v4.7.0 Gemini scan was not acceptable for deploy: 10 chapter headings, `【完】` present, version footer once, hard forbidden route terms 0, but generated progress/length meta remained in the manuscript (`ここまでの文字数概算`, `現在の出力文字数`, `文字数条件`, `最低文字数`).
- Implemented `v4.7.1`: added a Gemini progress/length meta stripper and save-gate issue for generated character-count commentary, wrapped `We`, `J`, `A`, and the adopted-chapters-only final assembler so progress meta is removed/rejected before saved chapter bodies or final manuscripts. Version metadata synced in `src/main.js`, `package.json`, `package-lock.json`, `src/data.js`, and `index.html`; README changelog updated without documenting local QA API persistence.
- Verification completed for v4.7.1: `node --check src\main.js`, local JS regression for the exact progress-meta pattern, `npm run lint --if-present`, `git diff --check -- . ':!dist'` (LF-to-CRLF warnings only), `npm run build`, HTTP 200 on port 5179, and in-app browser title `Story Maker v4.7.1`.
- Quality/deploy judgment: do not deploy yet. OpenAI v4.7.0 passed functionally, and Gemini v4.7.0 completed but exposed final manuscript contamination plus weak story quality for the selected local comedy axis. v4.7.1 needs a fresh full API QA before deploy/tag/release. No deploy, tag, release, commit, push, or backup has been performed.

## Codex Update - 2026-06-07 JST - v4.7.0 story-means ledger / OpenAI QA complete

- Implemented the post-v4.6.7 hardening stack through `v4.7.0`: `v4.6.8` keeps completed long-novel output assembled from adopted/saved chapters only and adds a local-comedy surface-term gate; `v4.6.9` strengthens OpenAI's initial long-plan/first-chapter prose contract; `v4.7.0` strips leading generated plan/header lines before chapter extraction and save-time validation.
- The active design now treats USB/POS/data/amnesia/cult/facility/Project/AI/world-scale/lab/treasure-hunt style routes as one unselected-template intrusion class unless the selected story axes allow them. For local convenience-store / shopping-street / comedy long novels, the allowed means are store-work and human evidence such as paper receipts, handwritten shift notes, cash-count slips, shelf labels, packaging/cardboard, customer or shopkeeper testimony, lies, confession, apology, misunderstanding, repayment, refusal, and ordinary practical loss.
- Local verification completed for `v4.7.0`: `node --check src\main.js`, `npm run lint --if-present`, `git diff --check -- . ':!dist'` (only LF-to-CRLF whitespace warnings from existing files), `npm run build`, and HTTP 200 on port 5179 at `?qaApiSession=1&v=local-v4.7.0-plan-stripper`.
- Fresh in-app browser OpenAI long-novel QA completed with the saved in-app OpenAI key present (checked by input length only) and the intended 11 chips restored: long novel, daily life, convenience store, comedy, dotabata, modern Japan, shopping street, all ages, modern, happy ending, first person. Saved chapters: 1=5,862 chars, 2=7,495, 3=6,668, 4=6,634, 5=7,807, 6=7,551, 7=7,678, 8=6,670, 9=8,260, 10=6,563.
- Runtime gates behaved as intended: chapter 6 was rejected once for Project/AI route drift plus the `プロジェクト` surface term, then regenerated and saved; chapter 9 was rejected once for premature whole-story resolution, then regenerated and saved. Final scan passed: output 71,477 chars, 10 chapter headings, `【完】` present, `Generated By AI Story Maker V4.7.0` exactly once, 0 hard forbidden route terms, and 0 generated header/logline/structure artifacts.
- Gemini follow-up was attempted because both API keys were saved in the in-app browser. After switching to Gemini (key presence checked by input length only) and attempting to start a fresh run, the in-app browser tab stopped responding to DOM, screenshot, navigation, and developer-log reads. The local Vite server still returned HTTP 200 on port 5179. Gemini v4.7.0 full QA remains pending until the user manually reloads the in-app browser tab or permits losing the current displayed output.
- Not deployed, tagged, released, committed, pushed, or backed up.

## Codex Update - 2026-06-07 JST - v4.6.7 OpenAI first-chapter prose kickoff

- After v4.6.6, fresh OpenAI QA was rerun with saved in-app OpenAI key present (checked by value length only) and the intended 11 chips restored: long novel, daily life, convenience store, comedy, dotabata, modern Japan, shopping street, all ages, modern, happy ending, first person.
- OpenAI failed at chapter 1 before any saved chapter: first draft was rejected for title/logline/structure metadata; second draft was rejected by the v4.6.1 ledger as a digital/off-ledger route; final retry was only 1,577 chars and lacked visible shopping-street grounding. This is not the v4.6.6 route-level gate anymore.
- Implemented v4.6.7 in `src/main.js`: version advanced from `v4.6.6` to `v4.6.7`; added `smOpenAiFirstChapterKickoffV467()` and wrapped first-chapter draft/repair/continuation prompts so OpenAI local-comedy long novels must output prose only, start with first-person physical store action, include shopping-street/convenience-store grounding in the first 800 chars, avoid digital/POS/lab/memory proof routes, and continue beyond 5,200 Japanese chars before memo.
- Version metadata synced in `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog updated. Next verification: `node --check`, lint, diff-check, build, reload/HMR port 5179, then fresh OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-07 JST - v4.6.6 route-level local-comedy gate

- In-app browser Gemini QA on v4.6.5 completed all 10 chapters. Saved chapters: 1=5,490 chars, 2=5,958, 3=10,065, 4=9,876, 5=9,043, 6=5,664, 7=7,601, 8=5,694, 9=5,094, 10=8,558. Post-run body scan found 0 hard forbidden matches for USB, POS, register-data, sales-data, amnesia, cult, underground facility, secret society, AI weapon, Project, world-scale/state-secret routes, old maps, hidden boxes, smuggling, code, or lab route terms. Gen-4 editorial JSON parse still skipped.
- Follow-up OpenAI QA on v4.6.5 started successfully with the saved OpenAI key present (checked by input length only). Chapter 1 saved, but chapter 2 exhausted retries because the v4.6.5 local-comedy gate counted ordinary store wording such as boxes, shelf backs, notes, and mild unease as `hidden-box/secret-society/code` drift while the visible draft stayed on cash-count slips, shelf labels, receipts, packages, testimony, and customer/store dialogue.
- Implemented v4.6.6 in `src/main.js`: version advanced from `v4.6.5` to `v4.6.6`; added `smLocalComedyForbiddenRouteGateV466()` and wrapped the save gate to remove over-broad v4.6.5 local-comedy messages before adding route-level compound detection. The new gate rejects concrete hidden/conspiracy/code/smuggling routes, not mundane store-work nouns.
- Version metadata synced in `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog updated. Next verification: `node --check`, lint, diff-check, build, reload port 5179 at `?qaApiSession=1&v=local-v4.6.6-route-gate`, then fresh OpenAI long-novel QA followed by a quick Gemini sanity pass if time allows. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-07 JST - v4.6.5 concrete local-comedy chapter beat plan

- Fresh in-app browser Gemini QA on v4.6.4 failed before saving chapter 1. The model repeatedly selected strong hidden-template plot seeds despite the ledger: first a `do not open` box, then management metadata leakage, then secret-message/smuggling/secret-society/cult framing. The save gates correctly rejected all attempts, but the pre-drafting design was still too open.
- Implemented v4.6.5 in `src/main.js`: version advanced from `v4.6.4` to `v4.6.5`; added `smLocalComedyBeatV465()` and `smLocalComedyBeatContractV465()` so each convenience-store / shopping-street / comedy chapter receives a concrete store-work beat before drafting.
- The assigned beats rotate through campaign-card stamp mismatch, handwritten shift note contradiction, customer impatience, cash-count slip, shelf-label mismatch, product package/cardboard trouble, shopkeeper/customer testimony, delivery invoice, customer complaint slip, staff confession, awkward apology, town association note, receipt fragment, repayment, and final receipt/shift-note reconciliation.
- v4.6.5 explicitly forbids hidden boxes, open-me notes, secret messages, codes, smuggling, secret societies, cults, old maps/keys/photos, chosen people, underground rooms, ominous strangers, and historical conspiracies for this axis, and adds a save-time gate for those routes. v4.6.4 footer-safe Project/AI filtering remains in place.
- Version metadata synced in `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog updated. Next verification: `node --check`, lint, diff-check, build, reload port 5179 at `?qaApiSession=1&v=local-v4.6.5-local-beat-plan`, then fresh Gemini long-novel QA followed by OpenAI long-novel QA in the in-app browser. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-07 JST - v4.6.4 footer-safe Project/AI hard-route classifier

- Fresh in-app browser Gemini QA on v4.6.3 reached the final chapter and saved 9 / 10 chapters. The v4.6.3 local-comedy ledger fixed the earlier chapter-3 treasure-hunt drift: chapter 3 saved successfully, and the run stayed mostly on store-work trouble, campaign cards, receipts, product/package mistakes, and customer/shopkeeper testimony.
- The final chapter then exposed a remaining false-positive path: after contradiction repair, the save gate reported `v4.5.6軸逸脱` plus `kinds:grandScale` even though the visible chapter tail contained ordinary local phrases such as menu development, secret flavor, store notes, and the generated `AI Story Maker` footer, not hard Project/AI weapon/world-scale content.
- Implemented v4.6.4 in `src/main.js`: version advanced from `v4.6.3` to `v4.6.4`; added `smGrandRouteTextV464()` and wrapped `smHardGrandRouteV462()` so generated footer text is stripped before hard Project/AI classification. The real Project/AI/weapon/world-scale hard tokens remain blocked.
- Version metadata synced in `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog updated. Next verification: `node --check`, lint, diff-check, build, reload port 5179 at `?qaApiSession=1&v=local-v4.6.4-footer-safe-grandscale`, then fresh Gemini long-novel QA followed by OpenAI long-novel QA in the in-app browser. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-07 JST - v4.6.3 local comedy ledger narrowing

- Fresh in-app browser Gemini QA after v4.6.2 started successfully on `Story Maker v4.6.2` with saved Gemini key present (checked by value length only; key contents were not read). It saved chapter 1 and saved chapter 2 after one valid digital/POS-style rejection/retry.
- The run then failed at chapter 3 after exhausting automatic regeneration (`📖 第3章を再試行`). The failure was not the v4.6.2 grandScale false positive. It was repeated drift into a shopping-street treasure-hunt / interview-suspense route plus chapter-2 replay: old photos, map/key/box/treasure-hunt phrasing, heavy inquiry beats, low comedy count, and duplicated chapter 2 opening material. Representative gate reason: `v4.5.7 axis gate: chapter 3 replaced comedy/dotabata with suspense texture` plus duplicate/short chapter checks.
- Implemented v4.6.3 in `src/main.js`: version advanced from `v4.6.2` to `v4.6.3`; added a local-comedy profile for long novels where daily/convenience/shopping-street/comedy is selected without suspense; replaced the v4.6.1 ledger contract for that profile with a store-work trouble/proof/settlement ledger; narrowed allowed means to receipts, shift notes, cash-count slips, stock/shelf discrepancies, paper invoices, customer complaint slips, packaging/cardboard, campaign-card misunderstandings, customer/shopkeeper testimony, face-to-face lies/confessions, awkward apologies, bad timing, practical loss, debt, refusal, and betrayal.
- v4.6.3 explicitly forbids treasure hunts, old maps, hidden keys/boxes, ominous strangers, store-history conspiracy, chase/thriller investigation, and historical-secret adventure as the engine for this axis. It also adds a save-time local-comedy route gate for that class while leaving v4.6.2 grandScale filtering and the digital/POS/amnesia/cult gates intact.
- Version metadata synced in `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog updated. Next verification: `node --check`, lint, diff-check, build, reload port 5179 at `?qaApiSession=1&v=local-v4.6.3-local-comedy-ledger`, then fresh Gemini long-novel QA followed by OpenAI long-novel QA in the in-app browser. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-07 JST - v4.6.2 final-chapter ledger false-positive fix

- In-app browser Gemini QA with both API keys saved reached 9 / 10 chapters. The v4.6.1 story-means ledger worked as intended in chapter 8: it rejected a real non-ledger relapse involving digital/lab/cult/memory/grand-scale families, regenerated, and later saved chapter 8.
- Chapter 10 then exhausted the automatic retries on `kinds:grandScale` even though the visible final-chapter prose did not contain hard Project/AI/weapon/world-scale tokens. The likely cause was older broad classification that treated ordinary final-chapter wording such as fate/closure/plan/resolution as a grand-scale route.
- Implemented v4.6.2 in `src/main.js`: version advanced from `v4.6.1` to `v4.6.2`; added a narrow hard Project/AI/grand-route classifier; filtered `smKindSetV461`, the v4.6.1 ledger gate, and the final save quality gate so grandScale-only failures are kept only when hard Project/AI/weapon/conspiracy tokens are present. Digital/POS/lab, amnesia, cult/facility, and mixed failures remain blocked.
- Version metadata synced in `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog updated. Next verification: `node --check`, lint, diff-check, build, reload port 5179 at `?qaApiSession=1&v=local-v4.6.2-grandscale-hotfix`, then fresh Gemini long-novel QA followed by OpenAI long-novel QA in the in-app browser. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-07 JST - v4.6.1 story-specific allowed means ledger

- User confirmed the previous conclusion: chapter-by-chapter forbidden-word chasing has reached its limit. The fix must move up one layer: freeze a per-work ledger of allowed mystery/evidence/solution means before drafting, and treat every route outside that ledger as the same unselected-template intrusion.
- Implemented v4.6.1 in `src/main.js`:
  - version advanced from `v4.6.0` to `v4.6.1`;
  - added `smStoryMeansLedgerV461()` to derive a deterministic allowed-means ledger from the current long-novel settings;
  - added `smLedgerContractV461()` across shared prompt, first-chapter long-plan, next-chapter, continuation, and repair paths;
  - added `smStoryMeansLedgerGateV461()` as a save-time gate for non-ledger proof/solution routes, including disguised or renamed USB/data/POS/screen/camera/device, amnesia/lost-memory, cult/facility, Project/AI/weapon, and world-scale escape families when those axes were not selected;
  - the ledger explicitly permits local physical/human means such as paper receipts, register tape, handwritten shift notes, cash-count slips, packaging/cardboard, physical keys/photos, customer/shopkeeper testimony, face-to-face lies/confession, misunderstandings, awkward apology, practical debt, refusal, betrayal, and bodily cost.
- Version metadata synced in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog updated. During recovery, `src/main.js` was restored from the last clean v4.6.0 `dist` artifact to remove accidental replacement-character corruption, then the v4.6.1 ledger block was reapplied.
- Local verification completed: `node --check src/main.js`, `npm run lint --if-present`, `git diff --check`, `npm run build`, HTTP 200 on port 5179, browser reload shows `Story Maker v4.6.1`, and clicking the long-novel chip keeps comedy/all-ages axes active without returning to suspense/Tokyo/adult. Fresh Gemini long-novel QA followed by OpenAI long-novel QA is still pending because it requires user-entered API keys. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.6.0 unselected memory-template guard

- Fresh Gemini long-novel QA on v4.5.9 confirmed the POS/register-data fallback was blocked (`POS/register-data/sales-data` count 0 in the monitored draft), but exposed another long-mode default leak: the model used `記憶喪失を装う店長` / lost-memory framing as the title/logline/premise even though the visible selected axes were `コンビニ / ドタバタ / 商店街 / 全年齢`.
- Implemented v4.6.0 in `src/main.js`:
  - version advanced correctly from `v4.5.9` to `v4.6.0`;
  - added `smMemoryTemplateContractV460()` across shared prompt, long-plan, next-chapter, continuation, and repair paths;
  - added `smMemoryTemplateGateV460()` to reject unselected amnesia/fake-amnesia/lost-memory/recovered-memory/forgotten-past routes as title, logline, premise, core mystery, chapter engine, or ending;
  - the replacement route is paper receipts, shift notes, mistaken campaign cards, physical keys/photos, testimony, lies, confession, or a store-work misunderstanding;
  - added a final quality-gate wrapper to filter false v4.5.6 Project/AI failures when the prose lacks hard Project/AI/weapon tokens, so ordinary "plan" wording does not waste retries.
- Version metadata synced in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog updated.
- Verification required next: `node --check`, lint, diff-check, build, reload port 5179 at `?qaApiSession=1&v=local-v4.6.0-memory-template-guard`, verify long-mode click still preserves axes, then fresh Gemini long-novel QA followed by OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.5.9 POS/register data fallback guard

- Fresh Gemini long-novel QA on v4.5.8 confirmed the UI-state fix: clicking long-novel mode preserved visible axes (`コンビニ`, `ドタバタ`, `商店街`, `誰でも楽しめる`, `2020年代`, `大団円`) instead of reverting to suspense/Tokyo/adult defaults.
- The same QA also proved why a beginning-only "no USB/data/device" declaration is still insufficient. The story no longer jumped to USB/device/AI/lab, but it started using convenience-store-shaped digital proof: `レジのログ`, `売上データ`, `レジのデータ`, and register display information as clue candidates.
- Implemented v4.5.9 in `src/main.js`:
  - version advanced to `v4.5.9`;
  - added `smPosDataContractV459()` across shared prompt, long-plan, next-chapter, continuation, and repair prompt paths;
  - added `smPosDataGateV459()` to reject unselected POS/register/sales-data/screen/camera-log routes as proof, twist, contradiction resolver, or ending;
  - register scenes remain allowed as normal store-work props, but evidence must come from printed receipts, register tape, handwritten shift notes, cash-count slips, paper invoices, customer testimony, staff lies, physical packages, and face-to-face confession.
- Version metadata synced in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog updated.
- Verification required next: `node --check`, lint, diff-check, build, reload port 5179 at `?qaApiSession=1&v=local-v4.5.9-pos-register-guard`, verify long-mode click still preserves axes, then fresh Gemini long-novel QA followed by OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.5.8 mode-change axis preservation

- v4.5.7 browser QA exposed the original UI-state bug again before generation started: clicking the long-novel mode chip changed the visible `daily-life / convenience store / comedy-dotabata / modern Japan / shopping street / all ages` setup into long-mode defaults (`mystery / lost memory / suspense / Tokyo / adult / surprise`).
- Implemented v4.5.8 in `src/main.js`:
  - version advanced to `v4.5.8`;
  - changed mode chip selection to call `Pt(s.mode,{ forceModeLabel: true, includeAxes: false })`;
  - changed mode randomization the same way;
  - kept startup/reset default application intact, but output-mode changes no longer rewrite theme/genre/worldview/target/era/ending/narration axes.
- Verification required next: `node --check`, lint, diff-check, build, reload port 5179 at `?qaApiSession=1&v=local-v4.5.8-mode-axis-preserve`, verify that clicking long mode preserves the visible axes, then fresh Gemini long-novel QA. If Gemini passes, continue to OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.5.7 selected-axis local adherence gate

- Fresh v4.5.6 Gemini long-novel QA was run with `long novel / daily-life / convenience store / comedy-dotabata / modern Japan / shopping street / all ages`.
- Result: the first draft drifted into memory-loss suspense and was rejected by the digital gate once, but the retry was still saved as a serious memory/Tokyo/underground-office suspense chapter. This proved the issue is not just USB/data/device. The local setting-consistency path was too weak: it accepted an off-axis story because it did not require the selected settings to appear as visible prose anchors.
- Implemented v4.5.7 in `src/main.js`:
  - version advanced to `v4.5.7`;
  - added `smAxisAdherenceGateV457()` as a local save-time gate for selected daily/convenience-store/shopping-street/comedy axes;
  - added `smAxisContractV457()` across first-chapter, continuation, next-chapter, and repair prompts;
  - wrapped continuation prompt `Bo()` so partial off-axis drafts are not simply extended blindly;
  - the gate rejects title/logline metadata in chapter prose, missing convenience-store/shopping-street anchors, comedy-to-suspense replacement, and unselected memory/Tokyo-thriller template takeover even when the AI contradiction checker reports no contradiction.
- Verification required next: `node --check`, lint, diff-check, build, reload port 5179 at `?qaApiSession=1&v=local-v4.5.7-axis-anchor`, verify QA API restore, then fresh Gemini long-novel QA. If Gemini passes, continue to OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.5.6 project/AI weapon escape guard

- Fresh v4.5.5 Gemini long-novel QA was run with daily-life/comedy/storefront/all-ages settings (`長編小説`, `日常・生活/コンビニ`, `コメディ/ドタバタ`, `現代日本/商店街`, `全年齢`).
- Result: v4.5.5 correctly rejected the first chapter's project-like clue once, but the retry still saved `Aoi Project`. Chapter 2 then escalated into `AI兵器`, secret development, giant conspiracy, and chase-thriller prose. The run was aborted at 1/10 saved chapters to avoid wasting API.
- Implemented v4.5.6 in `src/main.js`:
  - version advanced to `v4.5.6`;
  - added `smProjectAIEscapeContractV456()` across first outline/chapter, next-chapter, repair, and retry paths;
  - added `smProjectAIGateV456()` to reject unselected `Project`, `プロジェクト`, `計画`, `開発`, `AI`, `人工知能`, `AI兵器`, `兵器`, `巨大な陰謀`, `命がけの争奪戦`, and chase-thriller replacements of daily/comedy axes;
  - the replacement rule forces the same story function back to letters, photographs/film, signatures, receipts, testimony, lies, betrayal, refusal, bodily cost, storefront work, conversation, mistakes, or local human stakes.
- Version metadata synced in `src/main.js`, `package.json`, `package-lock.json`, and `index.html`; README changelog includes bilingual English/Japanese v4.5.6 notes.
- Still required after this note: `node --check`, lint, diff-check, build, reload port 5179 at `?qaApiSession=1&v=local-v4.5.6-project-ai-guard`, verify API restore, then fresh Gemini long-novel QA. If Gemini passes, continue to OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.5.5 always-on axis escape lock

- Fresh v4.5.4 Gemini long-novel QA answered the user's question directly: a beginning-only declaration that USB/data/device/lab ideas are forbidden is not enough.
- The run initially rejected and regenerated digital/research drift, but later completed with a different unselected large-template route: religious order/cult framing, brainwashing, purification, underground/special facility, "soul/rebirth" logic, and a giant villain plan. USB/data/device counts were mostly controlled, but the story escaped into another off-axis mechanism.
- Implemented v4.5.5 in `src/main.js`:
  - version advanced to `v4.5.5`;
  - added `smAlwaysContractV455()` as an always-on long-novel chapter/continuation/repair/retry contract, not just late-stage or post-rejection;
  - added `smCultFacilityGateV455()` to reject unselected religious-order/cult/brainwashing/purification/underground-facility/research-memory routes when they become proof, core threat, reveal, or ending;
  - added Japanese detection terms for the actual failure surface: `宗教団体`, `教団`, `信者`, `洗脳`, `浄化`, `地下施設`, `白い壁の部屋`, `別の部屋`, `光の園`, `蝶の再生`, `記憶操作`, `記憶の再構築`, etc.;
  - prompt contracts force the same plot function back to people, paper evidence, photographs/film, signatures, receipts, testimony, betrayal, refusal, confession, bodily cost, or visible object loss/damage.
- Version metadata synced in `src/main.js`, `package.json`, `package-lock.json`, `index.html`; README changelog includes bilingual English/Japanese v4.5.5 notes.
- Still required after this note: run `node --check src/main.js`, lint, diff-check, build, reload port 5179 at `?qaApiSession=1&v=local-v4.5.5-axis-lock`, verify version/API restore, then fresh Gemini long-novel QA. If Gemini passes, continue to OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.5.4 late-stage escape-route lock

- User asked whether simply declaring "USB/data/device-style ideas are forbidden" at the beginning would be enough.
- Fresh v4.5.3 Gemini long-novel QA answered no: the run reached chapter 11, but repeated late retries drifted across multiple rejected routes, including unselected digital props, ritual/sacrifice folklore, chapter-boundary replay, premature "everything is over" closure, and evidence-inventory contradictions.
- Implemented v4.5.4 in `src/main.js`:
  - version advanced to `v4.5.4`;
  - added `smLateEscapeTextV454`, `smLateEscapeKindsV454`, and `smLateEscapeContractV454`;
  - wrapped long-novel next-chapter and repair prompt paths so late chapters and compound retry failures discard all rejected routes as non-canon;
  - the contract forces continuation from saved canon, existing physical evidence, face-to-face conversation, refusal, betrayal, confession, debt, bodily cost, or visible loss/damage of an already-held object;
  - it forbids late chapter solutions using USB/data/device/research/experiment/capsule/ritual/sacrifice/cult/world-scale machinery unless explicitly selected.
- Version metadata synced in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog includes bilingual English/Japanese v4.5.4 notes.
- Still required after this note: run `node --check src/main.js`, lint, diff-check, build, reload port 5179 in the in-app browser at `?qaApiSession=1&v=local-v4.5.4-late-escape`, verify API restore, then rerun fresh Gemini long-novel QA. If Gemini passes, continue to OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.5.3 evidence handoff state lock

- Fresh v4.5.2 Gemini long-novel QA was run in the in-app browser with the same stress settings. The research/experiment/test-subject drift was no longer the blocker: saved text had no `実験`, `被験者`, or `世界が変わる` contexts, and the remaining `カプセル` occurrence was only a physical simile ("タイムカプセルのようだった").
- New blocker: chapter 7 repeatedly tried to rediscover/re-receive police records and a loan contract that chapter 6 context already marked as received and understood. Audit repair could not fix it without output damage, and the run was aborted at 6/10 saved chapters.
- Implemented in `src/main.js`:
  - version advanced to `v4.5.3`;
  - added `smEvidenceContractV453()` across first, next-chapter, and repair prompt paths;
  - saved chapters/context memos are now the only canon for physical evidence state;
  - once police records, contracts, letters, envelopes, notebooks, photos, film negatives, keys, ledgers, receipts, medical notes, victim reports, or signed/sealed papers are received/read/shown/used/lost/hidden, later chapters must not rediscover them, ask where they are, promise to receive them later, or stage first handoff again;
  - later chapters must instead use, confront with, hide, damage, refuse to show, notice a missed detail in, or pay a cost because of the already-held evidence.
- Version metadata is now `v4.5.3` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog includes bilingual English/Japanese v4.5.3 notes.
- Still required: `node --check`, lint, diff-check, build, reload port 5179 in the in-app browser, verify API remains present-redacted, then fresh Gemini long-novel QA followed by OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.5.2 research/experiment template guard

- Fresh v4.5.1 Gemini long-novel QA was run in the in-app browser with `長編小説 / 消えた記憶 / 二転三転 / 東京 / 2020年代 / 伏線回収の衝撃 / 群像劇`.
- Result: v4.5.1 improved the prior failure. Chapter 7, which failed badly in v4.5.0 (`digital:65 / payoff:10 / lab:2`), saved cleanly this time with no mass USB/data/lab relapse. However chapter 8 saved a new off-axis variant through research/experiment wording: the memo contained "被害者", "神崎に騙された", "焦げ付く匂いが実験の成功の証", and a rejected earlier draft also exposed "被験者データ". The run was aborted at 8/12 saved chapters.
- Implemented in `src/main.js`:
  - version advanced to `v4.5.2`;
  - added `smExperimentIssuesV452()` and a quality-gate wrapper so research/experiment/test-subject templates are blocked even when explicit lab/capsule/device words are absent;
  - added `smExperimentContractV452()` across shared, first, next-chapter, and repair prompt paths;
  - unselected long novels now reject experiment success/failure, test subjects, clinical/research plans, research data, "the world will change" slogans, and chemical/smell proof of experiments unless the user selected AI/SF/digital/research axes;
  - replacement route is human-scale harm: forged consent, unpaid debt, insurance fraud, workplace accident cover-up, victim report, diagnosis note, signed/sealed paper, receipt trail, witness silence, betrayal, refusal, or bodily cost.
- Version metadata is now `v4.5.2` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog includes bilingual English/Japanese v4.5.2 notes.
- Still required: `node --check`, lint, diff-check, build, reload port 5179 in the in-app browser, verify API remains present-redacted, then fresh Gemini long-novel QA followed by OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.5.1 false-route purge retry law

- Fresh v4.5.0 Gemini long-novel QA was run in the in-app browser with local QA key restore active. Settings matched the user's recurring stress case: `長編小説 / 消えた記憶 / 二転三転 / 東京 / 2020年代 / 伏線回収の衝撃 / 群像劇`.
- Result: v4.5.0 improved the run but did not fully solve it. Chapters 1-6 saved, with chapter 1 passing first try and chapter 6 passing after one regeneration. However chapter 7 drifted hard back into unselected digital/lab mechanics: logs showed `digital:65 / payoff:10`, `engine:5`, and `lab:2 / core:1`, plus a premature whole-story resolution pattern. The QA run was aborted at 6/10 saved chapters to avoid wasting API tokens.
- Implemented in `src/main.js`:
  - version advanced to `v4.5.1`;
  - added `smCarrierPurgeContractV451()` and wrappers for first, next-chapter, and repair prompt paths;
  - rejected USB/data/device/lab/capsule/memory-device drafts are now treated as false routes, not drafts to repair;
  - retries and repairs now explicitly delete the failed premise before planning, forbid even theory/background mentions of unselected digital/lab carriers, and force the next chapter to open with a human/physical action tied to paper/film/key/scar/receipt/testimony/confession/betrayal/refusal/cost.
- Version metadata is now `v4.5.1` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog includes bilingual English/Japanese v4.5.1 notes.
- Still required: `node --check`, lint, diff-check, build, reload port 5179 in the in-app browser, verify API remains present-redacted, then fresh Gemini long-novel QA followed by OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.5.0 analog mystery carrier ledger

- Fresh Gemini long-novel QA on v4.4.9 answered the user's question directly: a beginning-of-prompt ban is not enough. With settings `長編小説 / 消えた記憶 / 二転三転 / 東京 / 2020年代 / 伏線回収の衝撃 / 群像劇`, chapter 1 initially returned to digital proof and passed only after two full regenerations. Chapter 2 then repeatedly returned to AI/device/data/research-room/capsule/memory-device mechanics. The run was aborted before saving chapter 2 to avoid wasting tokens.
- Implemented in `src/main.js`:
  - version advanced to `v4.5.0` (correctly after v4.4.9);
  - added `smGlobalCarrierContractV450()` and `smChapterCarrierContractV450()`;
  - unselected digital/SF/lab long novels now assign an analog mystery carrier before drafting each chapter, not merely a ban list;
  - assigned carriers rotate through old photographs, handwritten notes, witness names, sealed envelopes, paper ledgers, receipts, physical keys, locked drawers, scars, signatures, police records, betrayal, refusal, confession, debt, and bodily cost;
  - "lost memory" is explicitly defined as human/social absence and contradictory physical evidence, not memory extraction, memory manipulation, memory devices, capsules, labs, apps, servers, USB, or analysis screens;
  - first chapter, next chapter, shared prompt, and repair prompt paths receive the carrier contract.
- Version metadata is now `v4.5.0` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog includes bilingual English/Japanese v4.5.0 notes.
- Still required: `node --check`, lint, diff-check, build, reload port 5179 in the in-app browser, verify API remains present-redacted, then fresh Gemini long-novel QA followed by OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.4.9 analog repair scope and online-search repair

- User asked whether simply declaring "USB/data/device stories are forbidden" at the beginning should be enough.
- Diagnosis: it helps, but is not enough for long novels. The model rebuilds context at first draft, retry, continuation, cleanup, and repair stages, so habitual "convenient proof" carriers such as smartphone zoom, online search, PC screens, USB, data, devices, and labs can reappear unless each stage either replaces the function or rejects the chapter before it becomes saved canon.
- Implemented in `src/main.js`:
  - version advanced to `v4.4.9`;
  - v4.4.8 analog repair no longer logs repeatedly during streaming/body cleanup paths without chapter context;
  - lightweight online-search habits are repaired to paper records, old indexes, handwritten notes, maps, and physical reference checks when digital/SF/lab axes are not selected;
  - core USB/device-data/server/lab/capsule/memory-device/memory-operation story engines remain hard save-gate failures, not silent rewrites.
- Version metadata is now `v4.4.9` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog includes bilingual English/Japanese v4.4.9 notes.
- Verification passed so far: `node --check src/main.js`. Still required: lint, diff-check, build, reload port 5179 in the in-app browser, verify API remains present-redacted, then fresh Gemini long-novel QA followed by OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.4.8 analog repair for minor digital habits

- v4.4.7 QA proved the user's concern: even when the no-digital rule is placed at the front of the prompt, Gemini can still fall back to habitual everyday actions such as "zooming a photo with a smartphone" or "moving away from a PC." Prompt declaration alone is not enough.
- Implemented in `src/main.js`:
  - version advanced to `v4.4.8`;
  - `smAnalogRepairV448()` locally repairs minor unselected digital habits in long-novel output before save-gate checks;
  - examples repaired: smartphone/photo zoom/camera/contact shots -> loupe + desk lamp + paper photo; PC screen / moving away from PC -> desk papers / desk;
  - wrappers now apply this repair through clean-text, memo-split, body-clean, and save-gate input paths;
  - core digital/lab engines such as USB, data files, servers, research rooms, capsules, and memory devices are not silently converted. They remain hard save-gate failures to avoid saving incoherent story engines.
- Version metadata is now `v4.4.8` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog includes bilingual English/Japanese v4.4.8 notes.
- Verification passed so far: `node --check src/main.js`. Still required: lint, diff-check, build, reload port 5179 in the in-app browser, verify API remains present-redacted, then fresh Gemini long-novel QA followed by OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.4.7 first-draft zero-tolerance carrier lock

- User asked why simply declaring "USB/data/device stories are not allowed" at the beginning did not stop the model from drifting there.
- Diagnosis: a warning appended later in the prompt is too weak for the strong `lost memory + suspense + twist` prior. The recurring digital/lab route must be blocked before outline and first-sentence design, not only after save-gate rejection.
- Implemented in `src/main.js`:
  - version advanced to `v4.4.7`;
  - `smZeroDigitalCarrierContractV447()` prepends a first-draft law for long-novel prompts when no digital/SF/lab/device axis is selected;
  - code names, hidden networks, D-plan style project labels, servers, PCs, emails, data trails, research rooms, capsules, and memory devices are forbidden as mystery carriers before outlining;
  - first chapters must start from paper evidence, physical objects, witnesses, direct accusations, betrayal, refusal, confession, debt, scars, or bodily cost;
  - `smRetryZeroDigitalCarrierContractV447()` applies the same lock immediately on the first retry after a digital/lab rejection, not only after multiple repeated failures;
  - first-chapter, next-chapter, shared long prompt, and repair prompt paths are all wrapped.
- Version metadata is now `v4.4.7` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog includes bilingual English/Japanese v4.4.7 notes.
- Verification passed so far: `node --check src/main.js`. Still required: lint, diff-check, build, reload port 5179 in the in-app browser, verify API remains present-redacted, then fresh Gemini long-novel QA followed by OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.4.6 repeated digital retry lock and QA abort automation

- User asked whether repeated regeneration can dynamically strengthen the next instruction and whether manual confirmation dialogs during QA can be automated.
- Implemented in `src/main.js`:
  - version advanced to `v4.4.6`;
  - repeated unselected digital/lab/device failures now trigger a hard retry lock after the second failure, forbidding USB, device data, PC/screen/email/smartphone clues, digital files, downloads/uploads, analysis screens, research/experiment rooms, capsules, and memory-operation devices across the whole chapter unless the selected axes explicitly permit them;
  - the retry lock forces proof/payoff back into paper evidence, physical objects, handwriting, photographs/film, ledgers, receipts, sealed envelopes, keys, scars, testimony, direct conversation, betrayal, refusal, confession, bodily cost, or practical human choice;
  - repair prompts receive the same hard digital lock so they rebuild the failed mechanism instead of renaming the same gadget;
  - fantasy/isekai rejection is narrowed so generic terms do not become a false "magic/reincarnation" failure unless real fantasy tokens appear in the prose;
  - local QA restore mode now auto-accepts generation-abort confirmation dialogs, so automated verification can stop/reload without waiting for a manual OK button. Normal/public pages still keep the native confirmation.
- Version metadata is now `v4.4.6` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog includes bilingual English/Japanese v4.4.6 notes.
- Still required after this note: `node --check`, lint, diff-check, build, reload port 5179 in the in-app browser, verify QA key restore/no-flag restore and auto abort confirmation, then fresh Gemini long-novel QA followed by OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.4.5 physical evidence scope and QA restore continuity

- Fresh in-app Gemini long-novel QA on v4.4.4 confirmed the dynamic retry layer is active: chapter 1 passed after a stronger retry, chapter 2 passed after repeated digital-drift retries, and later chapters continued through audit repair/regeneration instead of blindly saving broken prose.
- The run also exposed a new wasteful false-positive class: the anti-digital gate counted ordinary paper `ファイル` / locked office files / physical evidence as digital props, causing repeated regeneration even when the chapter had moved toward exactly the requested human-scale proof carrier.
- Implemented in `src/main.js`:
  - version advanced to `v4.4.5`;
  - physical files, locked folders, ledgers, notebooks, photos/film, signatures, seals, keys, scars, and ordinary pendants are treated as valid physical evidence instead of digital props;
  - real unselected USB/device/data/server/lab/capsule/memory-operation engines remain blocked by the save gate;
  - long-novel prompts and repair prompts now clarify that `file`/`key` words are allowed when they are paper or physical evidence, and the decisive payoff should come from handwriting, testimony, signatures, damage, refusal, betrayal, or cost;
  - local development QA restore now survives browser restart and local URL normalization after explicit opt-in, so a reopened localhost page can restore saved QA keys even if the QA query flag was dropped. Public/non-local pages remain memory-only and do not show the QA UI.
- Version metadata is now `v4.4.5` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog includes bilingual English/Japanese v4.4.5 notes.
- Still required after this note: run node check, lint, diff-check, build, reload port 5179 in the in-app browser, verify local QA restore, then fresh Gemini long-novel QA followed by OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.4.4 separator self-repair

- Fresh in-app Gemini QA on v4.4.3 confirmed the adaptive retry layer was active: chapter 1 regenerated after underlength, unselected digital-core drift, and separator-artifact failures. It still stopped after a removable standalone `---` consumed the retry budget, which is the wrong behavior for a trivial local artifact.
- Implemented in `src/main.js`:
  - version advanced to `v4.4.4`;
  - `smStripStandaloneSeparatorsV444()` removes isolated separator/control lines before cleanup, memo split, and save-gate checks;
  - valid prose is no longer fully regenerated merely because of a removable divider line;
  - real contamination and selected-setting/template failures remain save-gate failures and still route into the v4.4.3 dynamic retry escalation.
- Version metadata is now `v4.4.4` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog includes bilingual English/Japanese v4.4.4 notes.
- Still required after this note: run node check, lint, diff-check, build, reload port 5179 in the in-app browser, verify local QA API restore, then fresh Gemini long-novel QA followed by OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.4.3 adaptive retry and QA persistence hardening

- Resume context: v4.4.2 Gemini long-novel QA showed that dynamic regeneration was working for some failures (contradiction repair/regeneration and mid-story digital drift), but the run still exposed two product defects before completion: audit/API transport failures could be treated like story-quality failures, and raw separator artifacts such as `区切り線 ---` could appear in prose.
- Implemented in `src/main.js`:
  - version advanced to `v4.4.3`;
  - `smSeparatorArtifactIssuesV443()` rejects raw separators/control artifacts (`---`, `区切り線 ---`, separator labels) before chapter save;
  - `smRetryEscalationExtraV443()` adds another dynamic retry tier for separator artifacts, early-resolution failures, and repeated template takeover, forcing mechanism replacement through human action and physical evidence;
  - `smOperationalAuditErrorV443()` marks audit/API/network failures as operational errors so they do not consume the chapter story-regeneration budget;
  - `smQaPersistNowV443()` re-saves local QA API keys at key save, provider switch, long-generation start, next-chapter continuation, visibility-hide, and unload time, still only for explicit localhost `qaApiSession` URLs.
- Version metadata is now `v4.4.3` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog includes bilingual English/Japanese v4.4.3 notes.
- Verification passed after this note: `node --check src/main.js`, `npm run lint --if-present`, `git diff --check -- src\main.js package.json package-lock.json index.html src\data.js README.md HANDOFF.md`, workspace `PLAN.md` diff-check, and `npm run build`.
- Still required after this note: in-app browser reload at port 5179, then fresh Gemini long-novel QA followed by OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.4.2 full-template retry escalation

- Resume context: v4.4.1 local QA proved that dynamic retry escalation works better than repeating the same weak prompt, but it was still too narrow. Fresh Gemini long-novel QA saved chapters 1-3 after targeted retries, then chapter 4 exposed additional failure classes: chapter-boundary replay, unselected sacrifice/ritual/folklore/cult drift, and grand/chuni-style escalation. The run was stopped before wasting more API tokens.
- Implemented in `src/main.js`:
  - version advanced to `v4.4.2`;
  - `smRetryKindsV442()` expands retry classification to chapter-boundary replay, folklore/cult/sacrifice drift, and grand/chuni/isekai/world-scale drift;
  - `smRetryEscalationExtraV442()` adds failure-specific retry instructions so the next generation must change the failed carrier, not merely reword the same bad draft;
  - `smFirstDraftTemplateContractV442()` adds a long-novel first-draft carrier lock that pushes proof/payoff into human testimony, paper evidence, physical objects, dialogue, betrayal, refusal, confession, or cost before prose is written;
  - repair prompts now get matching v4.4.2 instructions for replay, folklore/cult, and grand/chuni failures;
  - `scripts/inapp_browser_uia.ps1` now supports semantic `pause`, `abort`, and `reload` actions for the Codex in-app browser via AutomationId, not screen coordinates.
- Version metadata is now `v4.4.2` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog includes bilingual English/Japanese v4.4.2 notes.
- Still required after this note: lint, diff-check, build, HTTP/browser reload, then fresh Gemini long-novel QA and OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.4.1 dynamic retry escalation and QA auto-keep

- User correctly pointed out two remaining product problems:
  - if a chapter needs regeneration, the retry instruction must become stronger based on the actual rejection reason and repeated failure count, not merely ask the model to try again;
  - the local QA API helper is not useful enough if interrupted QA keeps forcing manual key re-entry.
- Implemented in `src/main.js`:
  - version advanced to `v4.4.1`;
  - added `smRetryEscalationV441()` and wrappers for first-chapter, subsequent-chapter, and repair prompts;
  - repeated failure classes now get targeted retry pressure for unselected digital/lab drift, underlength chapters, paragraph/rhythm defects, duplicate/restarted prose, early whole-story resolution, canon contradictions, and internal label/memo leakage;
  - repeated unselected digital/lab failures explicitly force a different proof carrier and convert the function into human testimony, paper evidence, physical objects, dialogue, betrayal, refusal, or cost instead of preserving rejected USB/device/lab drafts;
  - local `qaApiSession` remains localhost + explicit query flag only, but key save/switch now automatically turns QA keep on and persists saved Gemini/OpenAI keys to the existing localStorage-backed QA session.
- Version metadata is now `v4.4.1` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog includes bilingual English/Japanese v4.4.1 notes.
- Still required after this note: `node --check`, lint, diff-check, build, HTTP/browser reload, then fresh Gemini long-novel QA and OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.4.0 mid-story digital/lab drift guard

- Resume context: fresh Gemini long-novel QA on v4.3.9 no longer reproduced the v4.3.7 domestic-comedy misfire. Chapters 1-5 saved under the visible suspense/mystery/Tokyo axes, but chapter 6 repeatedly drifted into unselected USB/device/data plus research room/capsule/memory-device explanations. The existing gates rejected several drafts, but the retry prompt kept returning to the same digital/lab core, so this was treated as a product failure before spending more API tokens.
- Implemented in `src/main.js`:
  - version advanced to `v4.4.0`;
  - added `smMidDigitalLabCoreIssuesV440()` as a hard save-time guard when AI/SF/digital/lab/device axes are not explicitly selected;
  - added Gemini/OpenAI shared prompt, long-chapter, and repair rules that mark rejected USB/lab drafts as non-canon and force the same plot function into human stakes, physical evidence, testimony, face-to-face conversation, betrayal, refusal, or cost;
  - kept explicit permission for genuinely selected digital/SF/lab categories so those genres can still use devices when the user asked for them.
- Version metadata is now `v4.4.0` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`; README changelog includes bilingual English/Japanese v4.4.0 notes.
- Still required after this note: `node --check`, lint, diff-check, build, reload/verify port 5179, then fresh Gemini long-novel QA followed by OpenAI long-novel QA if Gemini passes.
- Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.3.9 domestic-comedy gate scope fix

- Fresh Gemini long-novel QA was started after the user re-entered both API keys. The run was stopped before completion because the v4.3.7 domestic-comedy tone guard misfired on a long-novel suspense/mystery default run (`????? / ???? / ?? / ???????`). It rejected chapter 1 with `?????????v4.3.7` and began regenerating toward ???/?????? texture even though the visible fixed axes were suspense/mystery.
- Root cause: the domestic-comedy predicate used `pe()`/`oe()` defaults, so missing save-gate settings fell back to `???? / ???? / ???`; it also treated broad axes such as `????` and `???` as enough to activate the domestic-comedy guard.
- Implemented in `src/main.js`:
  - version advanced to `v4.3.9`;
  - `smDomesticAxisTextV439()` reads raw fixed settings without `pe()` defaults;
  - `Pn()` now requires explicit daily-life/comedy intent and excludes suspense/mystery/action/SF/fantasy style axes;
  - `B()` now receives the active long-novel settings during save-time quality checks, so setting-dependent guards judge the selected axes rather than default fallbacks.
- Verification passed after this note: `node --check src/main.js`, `npm run lint --if-present`, `git diff --check -- src/main.js package.json package-lock.json index.html src/data.js README.md HANDOFF.md`, `npm run build`, and in-app browser smoke showing `Story Maker v4.3.9` with no warning/error logs.
- Browser is now on `http://localhost:5179/?qaApiSession=1&v=local-v4.3.9-gate-scope`; the QA keep checkbox is visible and ON, but the API field is empty because the previous normal-memory tab had to be closed to stop the faulty Gemini run. User must re-enter Gemini/OpenAI keys in this QA URL before full QA resumes. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.3.8 persistent local QA API restore

- User correctly pointed out that the v4.3.5 QA helper was insufficient: because it used same-tab `sessionStorage`, closing the browser/tab during an interrupted full QA run forced API re-entry and defeated the purpose of the helper.
- Implemented in `src/main.js`:
  - version advanced to `v4.3.8`;
  - the existing local-only `qaApiSession` helper is overridden with a v4.3.8 restore path backed by `localStorage`, so it survives browser/tab restarts on localhost when explicitly enabled;
  - this is still restricted to local hosts plus the explicit QA query flag, and normal/public pages neither render the QA UI nor restore stored keys;
  - the QA status text now says browser restart restore instead of same-tab reload restore.
- Version metadata is now `v4.3.8` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, `src/data.js`, and README changelog.
- README intentionally does not document the hidden QA query flag. Public API-key behavior remains memory-only.
- Verification passed: `node --check src/main.js`, `npm run lint --if-present`, `git diff --check -- src/main.js package.json package-lock.json index.html src/data.js README.md HANDOFF.md`, and `npm run build`.
- In-app browser checks passed: normal `http://localhost:5179/` hides the QA helper and keeps the API field empty; QA `http://localhost:5179/?qaApiSession=1` shows the helper; enabling it survived closing the tab and opening a fresh in-app browser tab. Browser warning/error logs were empty on the normal page.
- During recovery, `index.html` and JSON BOM issues introduced by a prior PowerShell encoding write were corrected before build. Not deployed, tagged, released, or backed up.
## Codex Update - 2026-06-06 JST - v4.3.7 domestic comedy tone guard

- Fresh Gemini long-novel QA on `v4.3.6` was intentionally stopped as a quality failure before completion. The v4.3.6 chapter-boundary anti-replay fix helped: chapters 1-4 saved without the previous chapter-opening replay problem. However chapter 5 drifted from the selected/manual `譌･蟶ｸ繧ｳ繝｡繝・ぅ / 蝠・ｺ苓｡・/ 繧ｳ繝ｳ繝薙ル / 霆ｽ縺・Α繧ｹ繝・Μ繝ｼ` axes into suspense-thriller texture (`霑ｽ霍｡閠・, `蜊ｱ髯ｺ縺ｪ鬆伜沺`, `騾・ｵｰ`, `蜀ｷ縺溘＞隕也ｷ啻, `繧ゅ≧螳牙・縺ｧ縺ｯ縺ｪ縺・Κ螻義) and the save gate still accepted it.
- Implemented in `src/main.js`:
  - version advanced to `v4.3.7`;
  - `smDomesticComedyToneContractV437()` adds Gemini/OpenAI shared long-novel guidance so light mystery stays a spice, not a thriller takeover;
  - long-chapter context and repair prompts now redirect this drift into shop work, shifts, receipts, handwritten notes, local gossip, face-to-face confession, embarrassment, comic timing, and human relationship cost;
  - `smDomesticComedySuspenseIssuesV437()` rejects domestic-comedy chapters where pursuer/danger/surveillance/crime-fear language overwhelms life/comedy texture.
- Version metadata is now `v4.3.7` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, `src/data.js`, and README changelog.
- Still required: `node --check`, lint, diff-check, build, HTTP/browser reload, then fresh Gemini and OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.3.6 chapter-boundary anti-replay

- Resume context: Gemini long-novel QA on v4.3.5 exposed a repeated chapter-boundary bug before completion. Chapter 1/2/3/5/8 repeatedly replayed or copied previous-chapter openings, departures, investigations, or paragraph blocks, and one repair shrank the chapter below the save minimum. The run was stopped to avoid wasting more API tokens.
- Implemented in `src/main.js`:
  - version advanced to `v4.3.6`;
  - `smChapterBoundaryContractV436()` is appended to shared Gemini/OpenAI long-novel prompt contracts;
  - chapter continuation context now explicitly forbids replaying/copying prior prose and requires a new action, conversation, obstacle, object exchange, or human cost within the first 900 characters;
  - repair prompts now require duplicate prose to be replaced with new scene material, not simply deleted below the chapter minimum or overcompensated by jumping to the final solution;
  - `smChapterBoundaryReuseIssuesV436()` adds a save-gate check for previous-chapter start-slice reuse before polluted prose can enter saved chapters.
- Version metadata is now `v4.3.6` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, `src/data.js`, and README changelog.
- Still required: run `node --check`, lint, diff-check, build, HTTP/browser reload, then fresh Gemini and OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.3.5 local QA API session helper

- User asked whether repeated API-key entry can be avoided while iterating through `generate -> find defect -> patch -> regenerate`, and whether this can be deployed safely.
- Implemented local-only API session support in `src/main.js`:
  - version advanced to `v4.3.5`;
  - `smApiSessionRenderV435()` adds an opt-in `讀懆ｨｼ逕ｨAPI荳譎ゆｿ晄戟` checkbox and clear button only on local URLs with explicit `?qaApiSession=1`;
  - when enabled, `smApiSessionWriteV435()` keeps Gemini/OpenAI keys in the same tab's `sessionStorage` after user saves them in the UI;
  - `smApiSessionRestoreV435()` restores the saved provider/key pair on reload only in local QA contexts;
  - public GitHub Pages deployment does not show or restore this QA helper, so the feature does not need to be removed before deploy.
- Operational note: `sessionStorage` is tab/window scoped. To reuse both Gemini and OpenAI without re-entry, use the same tab: save Gemini with the QA checkbox on, click `API蛻・崛`, save OpenAI, then reload/regenerate as needed. A separate new window will not inherit this session copy.
- Documentation note: this is an internal local-QA convenience, not a public user feature. Do not describe the `?qaApiSession=1` mechanism in the public README. If another agent asks how to avoid repeated API-key entry during development QA, tell them to open the local app with `?qaApiSession=1`, enable the checkbox, and keep using the same tab.
- Verification passed after this note: `node --check src/main.js`, `npm run lint --if-present`, `git diff --check -- src/main.js package.json package-lock.json index.html src/data.js README.md HANDOFF.md scripts/inapp_browser_uia.ps1` (LF/CRLF warnings only), `npm run build`, HTTP v4.3.5 check, and in-app browser checks confirming normal `http://localhost:5179/` hides the QA helper while `http://localhost:5179/?qaApiSession=1` shows it. Browser warning/error logs were empty.
- Fresh Gemini/OpenAI API QA is still pending after this local helper change.
- Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.3.4 hard gates vs effort goals

- User correctly pointed out that `AI閾ｭ髯､蜴ｻ` should not be a hard save blocker and that some other craft rules should be effort goals too.
- Implemented in `src/main.js`:
  - version advanced to `v4.3.4`;
  - added `smEffortGoalContractV434()` for Gemini/OpenAI so prompts distinguish hard product failures from craft improvement targets;
  - added `smEffortGoalIssueV434()` and a final `ot()` wrapper so AI-smell, uniform paragraph rhythm, abstract repetition, neat afterglow, muddy-line, sensory-detail, chapter-temperature, and mild drift warnings are filtered out of the hard pre-save rejection path;
  - kept hard gates for selected/manual setting replacement, management/control labels, broken text, underlength, duplicate/restart, hard contradictions, and unselected template takeover.
- This directly addresses the stopped v4.3.3 Gemini long-novel run where chapter 1 was rejected only because `蟆剰ｪｬ蜷代￠AI閾ｭ髯､蜴ｻv4.3.3: 谿ｵ關ｽ繝ｪ繧ｺ繝縺悟插荳縺吶℃縺ｾ縺兪.
- Required verification after this note: `node --check src/main.js`, lint, diff-check, build, HTTP/browser v4.3.4 check, then fresh Gemini long-novel QA if the UI still has the user-entered API key or after the user re-enters it.
- Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - v4.3.3 late-template recovery and novel AI-smell removal

- User asked to continue the current work and also add the article-inspired "AI smell" removal method for fiction.
- Implemented in `src/main.js`:
  - version advanced to `v4.3.3`;
  - `smLateTemplateRecoveryContractV433()` adds a Gemini/OpenAI shared prompt contract for late chapters and endings;
  - `smLateTemplateRecoveryIssuesV433()` adds a late-stage save gate that catches unselected digital/research-facility/lab/capsule/data-core/USB/device/server/system payoff drift;
  - rejected late-stage drafts now carry a concrete rewrite direction in the save-gate reason: convert the payoff into witness testimony, paper ledger, photo, key, receipt, letter, wound, face-to-face dialogue, lie, betrayal, refusal, or human cost;
  - `smNovelAiSmellContractV433()` adds fiction-specific AI-smell removal without flattening all genres into casual chat;
  - `smNovelAiSmellIssuesV433()` detects explanation-summary endings, excessive abstract-word repetition, overly neat dawn/hope/truth afterglow, and too-uniform paragraph rhythm, then routes the chapter into the existing retry path.
- Version metadata is now `v4.3.3` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`.
- Verification passed after this note: `node --check src/main.js`, `npm run lint --if-present`, `git diff --check -- src/main.js package.json package-lock.json index.html src/data.js README.md HANDOFF.md scripts/inapp_browser_uia.ps1` (LF/CRLF warnings only), `npm run build`, HTTP v4.3.3 check on port 5179, and Codex in-app browser reload showing `Story Maker v4.3.3` with no browser error/warning logs.
- Fresh Gemini/OpenAI API QA has not been run after v4.3.3. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-06 JST - in-app browser QA control confirmed for v4.3.2

- Confirmed Codex can operate the Story Maker page inside the Codex in-app browser directly, without external Chrome and without desktop screen coordinates.
- Working target:
  - URL: `http://localhost:5179/`
  - title: `Story Maker v4.3.2`
  - app header: `Story Maker v4.3.2`
- Correct control path:
  1. Connect to the Browser plugin `iab` browser.
  2. Use the selected tab's Playwright API.
  3. Fill fields with selectors such as `#mode-custom`, `#theme-custom`, `#genre-custom`, `#worldview-custom`, `#target-custom`, `#era-custom`, `#ending-custom`, and `#narr-custom`.
  4. Click the actual long-novel mode chip and verify it is active.
  5. Click `#btn-generate`.
  6. Monitor progress and scan the final manuscript through DOM reads from `#output`.
- Important: Windows UI Automation can read state and invoke some buttons, but it must not be used to set React form fields for QA. `ValuePattern.SetValue()` can alter the visible value without updating Story Maker's internal React state.
- Fresh in-app Gemini long-novel QA result:
  - completed 10 / 10 chapters;
  - output length from `#output`: 69,690 chars;
  - `Generated By AI Story Maker V4.3.2`: exactly 1;
  - `縲仙ｮ後疏: exactly 1;
  - chapter headings: 10;
  - chapter-control artifacts: 0;
  - management/memo artifacts: 0;
  - final app scores: plot recovery 96, structure 97, constraint compliance 100.
- Quality caveat: although technical completion and contamination scans passed, the final manuscript still contains visible digital/research/capsule-style texture. If the product requirement is to avoid that texture unless selected, continue with a v4.3.3 quality hardening pass before deploy/tag/release/backup.
- Deployment status: not deployed, tagged, released, or backed up after this v4.3.2 in-app QA.

## Codex Update - 2026-06-06 JST - in-app browser control path for v4.3.2 QA

- The user asked to make verification controllable inside the Codex in-app browser, not external Chrome and not desktop screen-coordinate clicks.
- Confirmed the official Browser plugin works for the in-app browser:
  - selected tab: `http://localhost:5179/`;
  - title: `Story Maker v4.3.2`;
  - direct Playwright selectors such as `#mode-custom`, `#theme-custom`, and `#btn-generate` are available.
- Added `scripts/inapp_browser_uia.ps1` as a diagnostic/helper for the Codex in-app browser WebView:
  - `summary` finds the Codex `RootWebArea`, API field, generate button, and key Story Maker input controls;
  - `status` reads compact visible progress lines;
  - `values` reads non-secret custom input values;
  - `generate` can invoke the visible generate button;
  - it never prints or sets API keys.
- Important correction: Windows UI Automation `ValuePattern.SetValue()` can change the visible/input value but does not reliably dispatch React input events. Do not use UIA `SetValue` to configure QA settings.
- Correct operation path for real in-app browser generation:
  1. Use Browser plugin / `tab.playwright.locator('#field-id').fill(value)` for custom fields.
  2. Click the real output-mode chip (`getByRole('button', { name: '髟ｷ邱ｨ蟆剰ｪｬ(謨ｰ荳・ｭ・蜈ｨ遶・区欠遉ｺ譖ｸ)' })`) so the long-novel engine is actually selected.
  3. Verify the button has `className` containing `chip active`.
  4. Click `#btn-generate`.
  5. Verify logs show `髟ｷ邱ｨ蟆剰ｪｬ繝｢繝ｼ繝荏, `騾ｲ謐・ 0 / 10 遶`, and `逶ｮ讓・ 邏・0,000蟄輿.
- Live QA status when this note was written:
  - API state detected only as `present-redacted`;
  - long-novel engine started successfully in the in-app browser;
  - progress reached at least `1 / 10` chapters, with chapter 1 saved and chapter 2 under inspection/generation;
  - not deployed, tagged, released, or backed up after this change.

## Codex Update - 2026-06-06 JST - v4.3.2 final force-close elasticity

- User clarified: if the ending is interesting, a somewhat forceful close is acceptable. The goal is reader impact and human-like roughness, not endless mechanical rejection.
- Fresh Gemini long-novel QA on `v4.3.1` reached chapter 10 but failed: the final chapter repeatedly tried to make unselected digital props (`file/device/server/data/code` style payoff) carry the resolution. The v4.3.1 gate correctly rejected those drafts, but after 5 retries the run stopped instead of reaching a satisfying ending.
- Implemented in `src/main.js`:
  - version advanced to `v4.3.2`;
  - `smFinalForceCloseContractV432()` adds Gemini/OpenAI shared prompt guidance: final chapter may close roughly if vivid, but management labels, chapter controls, duplication, hard contradictions, broken markers, and broken text remain forbidden;
  - `smMaybeForceCloseFinalV432()` adds a final-chapter-only escape hatch after the save gate: only creative final warnings such as unselected digital payoff / over-clean afterglow are eligible, and only after enough retries plus enough dialogue, choices, cost terms, concrete objects, and closure signals.
- Version metadata is now `v4.3.2` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`.
- Still required after this note: run `node --check src/main.js`, lint, diff-check, build, HTTP/browser version check, then fresh Gemini long-novel QA with the user-entered API key. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-05 JST - v4.3.1 final-core/afterglow guard

- Fresh Gemini long-novel QA on `v4.3.0` technically completed 10 / 10 chapters with footer, but it is a QA failure, not a pass.
- Failure evidence:
  - final chapter accepted an unselected digital solution core: `black file`, server room, terminal/device, download/data extraction, and proof-publication became the final payoff even though the selected axes were suspense/mystery/modern Tokyo, not digital/SF;
  - the ending returned to AI-like clean afterglow: dawn/morning light, truth-light, future/hope, complete memory recovery, and no-regret resolve;
  - Gen-4 editorial evaluation also skipped because the evaluation JSON parser was too strict for the returned JSON.
- Implemented in `src/main.js`:
  - `smFinalQualityContractV431()` adds a Gemini/OpenAI prompt contract for standard and long-form generation that forbids unselected black-file/server/device/download payoff and requires a complicated final beat instead of dawn/hope/truth-light closure.
  - `smFinalCoreAndAfterglowIssuesV431()` adds a hard final-chapter pre-save rejection for unselected digital final-core takeover and over-clean final afterglow.
  - `smParseGen4EvalJsonV431()` makes Gen-4 editorial JSON parsing reuse the existing robust parser.
- Version metadata is now `v4.3.1` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`.
- Verification passed: local regression against the leaked final-tail sample rejects the failure, `node --check src/main.js`, `npm run lint --if-present`, `git diff --check`, `npm run build`, and HTTP 200 on port 5179 showing `Story Maker v4.3.1`.
- Browser status: existing Chrome is `Story Maker v4.3.1 - Google Chrome`, but the reload cleared the in-memory API key field. Fresh Gemini long-novel QA is pending user re-entry of the Gemini API key in the UI. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-05 JST - v4.3.0 bio-device/cult hard guard

- Correct versioning: the local patch after `v4.2.9` is `v4.3.0`. Removed the mistaken micro-version residue from app metadata and internal helper names.
- Fresh Gemini long-novel QA on the previous local patch was stopped before completion to avoid more API spend. The run reached chapter 3 and was already drifting into unselected `Ikenie/sacrifice`, giant research devices, capsules, biomechanical experiment imagery, and facility-activation language despite selected axes being suspense/mystery/modern Tokyo rather than SF, occult, or folklore.
- Implemented in `src/main.js`:
  - `smBioDeviceCultContractV430()` appends a Gemini/OpenAI shared prompt contract that forbids unselected research-facility/giant-device/capsule/biomechanical-experiment and Ikenie/sacrifice/ritual-activation templates from becoming the core mystery, threat, reveal, solution, or ending.
  - `smBioDeviceCultIssuesV430()` adds hard pre-save rejection for those motifs when they are not selected by the user.
  - `smSevereCreativeBreakIssuesV428()` was also strengthened so unselected AI/device/data systems cannot become the final reveal, black box, final weapon, or solution.
- Version metadata is now `v4.3.0` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`.
- Verification passed after the version correction: `node --check src/main.js`, `npm run lint --if-present`, `git diff --check`, and `npm run build`.
- Still pending: reload port 5179 and run a fresh Gemini long-novel QA on `v4.3.0`; if it passes, continue with OpenAI long-novel QA. Not deployed, tagged, released, or backed up.

## Codex Update - 2026-06-05 JST - v4.2.8 creative-elasticity gate

- User clarified the product direction: Story Maker should prioritize moving the reader and removing AI-like neatness. Some drift can be valuable as human-like "yuragi" if it deepens the scene and returns to the chosen axes.
- Implemented in `src/main.js`:
  - `smCreativeElasticityContractV428()` appends a Gemini/OpenAI shared prompt contract for standard and long-form generation.
  - The new contract treats selected/manual axes as gravity rather than a cage, permits emotionally strong detours, and requires them to be metabolized back into theme/genre/world/character stakes before output.
  - `smSoftCreativeIssueV428()` removes soft style/drift warnings from the hard pre-save rejection path: mild template drift, abstract repetition, report-like explanation, overly polished endings, and style-density warnings.
  - `smSevereCreativeBreakIssuesV428()` keeps true failures hard-rejected: explicit unselected isekai/fantasy systems, severe folklore/body-invasion takeover, device-as-solution drift, chuni/awakening takeover, mundane-to-world-crisis replacement, and final-chapter sequel bait.
  - `Lr()` now normalizes Gen-4 evaluation JSON scored as `0..1` into the app's `0..100` display scale.
- Version metadata advanced to `v4.2.8` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`.
- Fresh Gemini short-story API smoke passed in the user's existing Chrome window via UI Automation:
  - app state: `Gemini API`, `Story Maker v4.2.8`;
  - mode: short story (`遏ｭ邱ｨ蟆剰ｪｬ(縲・000蟄・`);
  - generated title: `骭・・縺滄・霍ｯ`;
  - output length: 2,164 chars;
  - exactly one `縲仙ｮ後疏;
  - exactly one `Generated By AI Story Maker V4.2.8`;
  - no chapter-control/management labels and no hard unselected isekai/folklore/world-scale drift hits in the extracted visible output;
  - progress finished as `AI騾ｲ謐励・諤晁・Ο繧ｰ: 螳御ｺ・(蜷域ｼ)`.
- Verification still required after this note:
  - `node --check src/main.js`
  - `npm run lint --if-present`
  - `git diff --check -- src/main.js package.json package-lock.json index.html src/data.js README.md HANDOFF.md`
  - `npm run build`
  - reload port 5179 and verify v4.2.8 is visible
  - optional fresh OpenAI API QA after the user enters the OpenAI key in the UI
- Not deployed, tagged, released, or backed up after v4.2.8. Bilingual tag/release text remains mandatory if deployment is later requested.

## Codex Update - 2026-06-05 JST - v4.2.7 local-folklore drift guard

- Fresh Gemini long-novel QA on v4.2.6 was intentionally stopped before completion because the selected mundane axes (`繧ｳ繝ｳ繝薙ル`, `繧ｳ繝｡繝・ぅ`, `蝨ｰ譁ｹ驛ｽ蟶Ａ, `蜈ｨ蟷ｴ鮨｢`, `2020蟷ｴ莉｣`) drifted into unselected local-folklore/body-horror material.
- Concrete failure evidence from the stopped run:
  - saved progress reached 9 / 10 chapters before manual abort;
  - the visible manuscript tail included `雍Я, `逾`, `邱代・邊荏, `闍斐・縺励◆遏ｳ`, `蝨ｰ縺ｮ蠎描, `逕溷多蜉嫣, `萓ｵ鬟歔, `驥悟ｱｱ蜈ｨ菴伝, and `雍・・蠢・∮`;
  - a leaked internal label appeared in prose: `縲舌％縺薙°繧峨す繝ｼ繝ｳ繝薙・繝医→繧｢繝輔ち繝ｼ繝・う繧ｹ繝医ン繝ｼ繝医疏;
  - this is a QA failure even if the technical long-novel engine could have finished chapter 10.
- Implemented in `src/main.js`:
  - `smTemplateContractV427()` appends a Gemini/OpenAI shared prompt contract through `Wt(settings, scope)`.
  - `smLocalFolkloreDriftIssuesV427()` rejects unselected folklore/occult/body-invasion drift in the long-novel save gate.
  - `smDesignBeatArtifactIssuesV427()` rejects internal scene/aftertaste/beat/checklist/method labels in saved chapter prose.
  - `ot()` now includes v4.2.7 drift and artifact checks after the v4.2.6 chapter-restart guard.
- Version metadata advanced to `v4.2.7` in `src/main.js`, `package.json`, `package-lock.json`, `index.html`, and `src/data.js`.
- Verification still required after this note:
  - `node --check src/main.js`
  - `npm run lint --if-present`
  - `git diff --check -- src/main.js package.json package-lock.json index.html src/data.js README.md HANDOFF.md`
  - `npm run build`
  - reload port 5179 and verify v4.2.7 is visible
  - fresh Gemini long-novel QA; if clean, continue to OpenAI long-novel QA
- Not deployed, tagged, released, or backed up after v4.2.7. Bilingual tag/release text remains mandatory if deployment is later requested.

## Codex Update - 2026-06-05 JST - v4.2.4 template invasion prevention

- User approved adding a system-level mechanism to prevent Story Maker from becoming "similar every time" despite its multi-axis randomization.
- Implemented in `src/main.js`:
  - `smTemplateContractV424()` appended to the shared `Wt(settings, scope)` prompt contract, so both Gemini and OpenAI, standard generation and long-novel generation, receive the same rules.
  - `smTemplateInvasionIssuesV424()` wired into the long-novel save gate through `ot()`.
- Behavior:
  - Digital/gadget, chuni/grandiose, isekai/fantasy, and world-scale conspiracy motifs are allowed when selected/manual axes explicitly call for them.
  - If not selected, those motifs may appear only as incidental texture and must not become the core reveal, protagonist identity, plot engine, solution, or ending.
  - Shared prompt contract now explicitly implements the requested seven methods: protagonist wound/desire/lie/impossible choice/price lock, explanation-chapter ban, anti-digital/template gate, raw human beat, final-afterglow betrayal, vocabulary repetition audit, and chapter temperature cards.
  - Long-novel save gate now detects unselected template-family drift, excessive abstract-word repetition without concrete scene texture, explanation/report-style resolution, missing raw human beat, final-chapter report/aftermath-summary endings, and overly clean final afterglow.
- Version metadata advanced to `v4.2.4`.
- Local verification passed: `node --check src/main.js`, `npm run lint --if-present`, `git diff --check`, `npm run build`, HTTP v4.2.4 check on port 5179, and a local regression test for the prompt contract/save gate.
- Fresh Gemini short-story API smoke passed with user-entered key:
  - settings: short story, theme `繧ｳ繝ｳ繝薙ル`, genre `繧ｳ繝｡繝・ぅ`, worldview `蝨ｰ譁ｹ驛ｽ蟶Ａ, target `蜈ｨ蟷ｴ鮨｢`, era `2020蟷ｴ莉｣`, ending `縺ｻ繧崎協縺・ｬ代＞`, narration `荳我ｺｺ遘ｰ`;
  - output tags included `Gemini`, `Gemini 2.5 Flash`, and all selected/manual axes;
  - output length 2,590 chars, title `繧ｳ繝ｳ繝薙ル縺ｮ迚・嚆縺ｧ縲√く繝弱さ縺ｯ隨代≧`;
  - exactly one `縲仙ｮ後疏, exactly one `Generated By AI Story Maker V4.2.4`;
  - body scan found zero digital/chuni/isekai/world-scale/template drift terms and zero management/chapter-control artifacts.
- Fresh OpenAI short-story API smoke passed with user-entered key:
  - same settings as Gemini;
  - output tags included `ChatGPT`, `gpt-4.1`, and all selected/manual axes;
  - output length 2,424 chars, title `繧ｯ繝ｪ繝ｼ繝繝代Φ縺ｮ蝣ｴ謇`;
  - exactly one `縲仙ｮ後疏, exactly one `Generated By AI Story Maker V4.2.4`;
  - body scan found zero digital/chuni/isekai/world-scale/template drift terms and zero management/chapter-control artifacts.
- Not deployed, tagged, released, or backed up after v4.2.4. Never paste or store API keys in files or chat.

## Codex Update - 2026-06-05 JST - v4.2.3 release separation correction

- User correctly pointed out that the prior redeploy mixed a fresh QA/deploy checkpoint into the existing `v4.2.2` release instead of separating it into a new version.
- Corrective action: advance metadata to `v4.2.3` without changing generation behavior, so the public app version, generated footer, commit, annotated tag, GitHub Release, and GitHub Pages deploy can represent the completed Gemini long-novel QA checkpoint separately.
- Gemini long-novel QA evidence for the checkpoint:
  - completed 10 / 10 chapters;
  - visible manuscript counter: 75,502 characters;
  - extracted scan found exactly one completion marker, exactly one version footer, 10 chapter headings, and zero chapter-control or management-memo artifacts;
  - visible footer reached after `縲仙ｮ後疏: `Generated By AI Story Maker V4.2.2` before the version bump.
- Release-copy rule reminder: annotated tag and GitHub Release notes must remain bilingual English/Japanese.

## Codex Update - 2026-06-05 JST - v4.2.2 long-form QA and deploy checkpoint

- After the OpenAI smoke, a fresh OpenAI long-novel run was started in the in-app browser on port 5179.
- The run reached completion:
  - progress: 10 / 10 chapters
  - visible manuscript counter: 72,180 characters
  - completed state reached
  - visible tail included the expected completion marker area: `縲仙ｮ後疏 followed by `Generated By AI Story Maker V4.2.2`
- Important limitation: current Codex tool access could not extract the already completed in-app-browser manuscript via clipboard or UIA without asking the user to spend API tokens on another full run. The available evidence is the completed in-app run plus the unchanged v4.2.0+ save-path contamination gates.
- Pre-deploy checks after v4.2.2 passed:
  - `node --check src/main.js`
  - `npm run lint --if-present`
  - `git diff --check -- src/main.js package.json package-lock.json index.html README.md HANDOFF.md`
  - `npm run build`
- `git diff --check` only reported expected CRLF/LF working-copy warnings.
- Keep generated verification artifacts untracked and out of releases:
  - `output_openai_v419_uia.txt`
  - `scratch_sm_api_qa_v419.jsonl`

## Codex Update 窶・2026-06-05 JST

### Status: v4.2.2 local provider-display fix after OpenAI API smoke

- User entered the OpenAI key and requested verification after the v4.2.1 quality-method stack.
- Real OpenAI smoke succeeded in the in-app browser:
  - active output tags: `ChatGPT` / `gpt-4.1`
  - generated story title: `鬲壼ｱ九ヨ繝ｼ繝医・螂・ｷ｡`
  - story body: 1,192 characters
  - exactly one `縲仙ｮ後疏
  - exactly one `Generated By AI Story Maker V4.2.1` before the version bump
  - no chapter-control or management-memo artifacts in the extracted story body
  - Gen-4 evaluation present with the three new v4.2.1 axes: narrative methods, category guides, and de-biasing
- During verification, Codex found a display-only bug: standard generation used the OpenAI route and showed the correct output tag `gpt-4.1`, but the progress log's initial connection line still displayed `gemini-2.5-flash` because it used `me[0].value` before provider routing.
- v4.2.2 fix:
  - added `smInitialTextModel()` and `smProviderLabel()`
  - standard-generation progress log now starts with `gpt-4.1` for OpenAI keys and `gemini-2.5-flash` for Gemini keys
  - long-novel chapter 1 initial `usedModel` now uses the active provider
  - long-novel continuation fallback initial model now uses the active provider when no saved model exists
  - post-generation evaluation passes the provider-derived initial model to the shared API call
- Version sync advanced to `4.2.2` in `src/main.js`, `package.json`, `package-lock.json`, and `index.html`.
- Completed after this note: rebuild/lint/node-check passed, local in-app browser showed v4.2.2, and a fresh short OpenAI smoke passed on v4.2.2. Verification evidence: output tags `ChatGPT` / `gpt-4.1`, progress log `AI繝｢繝・Ν (gpt-4.1)`, no Gemini model claim in the OpenAI run log, story title `縺雁ｮ医ｊ陲九・縺ｩ縺伝, 1,132 story characters, exactly one `縲仙ｮ後疏, exactly one `Generated By AI Story Maker V4.2.2`, no chapter-control or management-memo artifacts, and Gen-4 evaluation included the narrative-method/category-guide/de-biasing axes.

### Status: v4.2.1 local quality-method stack implementation in progress

- User asked whether the accumulated story-making methods documented in README are actually applied to long-form generation, and requested quality-up for both Gemini and OpenAI.
- Audit result:
  - Long-form prompts already include `Mn` with the 15+ long-novel narrative rules.
  - The first-chapter path `ha()` and continuation path `an()` both append `Wt(settings, "long")`.
  - Standard generation appends `Wt(settings, "standard")`.
  - API routing is provider-neutral at this prompt layer, so changes to `Wt()` affect both Gemini and OpenAI.
- v4.2.1 adds `smNarrativeMethodStackV421()` in `src/main.js`.
  - It explicitly injects README method coverage: Setup-Payoff, Emotion Gap Design, Motif Recurrence, 15-beat Emotion Curve, GMC+S, Show Don't Tell, Subtext, sensory balance, world-grounded metaphor, Character Knowledge Boundary, category-guide obedience, and multi-axis de-biasing.
  - It repeats the anti-digital/AI/gadget default rule unless the fixed axes explicitly call for those motifs.
- v4.2.1 expands the Gen-4 editorial evaluation rubric with:
  - `narrative_methods`
  - `category_guides`
  - `de_biasing`
- Version sync advanced to `4.2.1` in `src/main.js`, `package.json`, `package-lock.json`, and `index.html`.
- Local verification passed: `node --check src/main.js`, `npm run build`, `npm run lint --if-present`, `git diff --check`, HTTP v4.2.1 check on port 5179, dist asset check, and static prompt-path check proving standard, long-chapter-1, and long-continuation prompts all append the shared `Wt()` contract.
- Pending verification: optional user-entered Gemini/OpenAI API smoke.
- Not deployed, tagged, released, or backed up after v4.2.1 unless the user explicitly asks again.

### Status: v4.2.0 local fix completed and fresh OpenAI full-run QA passed

- v4.1.9 real OpenAI long-novel QA completed 10 / 10 chapters and produced an 88,226-character manuscript with exactly one `縲仙ｮ後疏 and one `Generated By AI Story Maker V4.1.9` footer, but it **failed final QA** because chapter 1 still contained two management memo blocks:
  - `縲千ｬｬ1遶譛ｫ繝ｻ讒矩繝｡繝｢縲疏
  - `縲千ｬｬ1遶繝ｻ蠕悟濠陬懷ｮ後・謗･邯壹Γ繝｢縲疏
- Root cause: the v4.1.9 classifier focused on chapter-control artifact lines and did not treat management/connection/supplement memo blocks as the same forbidden body-contamination class.
- v4.2.0 adds `smStripLongNovelManagementBlocksV420()` and related classifiers in `src/main.js`:
  - strips bracketed management headings plus following design bullets;
  - catches standalone design/management bullets if the heading is missing;
  - applies the same guard through `le()`, `Xe()`, `it()`, `He()`, `oa()`, and `ot()`;
  - rejects remaining management headings or design bullets in the save gate.
- Local regression used the extracted v4.1.9 real API manuscript at `output_openai_v419_uia.txt`:
  - before: 88,226 chars;
  - after local cleaning: 87,649 chars;
  - forbidden management artifacts after cleaning: 0;
  - both failed headings and their bullets removed;
  - resumed prose after each block preserved;
  - `縲仙ｮ後疏 count remains 1;
  - old footer count remains 1.
- Fresh v4.2.0 OpenAI QA was run from the in-app browser after user-entered API key:
  - API state: `ChatGPT API`;
  - mode: `髟ｷ邱ｨ蟆剰ｪｬ(謨ｰ荳・ｭ・蜈ｨ遶・区欠遉ｺ譖ｸ)`;
  - completed: 10 / 10 chapters;
  - copied manuscript chars: 73,830;
  - line count: 1,611;
  - `縲仙ｮ後疏: 1;
  - `Generated By AI Story Maker V4.2.0`: 1;
  - chapter-control artifact lines: 0;
  - management memo headings: 0;
  - standalone management/design bullets: 0;
  - specific bad-generic scan for `END CHAPTER`, `To be continued`, `譁・ц邯ｭ謖～, `谺｡遶騾｣邨秦, `蠕悟濠陬懷ｮ形, `讒矩繝｡繝｢`, `謗･邯壹Γ繝｢`, and `隨ｬn遶 邯壹￥`: 0.
- During the run, save gates rejected bad attempts before adoption:
  - Chapter 1 first attempt stopped for `繧ｿ繧､繝医Ν|繝ｭ繧ｰ繝ｩ繧､繝ｳ|蜈ｨ讒区・|莠亥ｮ夂ｷ乗枚蟄玲焚` management-info residue and was regenerated.
  - Chapters 6, 7, and 9 had early-resolution/teaser endings rejected before save and were regenerated.
  - Saved chapters and final copied manuscript passed the contamination scan.
- Verification already passed:
  - `node --check src/main.js`
  - v4.2.0 VM regression for both failed memo blocks, standalone management bullets, allowed headers, non-management bullets, and the extracted real API manuscript
  - `npm run build`
  - `npm run lint --if-present`
  - `git diff --check -- src/main.js package.json package-lock.json index.html src/data.js README.md`
- Do **not** deploy, tag, release, or run PS1 backup unless the user explicitly asks. If another fresh OpenAI full-run QA is needed, ask the user to type the key into the UI; never paste or store the key.

## Codex Handoff 窶・2026-06-05 07:05 JST

### Status: v4.1.9 螳溯｣・ｮ御ｺ・・螳蘗PI讀懆ｨｼ蠕・■

---

### 1. 莉翫☆縺占ｪｭ繧・ｼ域怙驥崎ｦ√さ繝ｳ繝・く繧ｹ繝茨ｼ・
- **蟇ｾ雎｡繧｢繝励Μ**: `[local Antigravity story-maker path]`・域里蟄倥い繝励Μ縺ｮ菫ｮ豁｣・・- **迴ｾ繝舌・繧ｸ繝ｧ繝ｳ**: `v4.1.9`・医Ο繝ｼ繧ｫ繝ｫ縺ｮ縺ｿ縲ゅ∪縺繝・・繝ｭ繧､繝ｻRelease譛ｪ菴懈・・・- **蜑阪ヰ繝ｼ繧ｸ繝ｧ繝ｳ**: `v4.1.8`・・itHub Pages 縺ｫ繝・・繝ｭ繧､貂医∩縺縺後∝ｮ蘗PI讀懆ｨｼ縺ｧ荳榊粋譬ｼ・・- **菫ｮ豁｣縺励◆閾ｴ蜻ｽ繝舌げ**: 髟ｷ邱ｨ蟆剰ｪｬ繝｢繝ｼ繝峨〒遶蛻ｶ蠕｡繝ｩ繝吶Ν・・・育ｬｬ9遶 邯壹￥・荏遲会ｼ峨′蟆剰ｪｬ譛ｬ譁・↓豺ｷ蜈･縺吶ｋ
- **菫ｮ豁｣譁ｹ驥・*: 譁ｭ迚・噪縺ｪ1-regex-per-patch繧｢繝励Ο繝ｼ繝√ｒ蟒・ｭ｢縺励∫ｵｱ荳逧・↑`smLooksLikeChapterControlArtifactLineV419`蛻・｡槫勣繧貞ｮ溯｣・- **繝・せ繝育ｵ先棡**: 38/38 蝗槫ｸｰ繝・せ繝磯夐℃・・6髯､蜴ｻ+12菫晁ｭｷ繧ｱ繝ｼ繧ｹ・・- **繝ｭ繝ｼ繧ｫ繝ｫ襍ｷ蜍慕｢ｺ隱・*: `http://localhost:5179/` 縺ｧv4.1.9縺後お繝ｩ繝ｼ縺ｪ縺丞虚菴・- **邨ｶ蟇ｾ遖∵ｭ｢**: 繝・・繝ｭ繧､繝ｻ繧ｿ繧ｰ繝ｻRelease繝ｻ繝舌ャ繧ｯ繧｢繝・・縺ｯ縲√Θ繝ｼ繧ｶ繝ｼ縺梧・遉ｺ逧・↓謖・､ｺ縺吶ｋ縺ｾ縺ｧ螳溯｡後☆繧九↑

---

### 2. v4.1.9 縺ｧ菴輔ｒ螳溯｣・＠縺溘°

#### 霑ｽ蜉縺励◆髢｢謨ｰ鄒､・・src/main.js` 譛ｫ蟆ｾ縲〕ine ~1808縲・944・・
| 髢｢謨ｰ蜷・| 蠖ｹ蜑ｲ |
|---|---|
| `smNormalizeControlLineV419(e)` | 諡ｬ蠑ｧ縺ｮ髯､蜴ｻ縲∵忰蟆ｾ蜿･隱ｭ轤ｹ縺ｮ繧ｯ繝ｪ繝ｼ繝九Φ繧ｰ |
| `smChapterControlArtifactReV419` | 5繧ｫ繝・ざ繝ｪ蟇ｾ蠢懊・邨ｱ荳豁｣隕剰｡ｨ迴ｾ |
| `smLooksLikeChapterReferenceProseV419(e)` | 謨｣譁・｡後・隱､讀懷・繧帝亟縺蝉ｿ晁ｭｷ蛻､螳・|
| `smIsChapterHeadingV419(e)` | 遶隕句・縺励ｒ菫晁ｭｷ |
| `smLooksLikeChapterControlArtifactLineV419(e)` | **邨ｱ荳蛻・｡槫勣譛ｬ菴・* |
| `smStripChapterControlArtifactLinesV419(text)` | 陦悟腰菴阪・荳諡ｬ髯､蜴ｻ繝倥Ν繝代・ |

#### 邨ｱ蜷亥・・亥・4繝代う繝励Λ繧､繝ｳ髢｢謨ｰ縺ｫ繝輔ャ繧ｯ・・
```
oa() 竊・繝・・繝ｫ陦後′繧｢繝ｼ繝・ぅ繝輔ぃ繧ｯ繝医°蛻､螳夲ｼ・419蛻・｡槫勣繧定ｿｽ蜉・・le() 竊・蜿ｯ隕悶ユ繧ｭ繧ｹ繝医け繝ｪ繝ｼ繝翫・・育ｫ髢灘性繧蜈ｨ陦後せ繧ｭ繝｣繝ｳ・矩勁蜴ｻ・俄・ 笘・ｸ蠢・He() 竊・繝・・繝ｫ菴咲ｽｮ縺ｮ繧ｳ繝ｳ繝医Ο繝ｼ繝ｫ繝ｩ繧､繝ｳ髯､蜴ｻ・域怙螟ｧ8蝗槭Ν繝ｼ繝暦ｼ・ot() 竊・菫晏ｭ伜燕蜩∬ｳｪ繧ｲ繝ｼ繝茨ｼ域ｮ句ｭ倥メ繧ｧ繝・け・九Μ繧ｸ繧ｧ繧ｯ繝育炊逕ｱ霑ｽ蜉・・```

#### 讀懷・繧ｫ繝舌・遽・峇・・繧ｫ繝・ざ繝ｪ・・
```
(A) 譌･譛ｬ隱・遶逡ｪ蜿ｷ+邯咏ｶ・ 隨ｬ9遶 邯壹￥ / 縺､縺･縺・/ 邯咏ｶ・/ 谺｡遶縺ｸ
(B) 譌･譛ｬ隱・遶逡ｪ蜿ｷ+螳御ｺ・ 隨ｬ9遶 莠・/ 邨・/ 螳・/ 邨ゆｺ・/ 邨ゅｏ繧・(C) 髱樒分蜿ｷ 邯咏ｶ夊ｪ・      邯壹￥ / 縺､縺･縺・/ 谺｡遶縺ｸ / 邨らｫ縺ｸ / To be continued
(D) 闍ｱ隱・遶蛻ｶ蠕｡:        END CHAPTER N / CHAPTER N END / NEXT CHAPTER
(E) 邂｡逅・Γ繝｢繝ｩ繝吶Ν:     譁・ц邯ｭ謖√Γ繝｢ / 谺｡遶騾｣邨舌Γ繝｢ / 邯壹￥遶...
```

#### 菫晁ｭｷ蟇ｾ雎｡・・alse positive 髦ｲ豁｢・・
```
- 謨｣譁・ｸｭ縺ｮ遶蜿ら・: 縲檎ｬｬ9遶縺ｧ縺ｯ縲∝ｽｼ縺ｯ縺ｾ縺邨ゅｏ繧翫ｒ隱阪ａ縺ｪ縺九▲縺溘ゅ坂・ 菫晁ｭｷ
- 蠑慕畑荳ｭ縺ｮ邯壹￥:   縲檎ｶ壹￥縲阪→縺縺題ｨ縺｣縺ｦ 竊・菫晁ｭｷ
- 謨｣譁・｡悟・縺ｮ邯壹￥: 邯壹￥髮ｨ髻ｳ縺ｮ荳ｭ縺ｧ 竊・菫晁ｭｷ
- 遶隕句・縺・       # 隨ｬ9遶: 繧ｿ繧､繝医Ν 竊・菫晁ｭｷ
- 120譁・ｭ苓ｶ・・髟ｷ譁・｡・竊・菫晁ｭｷ
```

---

### 3. 蜑榊屓縺ｾ縺ｧ縺ｮ螟ｱ謨怜ｱ･豁ｴ・亥酔縺倩ｽ阪ｒ雕上・縺ｪ・・
| 繝舌・繧ｸ繝ｧ繝ｳ | 菫ｮ豁｣蜀・ｮｹ | 螳蘗PI邨先棡 |
|---|---|---|
| v4.1.2 | oa()縺ｮ蝓ｺ譛ｬ諡｡蠑ｵ | 譁ｰ縺溘↑繧｢繝ｼ繝・ぅ繝輔ぃ繧ｯ繝育匱隕・|
| v4.1.3 | 邂・擅譖ｸ縺肴､懷・ + He/ot 諡｡蠑ｵ | 譁ｰ縺溘↑繧｢繝ｼ繝・ぅ繝輔ぃ繧ｯ繝育匱隕・|
| v4.1.4 | 險ｭ險域妙迚・､懷・ + oa/He/ot 諡｡蠑ｵ | 譁ｰ縺溘↑繧｢繝ｼ繝・ぅ繝輔ぃ繧ｯ繝育匱隕・|
| v4.1.7 | 遶螳御ｺ・Λ繝吶Ν + 4髢｢謨ｰ諡｡蠑ｵ | END CHAPTER 9 逋ｺ隕・|
| v4.1.8 | 闍ｱ隱樒ｫ蛻ｶ蠕｡繝ｩ繝吶Ν + 4髢｢謨ｰ諡｡蠑ｵ | ・育ｬｬ9遶 邯壹￥・・逋ｺ隕・|
| **v4.1.9** | **邨ｱ荳蛻・｡槫勣縺ｫ蜈ｨ邨ｱ蜷・* | **譛ｪ讀懆ｨｼ・亥ｮ蘗PI讀懆ｨｼ蠕・■・・* |

**螟ｱ謨励・譬ｹ譛ｬ蜴溷屏**: 繝・・繝ｫ(譛ｫ蟆ｾ)縺縺題ｦ九※縺・◆縺溘ａ縲∫ｫ蠅・阜縺ｫ謖溘∪縺｣縺溘Λ繝吶Ν繧定ｦ矩・＠縺溘Ｗ4.1.9縺ｧ縺ｯ le() 繝輔ャ繧ｯ縺ｧ蜈ｨ陦後せ繧ｭ繝｣繝ｳ縺ｫ螟画峩縲・
---

### 4. Codex 縺梧ｬ｡縺ｫ繧・ｋ縺ｹ縺阪％縺ｨ

#### 蠢・・ 螳蘗PI讀懆ｨｼ
1. 繝ｭ繝ｼ繧ｫ繝ｫ縺ｧ `npm run dev` 竊・繝悶Λ繧ｦ繧ｶ縺ｧ髢九￥
2. API繧ｭ繝ｼ繧旦I荳企Κ縺ｮ谺・↓蜈･蜉幢ｼ・emini or OpenAI・・3. 髟ｷ邱ｨ蟆剰ｪｬ繝｢繝ｼ繝峨〒蜈ｨ遶螳瑚ｵｰ縺輔○繧・4. 譛邨ょ次遞ｿ縺ｫ遶蛻ｶ蠕｡繝ｩ繝吶Ν縺御ｸ蛻・ｮ九▲縺ｦ縺・↑縺・％縺ｨ繧堤｢ｺ隱・
#### 讀懆ｨｼ繝代せ蠕・
- 繝ｦ繝ｼ繧ｶ繝ｼ縺ｫ蝣ｱ蜻翫＠縲√ョ繝励Ο繧､謖・､ｺ繧貞ｾ・▽
- 繝・・繝ｭ繧､謖・､ｺ縺梧擂縺溘ｉ `/deploy` 繝ｯ繝ｼ繧ｯ繝輔Ο繝ｼ縺ｫ蠕薙≧

---

### 5. 繧ｳ繝ｼ繝峨・菴咲ｽｮ髢｢菫・
```
src/main.js (1944陦・
笏懌楳笏 line 1~1700:    繝｡繧､繝ｳ繧｢繝励Μ繝ｭ繧ｸ繝・け・医Α繝九ヵ繧｡繧､貂医∩・・笏懌楳笏 line 1700~1807: v4.1.2縲忻4.1.8 繝代ャ繝∫ｾ､
笏披楳笏 line 1808~1944: 笘・v4.1.9 邨ｱ荳蛻・｡槫勣繝代ャ繝・```

繝舌・繧ｸ繝ｧ繝ｳ螳壽焚: line 1377 `const Fn="4.1.9"`

#### 繝舌・繧ｸ繝ｧ繝ｳ蜷梧悄貂医∩繝輔ぃ繧､繝ｫ・亥・4邂・園・・- `package.json` 竊・`"version": "4.1.9"`
- `index.html` 竊・`<title>Story Maker v4.1.9</title>` + 繝倥ャ繝繝ｼ v4.1.9
- `src/data.js` 竊・繧ｳ繝｡繝ｳ繝・Story Maker v4.1.9
- `src/main.js` 竊・`const Fn="4.1.9"`

---

### 6. 豕ｨ諢丈ｺ矩・
- API繧ｭ繝ｼ繧偵メ繝｣繝・ヨ縺ｫ雋ｼ繧九↑縲６I荳企Κ縺ｮ蜈･蜉帶ｬ・↓繝ｦ繝ｼ繧ｶ繝ｼ縺悟・蜉帙☆繧区婿蠑上・- src/main.js 縺ｯ繝溘ル繝輔ぃ繧､貂医∩縲ゅヱ繝・メ縺ｯ譛ｫ蟆ｾ縺ｫ霑ｽ險倥☆繧区婿蠑上・- v4.1.2縲忻4.1.8縺ｮ繝代ャ繝√・谿九＠縺溘∪縺ｾ縲」4.1.9縺梧怙荳贋ｽ阪〒繝ｩ繝・・・医メ繧ｧ繝ｼ繝ｳ讒矩・峨・- AGENTS.md 縺ｮ繝舌・繧ｸ繝ｧ繝ｳ繝ｫ繝ｼ繝ｫ: v4.1.9 縺ｮ谺｡縺ｯ **v4.2.0**縲・- README.md 縺ｮ繝舌・繧ｸ繝ｧ繝ｳ譖ｴ譁ｰ縺ｯ繝・・繝ｭ繧､譎ゅ↓螳滓命縲・
---


### Current Failure That Must Be Fixed
- User-entered OpenAI QA was run in the in-app browser at:
  - `http://localhost:5179/?v=local-v4.1.8-english-chapter-label`
- API was recognized as `ChatGPT API`.
- Mode/settings used for the run:
  - Mode: `髟ｷ邱ｨ蟆剰ｪｬ(謨ｰ荳・ｭ・蜈ｨ遶・区欠遉ｺ譖ｸ)`
  - Theme: `豸医∴縺溯ｨ俶・`
  - Genre: `莠瑚ｻ｢荳芽ｻ｢`
  - Worldview: `譚ｱ莠ｬ`
  - Target: `莉穂ｺ句ｸｰ繧翫↓隱ｭ繧`
  - Era: `2020蟷ｴ莉｣`
  - Ending: `莨冗ｷ壼屓蜿弱・陦晄茶`
  - Narration: `鄒､蜒丞括・郁ｦ也せ蛻・崛・荏
- Run title: `谿矩涸縺ｮ縺ｪ縺・Κ螻義
- The run completed all 10 / 10 chapters, around 77,281 visible manuscript characters.
- It still failed final manuscript quality because chapter 9 saved a non-prose chapter-control line:

```text
・育ｬｬ9遶 邯壹￥・・```

- Exact context found in the saved readable manuscript:

```text
逕ｷ縺ｯ遯楢ｶ翫＠縺ｫ譚ｱ莠ｬ縺ｮ陦礼・繧堤惻繧√√く繝ｼ繝帙Ν繝繝ｼ縺ｨ蜷榊絢繧帝撕縺九↓驥阪・逶ｴ縺吶・髱吶°縺ｪ螟懊・陬ゅ￠逶ｮ窶補戊ｪｰ縺ｫ繧ょ錐莉倥￠繧峨ｌ縺ｪ縺・ｴ謇縺ｫ縲√◎繧後◇繧後・險ｼ諡縺ｨ縲瑚・蛻・・蜷阪阪′縲√ｂ縺・ｸ蠎ｦ縺縺大━縺励￥貍ゅ▲縺ｦ縺・◆縲・・育ｬｬ9遶 邯壹￥・・
---

髮ｨ縺ｯ荳譎ｩ荳ｭ髯阪ｊ邯壹″縲∵擲莠ｬ縺ｮ螟懊↓貉ｿ繧頑ｰ励ｒ蛻ｻ縺ｿ霎ｼ繧薙□縲・```

- The bug is not that OpenAI generated bad text. The bug is that the system accepted and saved a chapter-control label as prose.
- This is the same class as the previous failures:
  - `END CHAPTER 9`
  - `・懃ｬｬ9遶莠・・`
  - `・育ｬｬ1遶縲∽ｺ・ｼ荏
  - `縲千ｬｬ9遶邨ゆｺ・・`
  - `(continued)` / `・育ｶ壹￥・荏
  - `邨らｫ縺ｸ...`
  - label-less outline bullets at tails
  - memo fragments such as `縲先枚閼育ｶｭ謖√・谺｡遶騾｣邨舌Γ繝｢縲疏
- The important difference: `・育ｬｬ9遶 邯壹￥・荏 was **not at the final manuscript tail after chapter 10**. It was a chapter-boundary artifact between chapter 9 and chapter 10. Tail-only final checks are insufficient.

### What Happened During The v4.1.8 Run
- Chapter 1 first attempt was rejected before save for management/header residue:
  - `繧ｿ繧､繝医Ν|繝ｭ繧ｰ繝ｩ繧､繝ｳ|蜈ｨ讒区・|莠亥ｮ夂ｷ乗枚蟄玲焚`
  - It regenerated and saved chapter 1 at `5,149` chars.
- Chapter 4 first attempt was rejected before save:
  - `遶譛ｫ縺御ｺ亥相繝ｻ辣ｽ繧頑枚縺ｧ邨ゅｏ縺｣縺ｦ縺・∪縺呻ｼ育ｬｬ4遶・荏
  - It regenerated and saved chapter 4.
- Chapter 9 first attempt was rejected before save:
  - `Whole-story resolution appears before the final chapter`
- Chapter 9 second attempt was rejected before save:
  - `遶譛ｫ縺御ｺ亥相繝ｻ辣ｽ繧頑枚縺ｧ邨ゅｏ縺｣縺ｦ縺・∪縺呻ｼ育ｬｬ9遶・荏
- Chapter 9 third attempt passed the save gate even though it contained `・育ｬｬ9遶 邯壹￥・荏.
- The app then continued to chapter 10 and completed. Because the bad label was no longer at the current tail, later tail-only stripping did not catch it.

Saved chapter sizes observed before final completion:
- Ch1: `5,149`
- Ch2: `8,613`
- Ch3: `7,535`
- Ch4: `7,214`
- Ch5: `8,070`
- Ch6: `6,738`
- Ch7: `8,508`
- Ch8: `8,075`
- Ch9: `8,082`

### Current Code Shape And Why The Previous Approach Failed
- `src/main.js` is a large generated/minified file with many wrapper patches appended over time.
- Important functions in the long-novel save pipeline:
  - `le(text)`: visible/combined output cleaner.
  - `qe(text)`: tail cleanup used before save.
  - `He(text)`: chapter-tail stripper used before save.
  - `ot(text, opts)`: pre-save quality gate. It returns an array of rejection reasons.
  - `Ve(text)`: paragraph/body normalization before save.
  - `Xe(...)` / `it(...)`: body and memo extraction paths.
- Current v4.1.8 wrappers are near the end of `src/main.js`. Search for:
  - `smLooksLikeTrailingDesignFragmentV414`
  - `smChapterDoneTailArtifactV417`
  - `smEnglishChapterControlLineV418`
  - `smLooksLikeEnglishChapterControlV418`
- v4.1.7 handled chapter-done labels only:
  - `莠・, `邨Ａ, `螳形, `邨ゆｺ・, `邨ゅｏ繧柿, `縺翫ｏ繧柿
- v4.1.8 handled English labels only:
  - `END CHAPTER 9`
  - `END OF CHAPTER 9`
  - `CHAPTER 9 END`
  - `CHAPTER 9 COMPLETE`
- Existing continuation handling caught some generic forms:
  - `・育ｶ壹￥・荏
  - `(continued)`
  - `邨らｫ縺ｸ...`
- The specific failed line `・育ｬｬ9遶 邯壹￥・荏 fell between those categories:
  - It has a chapter number, so it was not the generic `・育ｶ壹￥・荏.
  - It uses `邯壹￥`, so it was not the chapter-done set.
  - It is Japanese, so it was not the English set.
- This is why adding another tiny regex is the wrong fix. The classifier is conceptually fragmented.

### Required Fix Direction
Implement one canonical "chapter-control artifact line" classifier and make all save/preview/final paths use it.

The classifier should treat standalone chapter-control lines as non-prose, including at minimum:
- Japanese chapter-numbered continuation:
  - `隨ｬ9遶 邯壹￥`
  - `隨ｬ9遶縲∫ｶ壹￥`
  - `隨ｬ荵晉ｫ 邯壹￥`
  - `・育ｬｬ9遶 邯壹￥・荏
  - `縲千ｬｬ9遶 邯壹￥縲疏
  - `・懃ｬｬ9遶 邯壹￥・杼
  - `隨ｬ9遶 縺､縺･縺汁
  - `隨ｬ9遶 邯咏ｶ啻
  - `隨ｬ9遶 谺｡遶縺ｸ`
- Japanese chapter-numbered completion:
  - `隨ｬ9遶 莠・
  - `隨ｬ9遶 螳形
  - `隨ｬ9遶 邨Ａ
  - `隨ｬ9遶 邨ゆｺ・
  - `隨ｬ9遶 縺翫ｏ繧柿
  - existing garbled/bracketed variants such as `・懃ｬｬ9遶莠・・`
- Standalone non-numbered continuation:
  - `邯壹￥`
  - `縺､縺･縺汁
  - `・育ｶ壹￥・荏
  - `縲千ｶ壹￥縲疏
  - `谺｡遶縺ｸ`
  - `邨らｫ縺ｸ`
  - `To be continued`
  - `Continued`
  - `Next chapter`
- English chapter-control:
  - `END CHAPTER 9`
  - `END OF CHAPTER 9`
  - `CHAPTER 9 END`
  - `CHAPTER 9 DONE`
  - `CHAPTER 9 COMPLETE`
  - `CHAPTER 9 CONTINUES`
  - `CONTINUED IN CHAPTER 10`
  - `NEXT CHAPTER`

The classifier must preserve normal prose references such as:
- `隨ｬ9遶縺ｧ縺ｯ縲∝ｽｼ縺ｯ縺ｾ縺邨ゅｏ繧翫ｒ隱阪ａ縺ｪ縺九▲縺溘Ａ
- `隨ｬ9遶縺ｨ縺・≧險闡峨′縲√・縺溘ｊ縺ｮ髢薙〒蜀苓ｫ・∩縺溘＞縺ｫ髻ｿ縺・◆縲Ａ
- `蠖ｼ螂ｳ縺ｯ縲檎ｶ壹￥縲阪→縺縺題ｨ縺｣縺ｦ縲∫ｬ代▲縺溘Ａ
- Any long sentence where the chapter reference is part of prose rather than a standalone line.

### Required Pipeline Coverage
Do not rely on a tail-only cleanup.

Use the canonical classifier in all of these places:
- `He(text)`: strip chapter-control lines at the current chapter tail before save.
- `le(text)`: remove standalone chapter-control lines anywhere in visible combined manuscript text.
- `ot(text, opts)`: reject any chapter body that still contains standalone chapter-control artifact lines before it is appended to `longNovel.chapters`.
- Final-completed manuscript assembly: after all chapters are joined and before showing completed output / appending footer, run a whole-manuscript guard. If any chapter-control artifact remains between chapters, do not treat completion as accepted.
- Any `.txt` save/copy path should receive the already-clean manuscript, not a separate unclean source.

The final whole-manuscript guard is essential because the v4.1.8 failure was between chapters, not at the current tail after completion.

### Suggested Implementation Shape
Prefer a single helper group rather than separate v4.1.9 one-off wrappers:

```js
const smChapterControlArtifactLineRe = /.../i;

function smNormalizeControlLine(line) {
  return String(line || "")
    .trim()
    .replace(/^[>・杤s]+/, "")
    .replace(/^[<・懊申[(・・+/, "")
    .replace(/[>・槭曾])・云+$/, "")
    .trim();
}

function smLooksLikeChapterControlArtifactLine(line) {
  const raw = String(line || "").trim();
  const s = smNormalizeControlLine(raw);
  if (!s) return false;
  if (smLooksLikeChapterReferenceProseV417 && smLooksLikeChapterReferenceProseV417(raw)) return false;
  if (raw.length > 120 && /[縲ゑｼ・ｼ・?縲阪従$/.test(raw)) return false;
  return smChapterControlArtifactLineRe.test(s);
}

function smStripChapterControlArtifactLines(text) {
  return String(text || "")
    .replace(/\r\n/g, "\n")
    .replace(/\r/g, "\n")
    .split("\n")
    .filter(line => !smLooksLikeChapterControlArtifactLine(line))
    .join("\n")
    .replace(/\n{3,}/g, "\n\n")
    .trim();
}
```

This is only a sketch. The actual regex must be tested against all old failure forms and preservation cases.

### Mandatory Local Regression Tests Before Any Real API Run
Do not spend API tokens until deterministic local tests pass.

The tests must prove that all of these are removed/rejected:
- `・育ｬｬ9遶 邯壹￥・荏
- `隨ｬ9遶 邯壹￥`
- `縲千ｬｬ9遶 邯壹￥縲疏
- `隨ｬ荵晉ｫ縲∫ｶ壹￥`
- `隨ｬ9遶 縺､縺･縺汁
- `隨ｬ9遶 谺｡遶縺ｸ`
- `隨ｬ9遶 邯咏ｶ啻
- `END CHAPTER 9`
- `END OF CHAPTER 9`
- `CHAPTER 9 END`
- `CHAPTER 9 COMPLETE`
- `CHAPTER 9 CONTINUES`
- `To be continued`
- `NEXT CHAPTER`
- `・育ｶ壹￥・荏
- `邨らｫ縺ｸ...`
- `・懃ｬｬ9遶莠・・`
- `・育ｬｬ1遶縲∽ｺ・ｼ荏
- `縲千ｬｬ9遶邨ゆｺ・・`
- `縲先枚閼育ｶｭ謖√・谺｡遶騾｣邨舌Γ繝｢縲疏
- `・育ｶ壹￥遶繝ｻ螻暮幕縺ｮ`
- bare outline bullets at tails such as `繝ｻ谺｡遶...`, `繝ｻ蟶・浹...`, `繝ｻ蟆守ｷ・..`

The tests must prove that all of these are preserved:
- `隨ｬ9遶縺ｧ縺ｯ縲∝ｽｼ縺ｯ縺ｾ縺邨ゅｏ繧翫ｒ隱阪ａ縺ｪ縺九▲縺溘Ａ
- `隨ｬ9遶縺ｨ縺・≧繝ｩ繝吶Ν繧定ｦ九※縲∝ｽｼ螂ｳ縺ｯ闍ｦ隨代＠縺溘Ａ
- `蠖ｼ螂ｳ縺ｯ縲檎ｶ壹￥縲阪→縺縺題ｨ縺｣縺ｦ縲∝ｘ繧帝哩縺倥◆縲Ａ
- `邯壹￥髮ｨ髻ｳ縺ｮ荳ｭ縺ｧ縲∽ｺ御ｺｺ縺ｯ鮟吶▲縺ｦ豁ｩ縺・◆縲Ａ
- `迚ｩ隱槭・縺ｾ縺邯壹￥縺ｮ縺縺ｨ縲∝ｽｼ縺ｯ諤昴▲縺溘Ａ
- `縲仙ｮ後疏
- Normal chapter headings generated by the app, for example `# 隨ｬ9遶: 蜷堺ｻ倥￠繧峨ｌ縺溷､懊・陬ゅ￠逶ｮ`

Add a final combined-manuscript regression, not only per-chapter tail regression:

```text
# 隨ｬ9遶: 蜷堺ｻ倥￠繧峨ｌ縺溷､懊・陬ゅ￠逶ｮ
...ordinary prose...
・育ｬｬ9遶 邯壹￥・・
---

# 隨ｬ10遶: 譛邨らｫ
...ordinary prose...
縲仙ｮ後・Generated By AI Story Maker V4.1.9
```

Expected:
- The saved/visible/completed manuscript must not contain `・育ｬｬ9遶 邯壹￥・荏.
- `# 隨ｬ9遶:` and `# 隨ｬ10遶:` must remain.
- Exactly one final `縲仙ｮ後疏 must remain.
- The footer must remain once.

### Browser/QA Scan Snippet Used To Detect The Failure
This scan caught the v4.1.8 leak. Claude can reuse and expand it.

```js
const output = document.querySelector('#output')?.textContent
  || document.querySelector('#output-panel')?.textContent
  || '';

const contRe = /(?:^|\n)\s*[<・懊申[(・・?\s*(?:隨ｬ[\d・・・吩ｸ莠御ｸ牙屁莠泌・荳・・荵晏香逋ｾ蜊ゾ+遶\s*[縲・繝ｻ:・咯?\s*(?:邯壹￥|縺､縺･縺楯邯咏ｶ嘶谺｡遶縺ｸ)|(?:TO BE CONTINUED|CONTINUED|CONTINUE(?:D)?|NEXT CHAPTER))\s*[>・槭曾])・云?\s*[.縲ゅ・:・・縲√・窶・]*\s*(?=\n|$)/gi;

const matches = [...output.matchAll(contRe)].map(m => m[0].trim());
console.log(matches);
```

In v4.1.8 it returned:

```json
["・育ｬｬ9遶 邯壹￥・噂n\n---"]
```

Note: the regex above over-captured the following `---` because of whitespace punctuation handling. Fix the production classifier carefully; do not copy this exact regex without tightening.

### Commands Already Run For v4.1.8 Before This New Failure
- `node --check src/main.js`
- VM regression for English chapter labels and previous Japanese chapter-done labels
- `npm run build`
- `npm run lint --if-present`
- `git diff --check -- . ':!dist'`
- HTTP title check on port `5179`
- In-app browser version check at `http://localhost:5179/?v=local-v4.1.8-english-chapter-label`

These are now insufficient because the live QA found a new structural hole.

### Files Currently Dirty Before Claude Starts
Do not revert these blindly; they contain the current local v4.1.8 work.
- `src/main.js`
- `src/data.js`
- `index.html`
- `package.json`
- `package-lock.json`
- `README.md`
- `HANDOFF.md`

If Claude implements the next fix, version should likely advance to `v4.1.9` because this app uses single-digit patch increments and `4.1.8 -> 4.1.9` is valid. If another version after `4.1.9` is needed, the rule says use `4.2.0`, not `4.1.10`.

### Release / Deploy / Backup Rules
- Do not deploy or backup just because local checks pass.
- If the user later asks to deploy/release:
  - Annotated git tag message must be bilingual English/Japanese.
  - GitHub Release title/body must be bilingual English/Japanese.
  - Verify no English-only tag/release text.
- If the user later asks for a PS1 full backup:
  - Use the visible PowerShell window rule from workspace `AGENTS.md`.
  - Do not use a hidden/background backup.

### Acceptance Criteria For The Next Agent
- Local deterministic tests pass for the full artifact matrix above.
- Browser local version shows the new version, likely `Story Maker v4.1.9`.
- A no-API browser/manual smoke verifies the UI still loads on port `5179`.
- Only after that, ask the user to enter OpenAI API key in the UI if a real full-run QA is needed.
- A real full-run QA is accepted only if final scan shows:
  - 10/10 or planned chapters complete.
  - Exactly one `縲仙ｮ後疏.
  - Footer `Generated By AI Story Maker Vx.y.z` appears once.
  - No context memo / GMC+S / master prompt / repair log residue.
  - No chapter-control labels anywhere, including between chapters.
  - No `・育ｬｬn遶 邯壹￥・荏, `END CHAPTER n`, `隨ｬn遶莠・, `谺｡遶縺ｸ`, `邨らｫ縺ｸ`, or equivalent standalone control lines.

## Snapshot Date
2026-06-05T00:00:00+09:00

## Current Status
- Version: `v4.1.8`
- Branch: `main`
- Current task: v4.1.8 extends the OpenAI long-novel save guard after v4.1.7 user-entered OpenAI QA reached 9 / 10 chapters but saved an English chapter-control tail label, `END CHAPTER 9`.
- Development port: `http://localhost:5179/`
- Deployment/backups: v4.1.0 is committed, pushed, tagged, released, and deployed. Visible-window PS1 full backup completed: `backups\antigravity_full_backup_2026-06-04_170233.zip`. v4.1.8 is local implementation work; deterministic regression, build, lint, diff-check, HTTP, and in-app browser version checks passed. Do not run another full OpenAI API QA unless the user explicitly enters the key in the UI and asks to spend API.

## v4.1.8 Current Notes
- **Full-run QA finding / 螳蘗PI QA finding**: User-entered OpenAI long-novel QA on v4.1.7 reached 9 / 10 saved chapters, then final monitoring found `END CHAPTER 9` in the readable manuscript tail. The run was manually aborted before spending more tokens on chapter 10; treat that v4.1.7 run as not accepted.
- **Root cause / 蜴溷屏**: v4.1.7 covered Japanese/garbled chapter-done labels such as `・懃ｬｬ9遶莠・・`, but the save-time artifact classifier did not include standalone English provider control labels like `END CHAPTER 9`.
- **Guard extension / 繧ｬ繝ｼ繝画僑蠑ｵ**: `src/main.js` now treats standalone English chapter-control labels such as `END CHAPTER 9`, `END OF CHAPTER 9`, `CHAPTER 9 END`, and `CHAPTER 9 COMPLETE` as non-prose artifacts. `oa()` detects them, `le()` removes them from visible combined text, `He()` strips them from chapter tails, and `ot()` rejects any remaining line before save.
- **Local verification / 繝ｭ繝ｼ繧ｫ繝ｫ讀懆ｨｼ**: Passed `node --check src/main.js`, VM regression for `END CHAPTER 9`, `END OF CHAPTER 9`, `CHAPTER 9 END`, `CHAPTER 9 COMPLETE`, bracketed variants, previous Japanese chapter-done labels, and normal prose preservation; `npm run build`; `npm run lint --if-present`; `git diff --check -- . ':!dist'` (LF/CRLF warnings only); HTTP 200 title check on port 5179; and in-app browser version check at `http://localhost:5179/?v=local-v4.1.8-english-chapter-label`.
- **Next verification / 谺｡縺ｮ讀懆ｨｼ**: Full OpenAI API QA is still not accepted until a fresh paid-token run completes. Reloading to v4.1.8 cleared the memory-only API key, so ask the user to type the OpenAI key in the UI before another full-run API QA.

## v4.1.7 Current Notes
- **Full-run QA finding / 螳瑚ｵｰQA縺ｧ縺ｮ讀懷・**: User-entered OpenAI long-novel QA on v4.1.6 completed 10 / 10 chapters, produced 75,563 visible characters, and appended `Generated By AI Story Maker V4.1.6`, but the final manuscript scan found `・懃ｬｬ9遶莠・・` between chapters 9 and 10. Treat that v4.1.6 full run as not accepted for final save-quality.
- **Guard extension / 繧ｬ繝ｼ繝画僑蠑ｵ**: `src/main.js` now treats standalone chapter-completion labels such as `・懃ｬｬ9遶莠・・`, `・育ｬｬ1遶縲∽ｺ・ｼ荏, and `縲千ｬｬ9遶邨ゆｺ・・` as chapter-tail artifacts. `oa()` detects them, `qe()` / `He()` strip them from tails, `le()` removes them from combined visible text, and `ot()` rejects the chapter if any remain. Normal prose references such as `隨ｬ9遶縺ｧ縺ｯ縲・..縲Ａ are preserved.
- **Local verification / 繝ｭ繝ｼ繧ｫ繝ｫ讀懆ｨｼ**: Passed `node --check src/main.js`, VM regression against the exact failed `・懃ｬｬ9遶莠・・` form, `npm run build`, `npm run lint --if-present`, `git diff --check -- . ':!dist'` (LF/CRLF warnings only), HTTP 200 title check on port 5179, and in-app browser version check at `http://localhost:5179/?v=local-v4.1.7-chapter-done-tail-guard` showing `Story Maker v4.1.7`.
- **Next verification / 谺｡縺ｮ讀懆ｨｼ**: If the user approves another paid-token run, ask them to type the OpenAI key into the UI and then run a fresh full long-novel QA plus final manuscript scan. Do not treat v4.1.6 as accepted.

## v4.1.6 Current Notes
- **Full-run QA finding / 螳瑚ｵｰQA縺ｧ縺ｮ讀懷・**: User-entered OpenAI long-novel QA on v4.1.5 completed 10 / 10 chapters, produced 67,061 visible characters, and appended `Generated By AI Story Maker V4.1.5`, but the final scan found `縲先枚閼育ｶｭ謖√・谺｡遶騾｣邨舌Γ繝｢縲疏 in chapter 1 and an unfinished `・育ｶ壹￥遶繝ｻ螻暮幕縺ｮ` fragment at the end of chapter 9. Treat that v4.1.5 full run as not accepted for final save-quality.
- **Guard extension / 繧ｬ繝ｼ繝画僑蠑ｵ**: `src/main.js` now treats bracketed `譁・ц邯ｭ謖・..繝｡繝｢` / `谺｡遶騾｣邨・..繝｡繝｢` labels as management memo boundaries in `Nt()` / `Xe()` and strips the same labels through `le()`. It also rejects or trims incomplete parenthetical continuation fragments such as `・育ｶ壹￥遶繝ｻ螻暮幕縺ｮ` through the tail guard.
- **Local regression / 繝ｭ繝ｼ繧ｫ繝ｫ蝗槫ｸｰ**: Passed `node --check src/main.js` and a direct regression using the exact failed forms: `Xe()` / `Nt()` split `縲先枚閼育ｶｭ謖√・谺｡遶騾｣邨舌Γ繝｢縲疏, `le()` removes it from visible body, `$s()` flags it as management residue if still present, `oa()` detects `・育ｶ壹￥遶繝ｻ螻暮幕縺ｮ`, and `qe()` removes that tail while preserving normal prose.
- **Local verification / 繝ｭ繝ｼ繧ｫ繝ｫ讀懆ｨｼ**: Passed `node --check src/main.js`, `npm run build`, `npm run lint --if-present`, `git diff --check -- . ':!dist'` (LF/CRLF warnings only), local HTTP title check on port 5179, and in-app browser verification at `http://127.0.0.1:5179/?v=local-v4.1.6-memo-tail-guard` showing `Story Maker v4.1.6`, empty API field, neutral Gemini/OpenAI placeholder, and no console warnings/errors.
- **Next verification / 谺｡縺ｮ讀懆ｨｼ**: If the user approves another paid-token run, ask them to type the OpenAI key into the UI and then run a fresh full long-novel QA plus final manuscript scan. Do not treat v4.1.5 as accepted.

## v4.1.5 Current Notes
- **Full-run QA finding / 螳瑚ｵｰQA縺ｧ縺ｮ讀懷・**: User-entered OpenAI long-novel QA on v4.1.4 completed all 10 / 10 chapters and appended `Generated By AI Story Maker V4.1.4`, but the final manuscript scan still found final-line non-prose tail fragments: chapter 8 ended with a standalone `・彖, and chapter 9 ended with a `邨らｫ縺ｸ...` connector line after a scene separator. Treat that v4.1.4 full run as not accepted for final save-quality.
- **Guard extension / 繧ｬ繝ｼ繝画僑蠑ｵ**: `src/main.js` now treats lone bracket marks such as `<` / `・彖, standalone bracket-only lines, `(continued)` / `・育ｶ壹￥・荏, and next/final-chapter connector fragments as chapter-tail cleanup/rejection targets through `Is()`, `oa()`, `qe()`, and `Ls()`. The guard remains tail-only so normal in-story mentions of notes, keys, and evidence are not globally removed.
- **Local verification / 繝ｭ繝ｼ繧ｫ繝ｫ讀懆ｨｼ**: Passed `node --check src/main.js`, `npm run build`, `npm run lint --if-present`, `git diff --check -- . ':!dist'`, and a direct tail-guard regression for `・彖, `邨らｫ縺ｸ...`, `・育ｶ壹￥・荏, and separator-only endings. The in-app browser now shows `Story Maker v4.1.5`; reload cleared the memory-only API key.
- **Next verification / 谺｡縺ｮ讀懆ｨｼ**: Ask the user to type the OpenAI key in the UI again, then run another fresh full-run API QA and final manuscript scan.

## v4.1.4 Current Notes
- **Full-run QA finding / 螳瑚ｵｰQA縺ｧ縺ｮ讀懷・**: User-entered OpenAI long-novel QA on v4.1.3 completed all 10 / 10 chapters and appended `Generated By AI Story Maker V4.1.3`, but the tighter final manuscript scan still found non-prose chapter-tail fragments: a label-less bullet note at chapter 5, `(continued)`-style residue at chapter 6, `谺｡遶:` planning residue at chapter 7, and dangling parenthesis fragments at chapters 8-9. Treat that v4.1.3 full run as not accepted for final save-quality.
- **Guard extension / 繧ｬ繝ｼ繝画僑蠑ｵ**: `src/main.js` now strips/rejects broader long-novel tail fragments through `He()`, `oa()`, and `ot()`: scene separators at the actual tail, any tail bullet line, `・育ｶ壹￥・荏 / dangling parenthesis lines, and `窶・谺｡遶:` / planning-keyword lines. The goal is to keep rejected retry and outline tails out of readable manuscript text and carry-forward context.
- **Local verification / 繝ｭ繝ｼ繧ｫ繝ｫ讀懆ｨｼ**: Passed `node --check src/main.js`, `npm run build`, `npm run lint --if-present`, `git diff --check -- . ':!dist'`, and a VM regression for the broader tail stripper/gate. The in-app browser now shows `Story Maker v4.1.4`; the API field is empty after the update.
- **Next verification / 谺｡縺ｮ讀懆ｨｼ**: Ask the user to type the OpenAI key in the UI again, then run another fresh full-run API QA and final manuscript scan.

## v4.1.3 Current Notes
- **Full-run QA finding / 螳瑚ｵｰQA縺ｧ縺ｮ讀懷・**: User-entered OpenAI long-novel QA on v4.1.2 completed all 10 / 10 chapters and appended `Generated By AI Story Maker V4.1.2`, but the tighter final manuscript scan found label-less outline bullet fragments at several chapter endings, such as `繝ｻ谺｡遶...`, `繝ｻ蟶・浹...`, and `繝ｻ蟆守ｷ・..`. Treat that v4.1.2 full run as not accepted for final save-quality.
- **Guard extension / 繧ｬ繝ｼ繝画僑蠑ｵ**: `src/main.js` now strips trailing outline-style bullet lines from chapter tails through `He()` and rejects any remaining tail bullets through `ot()`. The target is only save-time long-novel body quality: chapter-end design notes must not enter readable manuscript text or carry-forward context.
- **Next verification / 谺｡縺ｮ讀懆ｨｼ**: Run `node --check src/main.js`, build/lint/diff checks, a VM regression for the bullet-tail stripper/gate, browser version check on port 5179, then ask the user to type the OpenAI key in the UI before another fresh full-run API QA.

## v4.1.2 Current Notes
- **Full-run QA finding / 螳瑚ｵｰQA縺ｧ縺ｮ讀懷・**: User-entered OpenAI long-novel QA on v4.1.1 completed all 10 / 10 chapters and produced a 77,488-character output with `Generated By AI Story Maker V4.1.1`, but the tighter final manuscript scan found chapter 8 ending with a standalone unfinished design fragment, `縲千ｫ蜀・. Treat that v4.1.1 full run as not accepted for final save-quality.
- **Guard extension / 繧ｬ繝ｼ繝画僑蠑ｵ**: `src/main.js` now extends `isTrailingStoryFragmentLine()` to strip or reject short trailing chapter/design fragments such as chapter-internal notes, chapter-end notes, scene labels, memo labels, turn labels, aftertaste labels, and related planning fragments before save.
- **Local verification / 繝ｭ繝ｼ繧ｫ繝ｫ讀懆ｨｼ**: Passed `node --check src/main.js`, `npm run build`, `npm run lint --if-present`, `git diff --check -- . ':!dist'`, and a trailing-fragment regression for `縲千ｫ蜀・, `縲千ｫ譛ｫ繝｡繝｢`, `縲仙ｴ髱｢`, `縲占ｨｭ險医Γ繝｢縲疏, `縲千ｬｬ1遶邨ゆｺ・・`, and `窶覗 while preserving `縲仙ｮ後疏.
- **Browser verification / 繝悶Λ繧ｦ繧ｶ讀懆ｨｼ**: In-app browser on `http://127.0.0.1:5179/?v=local-v4.1.2-fragment-guard` shows `Story Maker v4.1.2`, empty API field, neutral `Gemini 縺ｾ縺溘・ OpenAI 縺ｮAPI繧ｭ繝ｼ繧貞・蜉嫣 placeholder, and no browser warn/error logs.
- **Next verification / 谺｡縺ｮ讀懆ｨｼ**: Reload cleared the memory-only API key. Ask the user to type the OpenAI key in the UI before a fresh full-run API QA.

## v4.1.1 Current Notes
- **Paragraph normalization / 谿ｵ關ｽ謨ｴ蠖｢**: `src/main.js` now normalizes story paragraph spacing before standard output footer insertion and before long-novel chapter save gates. It removes blank empty lines between ordinary prose/dialogue lines while preserving one blank around headings, section markers, scene breaks, and memo-like structures.
- **Long-novel fail-closed gate / 髟ｷ邱ｨ菫晏ｭ伜燕繧ｲ繝ｼ繝・*: `Ve()` now includes paragraph-spacing checks for excessive blank lines, consecutive blank runs, and chat-like over-fragmentation. Long-novel retry guidance now explicitly says to use single newlines and not blank empty lines after every sentence.
- **Prompt contract / 繝励Ο繝ｳ繝励ヨ螂醍ｴ・*: The Gen-4 quality contract now includes paragraph discipline for standard and long outputs. OpenAI live QA still requires the user to type the key into the UI; do not paste or store the key.
- **API input placeholder / API蜈･蜉帶ｬ・*: Empty API state now shows a neutral `Gemini 縺ｾ縺溘・ OpenAI 縺ｮAPI繧ｭ繝ｼ繧貞・蜉嫣 placeholder instead of defaulting the visible empty field to Gemini-only wording.
- **Design label guard / 險ｭ險医Λ繝吶Ν貍上ｌ髦ｲ豁｢**: First OpenAI chapter QA showed paragraph spacing was fixed but planning labels such as chapter turn/aftertaste headings leaked into the saved text. `src/main.js` now strips those labels before save and adds them to the fail-closed pre-save quality gate if residue remains.
- **OpenAI live QA / OpenAI螳蘗PI讀懆ｨｼ**: User-entered OpenAI QA on port 5179 recognized `ChatGPT API`, generated long-novel chapter 1, rejected the first chapter fail-closed for management-info residue and teaser ending, regenerated, saved chapter 1 at 8,891 body chars, and stopped at 1 / 10 as requested. Saved output scan: max consecutive blank lines 1, blank-line ratio 1.6%, no design-label residue hits.
- **Trailing fragment guard / 遶譛ｫ譁ｭ迚・ぎ繝ｼ繝・*: The same live QA exposed a saved chapter ending on a standalone scene separator `窶覗. `src/main.js` now strips/rejects trailing separators and dangling chapter/design fragments (`窶覗, `・柿, `縲千ｬｬ1遶邨ゆｺ・・`, `縲舌％縺ｮ遶縺ｮ繧ｿ繝ｼ繝ｳ縲疏) before save.
- **Post-guard OpenAI rerun / 遶譛ｫ繧ｬ繝ｼ繝牙ｾ後・OpenAI蜀肴､懆ｨｼ**: A second user-entered OpenAI QA on port 5179 rejected the first regenerated attempt fail-closed because memo stripping left only 2,070 body chars, then regenerated and saved chapter 1 at 8,637 body chars, stopping at 1 / 10. Saved body scan: 8,676 chars in output body, max consecutive blank lines 1, blank-line ratio 9.8%, no memo residue, no design-label residue, no management-info residue in body, and no trailing separator/design fragment; final line was `・育ｬｬ1遶縲∽ｺ・ｼ荏.
- **Local verification / 繝ｭ繝ｼ繧ｫ繝ｫ讀懆ｨｼ**: Passed `node --check src/main.js`, `npm run build`, `npm run lint --if-present`, `git diff --check -- . ':!dist'`, an attachment-based paragraph normalization regression, a design-label residue regression, a trailing-fragment regression, and in-app browser placeholder/OpenAI recognition verification on port 5179.

## v4.1.0 Current Notes
- **Verified summary / 讀懆ｨｼ貂医∩隕∫ｴ・*: v4.1.0 adds the Gen-4 quality contract, post-generation AI editorial evaluation, and stricter API-key handling. Checks passed: `node --check src/main.js`, `npm run build`, `npm run lint --if-present`, and `git diff --check -- . ':!dist'`.
- **Fresh Gemini API QA / Gemini螳蘗PI讀懆ｨｼ**: In-app browser QA on port 5179 generated 3,197 characters in short-novel mode with `Generated By AI Story Maker V4.1.0`. Explicit settings and tags were preserved: `繧ｳ繝｡繝・ぅ / 繝峨ち繝舌ち`, `迴ｾ莉｣ / 2020蟷ｴ莉｣`, `迴ｾ莉｣譌･譛ｬ / 蝠・ｺ苓｡輿, `蜈ｨ蟷ｴ鮨｢ / 隱ｰ縺ｧ繧よ･ｽ縺励ａ繧義, `繝上ャ繝斐・繧ｨ繝ｳ繝・/ 螟ｧ蝗｣蜀・, and `遏ｭ邱ｨ蟆剰ｪｬ(縲・000蟄・`.
- **Gen-4 evaluation result / 隨ｬ4荳紋ｻ｣隧穂ｾ｡邨先棡**: The progress log recorded fact/logic 5, emotional arc 5, human noise 5, constraint fit 5, and commercial prose 4. Weak point was that the slapstick comedy could be more exaggerated.
- **API key handling / API繧ｭ繝ｼ蜃ｦ逅・*: Gemini keys are sanitized before save/send, request URLs encode the key, masked/too-short/bad-character values are rejected, and Gemini keys must pass a live model-list check before being accepted as saved. Do not ask the user to paste keys into chat or files.
- **Gen-4 Quality Contract / 隨ｬ4荳紋ｻ｣蜩∬ｳｪ螂醍ｴ・*: Standard and long-novel generation now append an internal quality contract for preflight fact/logic audit, emotional-arc ledger, controlled human noise, and anti-uniform endings. Selected/manual settings remain fixed and must not be rewritten by the quality pass. / 騾壼ｸｸ逕滓・縺ｨ髟ｷ邱ｨ逕滓・縺ｫ縲∽ｺ句燕繝輔ぃ繧ｯ繝医・繝ｭ繧ｸ繝・け逶｣譟ｻ縲∵─諠・峇邱壹Ξ繧ｸ繝｣繝ｼ縲∝宛蠕｡縺輔ｌ縺滉ｺｺ髢鍋噪繝弱う繧ｺ縲∝ｮ壼梛逹蝨ｰ蝗樣∩繧定ｿｽ蜉縲る∈謚槭・謇句・蜉帶ｸ医∩險ｭ螳壹・蜩∬ｳｪ謾ｹ蝟・・縺溘ａ縺ｫ繧ょ､画峩荳榊庄縺ｧ縺吶・- **Post-Generation AI Editorial Evaluation / 逕滓・蠕窟I邱ｨ髮・ｩ穂ｾ｡**: Completed normal outputs and completed long novels call the active API for a JSON editorial evaluation covering fact/logic, emotional arc, human noise, constraint fit, and commercial prose strength, then write the report into the progress log. / 逕滓・螳御ｺ・ｾ後√い繧ｯ繝・ぅ繝泡PI縺ｧ繝輔ぃ繧ｯ繝・繝ｭ繧ｸ繝・け縲∵─諠・峇邱壹∽ｺｺ髢鍋噪繝弱う繧ｺ縲∵欠螳夐・螳医∝膚讌ｭ隱ｭ蠕梧─繧谷SON隧穂ｾ｡縺励・ｲ陦後Ο繧ｰ縺ｸ陦ｨ遉ｺ縺励∪縺吶・
## v4.0.9 Current Notes
- `src/main.js` now classifies Gemini failures through dedicated helpers for auth, quota, safety, and model/request errors. A generic `invalid` message no longer marks the user-entered API key invalid.
- If the diagnostic model-list endpoint returns `Available Models: ...`, Story Maker treats the key as present/valid and reports remaining failures as model/request errors with per-model details.
- The stale `gemini-3.5-flash` model candidate was removed from the visible Gemini model list and fallback arrays; fallback now starts with Gemini 2.5/current latest candidates.
- Verification passed after patch: `node --check src/main.js`, `npm run build`, `npm run lint --if-present`, `git diff --check -- . ':!dist'`, helper classification regression, fresh in-app browser Gemini API smoke on port 5179, `npm run deploy`, remote `origin/gh-pages:index.html`, and live GitHub Pages cache-busted smoke check. The API smoke generated 2,413 chars with `Generated By AI Story Maker V4.0.9` and no false invalid-key error.

## v4.0.8 Current Notes
- `src/main.js` now has a shared footer path: `appFooterText()`, `stripAppFooter()`, and `appendAppFooter()`. The old short-form `Generated by Super FURU AI Story` footer is replaced with `Generated By AI Story Maker V4.0.8`.
- Long-novel finalization now appends the shared footer after `縲仙ｮ後疏; `Ir()` allows that exact footer after the final marker, and completed manuscript assembly re-applies the shared footer once to avoid duplicates.
- Long-novel initial/continuation/reproduction prompt contracts now forbid repeated scenery-only afterglow endings and require concrete human friction such as a messy spoken line, practical task, bodily discomfort, awkward silence, mundane noise, or unfinished obligation.
- `Er()` now rejects formulaic chapter endings that lean on sunset/sky/wind/light/echo emotional imagery without concrete human friction, sending the chapter into the existing fail-closed retry path instead of saving it.
- Verification passed: `node --check src/main.js`, `npm run build`, `npm run lint --if-present`, `git diff --check -- . ':!dist'`, HTTP/title check on port 5179, in-app browser visible version check, and VM regression checks for `Ht()`, `Ir()`, `appendAppFooter()`, `Er()`, and `xr()`.

## v4.0.7 Current Notes
- User clarified that every configurable item/detail must be treated as strict user intent, not only broad categories. Manual entries, detailed sub-chip selections, and category-only selections must not be overwritten by mode defaults, individual random buttons, or one-shot full random.
- `src/main.js` now tracks `axisSource` and `modeSource`: `manual`, `selectedDetail`, and `selectedCategory` are fixed; `default` and `random` remain randomizable. Category-only axes can only receive an open detail from inside that selected parent category.
- Flat random idea seeds now filter unrequested digital/electronics/AI/SNS/gadget motifs unless an SF/future/cyber/AI/robot-like axis is explicitly selected. Prompt contracts now also forbid filling unspecified gaps with those digital motifs.
- Verification passed: `node --check src/main.js`, `npm run build`, `npm run lint --if-present`, `git diff --check -- . ':!dist'`, HTTP 200/title check on port 5179, and route-mocked browser QA.
- Route-mocked QA covered all 15 output modes with selected mode + Comedy / Modern Japan / All Ages categories plus manual theme/era/ending. Full random did not overwrite fixed values, category-only axes filled only inside the selected parent category, and prompt capture for 4koma/novel/long included the strict-setting and no-digital-gap contract.
- Digital-debias QA sampled 80 theme random values and 60 narration random values; no unrequested AI/SNS/electronics/gadget/robot/quantum/cyber terms appeared. Do not ask the user to paste an API key into chat or save it to files; ask them to type it into the in-app browser UI for live QA.
- Live API QA passed for a category-only `4koma` case: selected mode + Comedy / Modern Japan / All Ages stayed unchanged after generation, with no switch to Suspense, Tokyo, or Adult Audience; output was 502 chars.
- Live API QA passed for a detailed-chip `novel` case: Daily Life / Convenience Store, Comedy / Slapstick, Modern Japan / Shopping Street, All Ages / Family, Modern / 2020s, Happy End / Grand Finale, and Third Person / Omniscient stayed unchanged; tags preserved parent/detail pairs; output was 3,840 chars.
- Long-mode live API QA was attempted with category-only Comedy / Modern Japan / All Ages, but after attempting to abort at the first useful checkpoint the in-app browser tab stopped responding to DOM/screenshot controls. A reload/new-tab attempt did not recover the browser control surface. Do not treat long live QA as passed; ask the user to refresh/open a fresh in-app browser tab and re-enter the API key for the long-only retry.

## v4.0.6 Current Notes
- Root cause was system-side state mutation, not only model drift: the common generate handler called `_e(i.mode || "4koma", { includeAxes: true })`, which reached `Fa()` and rewrote genre/worldview/target/theme inputs to mode defaults when generation started.
- `src/main.js` now removes that generate-start refill call. Generation logs that selected/manual settings are fixed and collects the current UI state without changing visible chips or custom inputs.
- Category-only selections are now treated as real values: `Va()` counts `catKey`. Detailed selections are now collected as parent/detail combined values, for example `繧ｳ繝｡繝・ぅ / 繝峨ち繝舌ち`, `迴ｾ莉｣譌･譛ｬ / 蝨ｰ譁ｹ驛ｽ蟶Ａ, and `蜈ｨ蟷ｴ鮨｢ / 繝輔ぃ繝溘Μ繝ｼ`.
- Prompt contracts for standard, scenario, long-novel initial/continuation, and reproduction generation now forbid replacing user settings with unrequested subvalues such as plot twist, Tokyo, or adult audience.
- This is a cross-mode fix because `tr()` is the shared generation entry point for long novel, normal story, 4koma/scenario, and other modes.
- Checks passed for v4.0.5: `node --check src/main.js`, `npm run build`, `npm run lint --if-present`, `git diff --check -- . ':!dist'`, HTTP 200/version smoke on port 5179, and Playwright route-mocked browser verification across 17 cases: all 15 output modes with category-only settings plus manual-entry checks for `novel` and `long`. No setting deltas, no old default-refill log, and prompts contained the chosen Comedy/Modern Japan/All Ages values.
- After user entered the API key, live Gemini QA also passed on port 5179: `4koma` category-only generated 809 chars with settings unchanged; `novel` manual fields generated 1,941 chars with exact manual values preserved; `long` category-only generated chapter 1, stopped at `1 / 10` with 6,475 chars saved, then was manually aborted. No test showed a switch to suspense, Tokyo, or adult audience.
- Additional v4.0.6 detail-subchip QA passed all 15 modes with theme/genre/worldview/target/era/ending/narration subchips selected. UI state stayed unchanged after generation start, the old default-refill log stayed absent, and prompt collection now includes the combined parent/detail values.
- Deploy passed for v4.0.6: `npm run deploy` published `dist` to `gh-pages`; `origin/gh-pages:index.html` and the live GitHub Pages URL both show `Story Maker v4.0.6`.

## v4.0.3 Current Notes
- User noticed that the long-novel self-score showed text scores but not visible bars. Browser inspection confirmed the rendered rows used `score-bar` with widths such as `96%`, but CSS only styled `score-bar-fill`, leaving the fill height at `0px`.
- `src/main.js` now renders long-novel scores with the same classes as the existing short-form score board: `score-row passed`, `score-bar-fill`, and `score-val`.
- `src/style.css` also styles legacy `score-bar` / `score-value` aliases so any already-rendered long-novel score rows display visible bars once the CSS updates.
- The latest v4.0.2 full API QA completed 12 / 12 chapters, 99,225 characters, clean final manuscript scan, and visible scores: 莨冗ｷ壼屓蜿主ｺｦ 96 / 襍ｷ謇ｿ霆｢邨舌・讒矩 97 / 蛻ｶ邏・・螳亥ｺｦ 100.
- Retry analysis from that run showed several chapters reached audit repair attempt `2/2`, but whole-chapter regeneration only used `1/2` or `1/4`. Do not simply increase audit repair attempts; keeping repair at two avoids over-patching a structurally broken draft.
- `src/main.js` now treats unresolved chronology/geography/setting/character/foreshadowing contradictions as structural failures that get the extended four-regeneration budget plus state-lock guidance: fix position, time, possessions, injuries, life/death, and relationship state before rewriting.
- Checks passed for v4.0.3: `node --check src/main.js`, `node --check src/consistencyAudit.js`, `npm run build`, `npm run lint --if-present`, `git diff --check -- . ':!dist'`, and in-app browser version check showing `Story Maker v4.0.3`.
- Deploy checks passed: no generated garbage files found; secret scan found no real API keys or credentials, only expected API input/link text and existing public GitHub links. README changelog includes v4.0.3. `v4.0.3` annotated tag and GitHub Release were created with English/Japanese wording.
- `npm run deploy` published successfully. `origin/gh-pages:index.html` and live GitHub Pages smoke check both show `Story Maker v4.0.3`; public API field is empty.

## v4.0.2 Current Notes
- Fresh API QA on `v4.0.1` reached chapter 8 and exposed a real save-through bug: when a repair candidate was rejected for being too short or changing length too much, the unchanged draft could be re-audited, receive a clean second audit result, and then be saved.
- Evidence captured from the in-app browser showed the phrase `雜ｳ繧翫ｋ縺九↑・歔 present at the end of chapter 7 and again at the start of chapter 8, confirming chapter-to-chapter duplicate prose was saved after the rejected repair path.
- `src/consistencyAudit.js` now returns a blocking issue when a repair candidate fails `validateFixedText` or the length-ratio gate, instead of continuing to re-audit the unchanged draft.
- `src/main.js` contains the same fail-closed behavior in the bundled runtime path used by the browser app: rejected repairs now return `remainingCriticalCount >= 1`, which sends long-novel chapters into the existing whole-chapter regeneration flow.
- Version metadata is synced to `v4.0.2` across package files, visible UI, runtime metadata, and reproduction metadata. README changelog now has a `v4.0.2` entry.
- Checks passed after the patch: `node --check src/main.js`, `node --check src/consistencyAudit.js`, `npm run build`, `npm run lint --if-present`, and `git diff --check -- . ':!dist'` (only normal LF-to-CRLF warnings).
- Fresh full API QA on `v4.0.2` after user UI API-key entry completed 12 / 12 chapters with 99,225 visible characters. Final scan found 12 parsed chapters, one final `縲仙ｮ後疏, no context memo / GMC+S / progress-log / code-fence contamination, and no duplicate-chapter hints.

## v4.0.1 Current Notes
- Long-novel completion now builds a deterministic score object and renders the existing `thought-score-board` UI with the same three categories used by shorter modes: `莨冗ｷ壼屓蜿主ｺｦ`, `襍ｷ謇ｿ霆｢邨舌・讒矩`, and `蛻ｶ邏・・螳亥ｺｦ`.
- The score board is hidden when a long-novel run resets/starts, then shown only after all chapters complete and the final manuscript is displayed.
- Long-novel chapter prompts now add an "Entertainment engine" rule set: each chapter needs an irreversible choice, visible cost, expectation reversal, scene-to-scene escalation, and exposition converted into conflict/objects/gestures/decisions.
- Version metadata was bumped to `v4.0.1` across visible UI, package files, runtime metadata, and reproduction metadata.
- README changelog was consolidated into a single compact section. Old duplicate/obsolete changelog blocks were removed.
- Fresh API QA on `http://127.0.0.1:5179/?v=local-v4.0.1` reached chapter 8 before being stopped to avoid further API consumption. Confirmed behaviors: chapter 2 and 3 contradiction repair passed after recheck; chapter 6 was rejected fail-closed, regenerated, then adopted; chapter 7 early-resolution gate and physical-key contradictions triggered regeneration and eventually passed.
- QA also found two issues before completion: the main `繧ｹ繝医・繝ｪ繝ｼ逕滓・` button could become enabled during long-novel retry/regeneration, and the audit/fix prompts could let the model confuse the current unsaved retry draft with saved recent chapters. `src/main.js` now keeps long-mode UI locked across retry gaps, and `src/consistencyAudit.js` now explicitly separates saved canon from the current draft/rejected retry drafts.
- Post-fix checks passed: `node --check src/main.js`, `node --check src/consistencyAudit.js`, `npm run build`, `npm run lint --if-present`, `git diff --check -- . ':!dist'`, and in-app browser reload. Reload cleared the in-memory API key; full post-fix API QA still requires the user to type the key into the UI again.

## v4.0.0 Current Notes
- `v3.9.9` fresh in-app Gemini QA after user API entry saved chapter 1, then stopped fail-closed at chapter 2 with `髟ｷ縺・ｪｬ譏取ｮｵ關ｽ縺悟､壹￥縲∝ｴ髱｢縺ｮ蛻・ｌ逶ｮ縺御ｸ崎ｶｳ縺励※縺・∪縺呻ｼ育ｬｬ2遶: 4谿ｵ關ｽ・荏. The rejected chapter 2 was not appended to the readable manuscript; the output box kept the clean chapter 1 manuscript.
- Root cause: the paragraph-density gates counted only blank-line-separated blocks, while generated Japanese prose often uses normal single-newline paragraph breaks. Properly paragraphed prose could be misread as a few giant text blocks.
- Versioning correction: after `v3.9.9`, the valid next app version is `v4.0.0`; minor and patch slots must stay single digit.
- `v4.0.0` changes the paragraph-density checks to count normal visible line breaks, adds explicit prompt rules for visible paragraph breaks, and gives paragraph/scene-density failures the extended four-regeneration path with specific retry guidance.
- Fresh Gemini in-app browser QA completed 12 / 12 chapters at 96,555 visible characters. The final scan found 12 chapter headings, exactly one `縲仙ｮ後疏, no context memo / GMC+S / regeneration-log contamination, no bare heading artifacts, and no stray `AND` route residue.
- Notable gates during QA: chapter 2 passed the corrected paragraph-density handling; chapter 4 and chapter 8 rejected unresolved contradictions and regenerated; chapter 9 rejected visible design-memo bullets; chapter 10 rejected premature whole-story resolution twice before saving a non-final continuation.
- User asked Codex to also deploy from this side at appropriate times. After the passing full QA, `npm run deploy` published `v4.0.0` to GitHub Pages. Remote `origin/gh-pages:index.html` and the live page both show `Story Maker v4.0.0`.

## v3.9.9 Previous Notes
- `v3.9.8` fresh in-app Gemini QA after user API entry saved 8 / 12 chapters, then stopped fail-closed during chapter 9. The broken chapter 9 was not saved.
- That run showed the save gates worked, but repeated inert empty Markdown heading lines (`#` / `##`) consumed all regeneration attempts. One chapter 9 retry also introduced a critical setting contradiction by making 螳溯干 into `鬮倡ｬ螳溯干` / 鄒主調's sister instead of 逾槫ｴ惹ｿｮ蟷ｳ's sister; repair fixed the contradiction, but the empty heading artifact remained and the chapter stayed rejected.
- `v3.9.9` strips bare empty Markdown heading artifacts during long-novel body cleanup before the save gate, while preserving the fail-closed management-info gate if cleanup misses a real leak.
- Late non-final whole-story resolution detection now starts at roughly 70% of the planned chapter count instead of only the penultimate chapter, and those late-resolution failures get the extended four-regeneration path.
- When a chapter stops fail-closed after prior clean chapters exist, the readable output box now keeps the saved clean manuscript instead of appending the error text into the manuscript view; the error remains in status/logs.
- Static/browser smoke checks passed for v3.9.9. The app is open on `http://localhost:5179/` showing `v3.9.9`; the update cleared the in-memory API key, so fresh full API QA is waiting for the user to enter the key in the UI again.

## v3.9.8 Previous Notes
- `v3.9.7` fresh in-app Gemini QA after user API entry saved 8 / 10 chapters, then stopped fail-closed during chapter 9 with repeated chapter 8 replay / data-center timeline rewind and premature `縺吶∋縺ｦ...邨ゅｏ` wording. The broken chapter 9 was not saved.
- `v3.9.8` adds a concrete previous-chapter endpoint anchor to next-chapter prompts, so late non-final chapters must continue from the saved chapter's final location/state instead of redoing travel, arrival, or already witnessed events.
- Regeneration guidance now treats rejected current-chapter output as non-canon, preserves chronology during duplicate/replay retries, and keeps duplicate/near-final retry paths at up to four full-chapter regenerations even if later failures become audit/timeline contradictions.
- Static/browser smoke checks passed. The app is open on `http://localhost:5179/` showing `v3.9.8`; the reload cleared the in-memory API key again, so fresh full API QA is waiting for the user to enter the key in the UI.

## v3.9.7 Previous Notes
- `v3.9.6` fresh in-app Gemini QA saved 8 / 10 chapters, then stopped fail-closed during chapter 9 with long exposition paragraphs plus premature whole-story resolution (`縺吶∋縺ｦ...邨ゅｏ`).
- The v3.9.6 gates behaved correctly during that run: empty Markdown headings were rejected before save in multiple chapters, and broken chapter 9 retries were not adopted into the manuscript or memory state.
- `v3.9.7` added a specific prompt lock for the chapter immediately before the final chapter: do not resolve the whole story, do not declare victory/final reconstruction, do not replay completed prior events, and keep action/dialogue/choice beats every 400-900 characters.

## v3.9.6 Previous Notes
- `v3.9.5` in-app Gemini QA completed all 10 / 10 chapters with 74,267 visible manuscript characters and one final `縲仙ｮ後疏.
- The same final scan found no repair-log, master-prompt, code-fence, or regeneration-text contamination, but did find two bare empty Markdown headings (`##`) inside the saved manuscript body.
- `v3.9.6` adds a fail-closed save-gate pattern for bare Markdown headings (`#` through `######`) so future chapters with empty heading artifacts are rejected before save and can regenerate instead.
- Fresh v3.9.6 full API QA later saved 8 / 10 chapters and stopped fail-closed at chapter 9, which led to the v3.9.7 late non-final prompt patch.

## v3.9.5 Previous Notes
- `v3.9.4` in-app Gemini QA stopped fail-closed before saving chapter 1 because the pre-save literary gate detected a teaser/preview-style chapter ending. This confirmed the save gate blocks bad prose, but exposed that chapter 1 was not using the later-chapter regeneration route.
- `v3.9.5` gives chapter 1 the same retry-state preservation and full-chapter regeneration behavior as later chapters for save-gate failures.
- Chapter 1 retry prompts now include the prior rejection reason and explicitly ask for a full rewrite that avoids teaser endings, repair chatter, and references to the failed attempt.
- Full API QA on `v3.9.5` later completed 10 / 10 chapters, but final scanning found two bare `##` lines, so `v3.9.6` was added before accepting the result.

## v3.9.4 Previous Notes
- `v3.9.3` in-app Gemini QA correctly stopped fail-closed at chapter 4 because the provider repeatedly replayed long chapter 3 prose. The gate worked, but the regeneration guidance was not strong enough to escape the replay loop.
- `v3.9.4` keeps the duplicate/replay save gate fail-closed, but duplicate-specific rejections now get up to four complete chapter regenerations instead of two.
- `v3.9.4` adds stronger retry instructions that forbid prior chapter openings, paragraphs, dialogue runs, metaphors, and scene placement, and require the retry to restart from a different location, obstacle, or choice.

## v3.9.3 Previous Notes
- `v3.9.2` full in-app Gemini QA completed 12 / 12 chapters and 91,252 characters, but deeper manuscript scan found exact long paragraph replay from chapter 7 inside chapter 8 plus one observed typo (`郢ｰ縺｣蠎・￡`).
- `v3.9.3` adds a save-time fail-closed duplicate gate that compares a candidate chapter against already saved chapter bodies and rejects long exact paragraph/opening-scene replay before the chapter can be adopted.
- `v3.9.3` also adds automatic cleanup for `郢ｰ縺｣蠎・￡` / `郢ｰ縺｣蠎チ and synchronizes the prompt rule to forbid chapter-body replay while preserving consequence carry-forward.

## v3.9.2 Previous Notes
- `v3.9.2` strengthens long-novel chapter prompts with an internal 3-5 scene ledger requirement, character agency, consequence carry-forward, dialogue subtext, and final-paragraph aftertaste.
- Long-novel save checks now add a literary-density gate that rejects chapters that look like compressed summaries, lack sensory/physical detail, lack visible choice/conflict, or end with teaser/preview wording.
- Full in-app Gemini QA completed 12 / 12 chapters and 91,252 characters; final scan found no memo/master-prompt/repair-log contamination and exactly one final marker, but deeper duplicate scan found chapter 7 prose replay inside chapter 8.

## v3.9.1 Previous Notes
- `v3.9.1` fixes the startup/default handling gap: the first visible mode now has visible defaults immediately, and generation runs one final default-fill pass before settings are read.
- Mode clicks now replace only system-applied default values. User-entered, manually selected, random, or locked values are preserved instead of being overwritten.
- Long-novel mode now gets meaningful visible defaults when selected from a fresh startup state: mystery/suspense, modern Tokyo, adult reader, surprise ending, and ensemble third-person narration.
- Internal version drift from the v3.9.0 deployment was corrected across `src/main.js`, `src/prompt.js`, `src/data.js`, `index.html`, `package.json`, and `package-lock.json`.

## v3.8.9 Previous Notes
- `v3.8.8` full Gemini browser QA completed 10 / 10 chapters with 74,659 manuscript characters, one final `縲仙ｮ後疏, and no management memo / repair log / English residue contamination. It also confirmed the output box stayed scroll-contained.
- The remaining quality defect in that run was weak completed-header outline lines such as `隨ｬ1遶: 隨ｬ1遶`, abstract fallback rows for some chapters, and weak axis labels such as `繝・・繝槭後Λ繝ｳ繝繝縲港. `v3.8.9` normalizes generic chapter titles at save time, derives/falls back to concise chapter labels for completed headers, and replaces empty/random axes with natural defaults.
- `v3.8.7` full Gemini browser QA reached 11 / 12 saved chapters, then stopped fail-closed in chapter 12 because the provider kept replaying the chapter 11 climax and the old English-residue gate also treated valid proper nouns such as `Morris` as hard failures.
- `src/main.js` now rejects near-final chapters that prematurely complete the whole-story objective before the final chapter, adds a final-chapter state lock that forbids rewinding saved events, and narrows the English-residue gate to line-only scraps.
- `v3.8.0` was already the deployed baseline; the active Codex patch is `v3.8.9`.
- `src/main.js` now builds a deterministic visible long-novel header block before chapter 1, even if the provider omits those sections.
- `v3.8.5` fixes the v3.8.4 regression where fallback chapter outlines repeated the same generic sentence and provider metadata such as `繧ｿ繧､繝医Ν:`, `繝ｭ繧ｰ繝ｩ繧､繝ｳ:`, and `蜈ｨ讒区・:` could remain inside the first chapter body.
- `src/main.js` now normalizes the final chapter before the save gate so the saved manuscript ends with exactly one independent `縲仙ｮ後疏 line.
- Full user-entered Gemini QA on `v3.8.5` completed 10 / 10 chapters and ended with one `縲仙ｮ後疏, but found the finished header still contained logline/planning metadata and an 8-chapter plot outline. `v3.8.6` rebuilds the final header from the saved chapter roster and prints only title, synopsis, and plot outline.

## Changed Files
| File | Purpose |
|---|---|
| `src/main.js` | Maintains long-novel pause, continuation, audit, save-gate, preview-scroll, regeneration safeguards, fallback outline quality, v3.8.6 final-header rebuild, v3.8.8 final-chapter/near-final story gates, v3.8.9 chapter-title / axis-label normalization, v3.9.1 default-visible mode presets, v3.9.2 literary-density gates, v3.9.3 chapter-replay rejection, v3.9.4 duplicate-specific regeneration hardening, v3.9.5 chapter-1 quality-gate regeneration, v3.9.6 empty-Markdown-heading rejection, v3.9.7 late non-final chapter prompt lock, v3.9.8 endpoint-anchor / retry-chronology hardening, v3.9.9 empty-heading cleanup / late-resolution retry hardening / clean output error handling, v4.0.0 paragraph-density counting / retry hardening, and v4.0.1 long-novel score-board rendering plus entertainment-engine prompt rules. |
| `src/prompt.js` | Long-novel chapter guidance now uses the same roughly 8,000 chars/chapter planning basis, so 80,000 chars plans as about 10 chapters; char-count labels are parsed robustly; next-chapter prompts now prioritize the latest context memo/GMC+S and forbid replaying completed prior events; non-final chapters now explicitly cannot resolve the whole story; v4.0.1 adds the same entertainment-engine guidance and reproduction metadata version sync. |
| `src/api.js` / `src/consistencyAudit.js` | Long-novel audit/fix calls now use bounded timeout and model-attempt options to reduce provider-side stalls during inspection and repair. |
| `src/style.css` | Disables smooth scrolling and scroll anchoring while long-novel live preview is active; during and after long-novel generation the novel body itself now fills the remaining right-side height and scrolls inside the manuscript box. |
| `package.json` / `package-lock.json` | Version synced to `4.0.1`. |
| `index.html` / `src/data.js` | Visible/internal version synced to `v4.0.1`; long-novel panel keeps a fixed live status line. |
| `README.md` | Added v4.0.1 long-novel self-grading and story-quality prompt notes, plus prior v4.0.0 versioning correction, paragraph-density gate, retry-hardening, QA-pass, and deploy changelog notes. |

## Done
- Reverted the earlier v3.6.x behavior where the pause button became a disabled progress indicator during chapter generation.
- Pause during generation now means "finish the current chapter, then stop before the next chapter"; clicking again cancels that reservation.
- The automatic continuation loop now logs why it is continuing: body chars, required minimum chars, raw chars, memo marker presence, and final marker presence when relevant.
- The continuation prompt is now a tight "continue from here" prompt using the partial chapter output only, avoiding full-prompt replay and model replanning.
- Continuation output is now merged before the existing context memo, and a newer generated memo replaces the old memo. This fixes false body-short failures after continuation.
- Live preview now strips context memo /莨冗ｷ夂ｮ｡逅・blocks before rendering the visible manuscript, preventing management notes from appearing in the main output area during generation.
- Long-novel streaming no longer calls `scrollIntoView` on every chunk, and the output pane disables smooth scroll anchoring during live preview to prevent vertical jumping.
- Long-novel live preview now scrolls the manuscript box itself to the latest text so the first chapter title does not remain fixed while generation continues below.
- Long-novel panel now has a fixed live status row that updates during generation, continuation, audit, save checks, pauses, aborts, and completion.
- Aborting a long-novel run now clears `ln-generating` / live-preview state immediately, so the UI no longer appears active after interruption.
- Full-run QA on `v3.7.3` reproduced a chapter 10 stop: raw continuation reached about 24,753 chars while extracted body stayed at 4,006 chars because the first premature final marker cut off later continuation.
- Final-chapter continuation now removes premature final markers from the existing partial text before requesting/merging continuation, and body extraction uses the last final marker.
- Long-novel planning no longer lets prompt guidance and runtime state diverge for the common 80,000-char mode.
- Long-novel prompt planning now safely parses numeric, comma-separated, and Japanese "荳・ char-count labels instead of dividing raw strings.
- First-chapter aborts keep a meaningful planned chapter count and show "chapter 1 generation interrupted" instead of "chapter 0 interrupted".
- Next-chapter prompts now make the latest context memo and next-scene GMC+S higher priority than the older overall outline, preventing a completed prior event from being replayed as a new chapter event.
- When long-novel audit or save gates reject a chapter, the app now regenerates the whole chapter up to two times with explicit failure guidance before the final fail-closed stop.
- Long-novel audit/fix calls now use bounded timeout/model-attempt settings so broken inspection/fix responses do not stall the full run as long.
- Full-run QA on `v3.7.6` was manually stopped after chapter 4 because a partial `縲仙・遶縺ｮ` management marker leaked into the manuscript between chapters 3 and 4. `v3.7.7` adds extraction, preview sanitization, and save-gate coverage for that fragment class.
- Full-run QA on `v3.7.7` reached chapter 4, but a structurally broken chapter entered repeated audit repair attempts. `v3.7.8` caps repair attempts at two so the chapter is regenerated sooner.
- Full-run QA on `v3.7.8` completed all 10 chapters with 81,177 total characters. Saved chapter sizes: 5,970 / 8,679 / 4,879 / 7,510 / 9,387 / 8,459 / 8,314 / 11,387 / 10,051 / 6,287. Final scan found 10 chapter headings, one `縲仙ｮ後疏, no duplicated chapter numbers, and no management memo/master-prompt/repair-meta contamination in the manuscript body.
- After completion, the output box expanded to full manuscript height; `v3.7.9` keeps the completed manuscript in the same in-box scroll view used during generation.
- `v3.8.1` adds a long-novel prose contract and save gate for performed scenes, chapter turns, aftertaste, and rejection of memo-like/summary-like chapter bodies.
- `v3.8.1` keeps both completed and stopped manuscripts in `ln-novel-scroll`, so the final page no longer expands the text box to the full manuscript height.
- Full-run QA on `v3.8.1` completed all 10 chapters with 80,343 total characters. Final scan found chapter headings 1-10, one final `縲仙ｮ後疏, and no management memo/master-prompt/repair-log contamination in the manuscript body.
- Completion UI QA on `v3.8.1` confirmed `#output` remains a fixed internal scroll box (`clientHeight` 720, `scrollHeight` 87,168, `overflow-y: auto`) and the long-novel control header remains `position: sticky` with z-index 60.
- `v3.8.5` restores app-side long-novel header sections, canonical final marker output, distinct fallback chapter beats, and body-level rejection of embedded header metadata. Fresh full-run/editorial QA remains pending.
- Full browser QA on `v3.8.5` completed all 10 chapters with 68,202 total characters and a final `縲仙ｮ後疏, then exposed remaining header defects: the printed plot outline had only 8 chapters and logline/planning metadata was visible. `v3.8.6` fixes this by rebuilding the completed manuscript header from saved chapters.
- Full browser QA on `v3.8.7` reached chapter 12 after saving 11 chapters (92,274 total characters), then stopped fail-closed because chapter 12 repeatedly replayed the completed chapter 11 climax and an over-broad English-residue gate rejected valid proper nouns. `v3.8.8` adds near-final whole-story-resolution rejection, final-chapter state-lock prompting, and narrower English-residue handling.
- Full browser QA on `v3.8.8` completed all 10 chapters with 74,659 characters, one final `縲仙ｮ後疏, no management memo/master-prompt/repair-log/English-residue contamination, and a scroll-contained output box. `v3.8.9` fixes the remaining generic completed-header outline titles.

## Verification State
- `node --check src/main.js` passed for `v4.0.1`.
- `npm run build` passed for `v4.0.1` and generated `dist/assets/index-Dva3j8LW.js`.
- `npm run lint --if-present` passed for `v4.0.1`.
- `git diff --check -- . ':!dist'` passed for `v4.0.1`; only normal LF-to-CRLF warnings were reported.
- Static helper check confirmed the v4.0.1 visible/runtime metadata, long-novel score-board completion call, score-board reset hide call, and entertainment-engine prompt rules exist in `src/main.js`.
- In-app browser DOM check on `http://127.0.0.1:5179/?v=local-v4.0.1` confirmed visible `Story Maker v4.0.1`, empty API field, hidden initial score board, hidden long-novel panel, and no browser warn/error logs.
- Note: `src/prompt.js` still does not pass standalone `node --check` because of pre-existing mojibake string damage near the top of the file; Vite build does not import that broken path and passed.
- `v3.9.9` in-app Gemini QA after user API entry saved 1 / 10 chapters, then stopped fail-closed at chapter 2 because the paragraph-density gate counted only blank-line-separated blocks. The rejected chapter 2 was not adopted into `cleanText` or the visible manuscript output.
- `node --check src/main.js` passed for `v4.0.0`.
- `npm run build` passed for `v4.0.0` and generated `dist/assets/index-DglsxoYH.js`.
- `npm run lint --if-present` passed for `v4.0.0`.
- `git diff --check -- . ':!dist'` passed for `v4.0.0`; only normal LF-to-CRLF warnings were reported.
- Helper check confirmed the v4.0.0 runtime version, visible-paragraph prompt rules, single-newline paragraph counting, paragraph-density retry budget, and paragraph retry guidance exist in `src/main.js`.
- In-app browser DOM check on `http://localhost:5179/` confirmed visible `Story Maker v4.0.0`; the user-entered Gemini API key remained saved in-memory as a masked field, without exposing the key.
- Fresh full Gemini QA completed 12 / 12 chapters, 96,555 visible characters, and exactly one final `縲仙ｮ後疏. Final scan found 12 body chapter headings, no context memo / GMC+S / regeneration-log contamination, no bare heading artifacts, no design-memo bullet lines, and no stray `AND` residue. The lone `繝励Ο繝ｳ繝励ヨ` hit was normal in-story terminal text, not management metadata.
- `npm run deploy` passed for `v4.0.0`; `gh-pages` published successfully.
- Remote verification passed: `git fetch origin gh-pages` updated `origin/gh-pages`, and `git show origin/gh-pages:index.html` contains `Story Maker v4.0.0`.
- Live GitHub Pages smoke check passed at `https://furuyan1234.github.io/story-maker/?v=...`: title/version show `Story Maker v4.0.0`, API field is empty on the public page, and no browser warn/error logs were reported.
- `node --check src/main.js` passed for `v3.9.9`.
- `npm run build` passed for `v3.9.9` and generated `dist/assets/index-KUZgCpTf.js`.
- `npm run lint --if-present` passed for `v3.9.9`.
- `git diff --check -- . ':!dist'` passed for `v3.9.9`; only normal LF-to-CRLF warnings were reported.
- Helper check confirmed the v3.9.9 late-resolution gate, empty-heading sanitizer, extended late retry budget, cleanText-preserving error output, and runtime version constant exist in `src/main.js`.
- In-app browser DOM check on `http://localhost:5179/` confirmed visible `Story Maker v3.9.9`, no browser warn/error logs, and an empty API field after the update.
- `v3.9.8` in-app Gemini QA after user API entry saved 8 / 12 chapters, then stopped fail-closed in chapter 9 after repeated empty Markdown heading artifacts and a repaired-but-rejected setting contradiction around 螳溯干 / 鄒主調. The broken chapter 9 was not saved.
- `v3.9.8` patch check confirmed the endpoint anchor, retry-chronology guidance, runtime version constant, and visible version metadata exist in `src/main.js` / `index.html`.
- `node --check src/main.js` passed for `v3.9.8`.
- `npm run build` passed for `v3.9.8` and generated `dist/assets/index-CYm4Slfy.js`.
- `npm run lint --if-present` passed for `v3.9.8`.
- `git diff --check -- . ':!dist'` passed for `v3.9.8`; only normal LF-to-CRLF warnings were reported.
- In-app browser DOM check on `http://localhost:5179/` confirmed visible `Story Maker v3.9.8`; the API field is empty after reload, so fresh full API QA is waiting for user UI key re-entry.
- `v3.9.7` in-app Gemini QA after user API entry saved 8 / 10 chapters, then stopped fail-closed in chapter 9 after rejecting repeated chapter 8 replay / data-center timeline rewind and premature `縺吶∋縺ｦ...邨ゅｏ` wording. The broken chapter 9 was not saved.
- `v3.9.6` in-app Gemini QA after user API entry saved 8 / 10 chapters, then stopped fail-closed in chapter 9 with: `隨ｬ9遶縺ｮ菫晏ｭ伜燕蜩∬ｳｪ繧ｲ繝ｼ繝医〒蛛懈ｭ｢縺励∪縺励◆: 髟ｷ縺・ｪｬ譏取ｮｵ關ｽ縺悟､壹￥縲∝ｴ髱｢縺ｮ蛻・ｌ逶ｮ縺御ｸ崎ｶｳ縺励※縺・∪縺呻ｼ育ｬｬ9遶: 6谿ｵ關ｽ・・/ Whole-story resolution appears before the final chapter (/縺吶∋縺ｦ(?:縺芸縲・?邨ゅｏ/)`.
- `v3.9.4` in-app Gemini QA after user API entry stopped fail-closed at chapter 1 with: `隨ｬ1遶縺ｮ菫晏ｭ伜燕蜩∬ｳｪ繧ｲ繝ｼ繝医〒蛛懈ｭ｢縺励∪縺励◆: 遶譛ｫ縺御ｺ亥相繝ｻ辣ｽ繧頑枚縺ｧ邨ゅｏ縺｣縺ｦ縺・∪縺呻ｼ育ｬｬ1遶・荏.
- `v3.9.5` patched the chapter-1 quality-gate retry route.
- `node --check src/main.js` passed for `v3.9.5`.
- `npm run build` passed for `v3.9.5`.
- `npm run lint --if-present` passed for `v3.9.5`.
- `git diff --check -- . ':!dist'` passed for `v3.9.5`; only normal LF-to-CRLF warnings were reported.
- HTTP check passed: `http://localhost:5179/` returns `Story Maker v3.9.5`.
- In-app browser reload confirmed visible `Story Maker v3.9.5`; the reload cleared the in-memory API key, so full API QA is waiting for user UI key re-entry.
- `node --check src/main.js` passed for `v3.9.4`.
- `npm run build` passed for `v3.9.4`.
- `npm run lint --if-present` passed for `v3.9.4`.
- `git diff --check -- . ':!dist'` passed for `v3.9.4`; only normal LF-to-CRLF warnings were reported.
- HTTP check passed: `http://127.0.0.1:5179/` returns `Story Maker v3.9.4`.
- In-app browser DOM check on `http://127.0.0.1:5179/` confirmed visible `Story Maker v3.9.4`, no Vite parse overlay, and no saved API key after reload. Full API QA is waiting for user UI key re-entry.
- `v3.9.3` in-app Gemini QA stopped fail-closed at chapter 4 after the duplicate gate found long chapter 3 replay. This confirmed the save gate blocks broken prose, but also showed duplicate regeneration needed stronger instructions.
- `node --check src/main.js` passed for `v3.9.3`.
- `npm run build` passed for `v3.9.3`.
- `npm run lint --if-present` passed for `v3.9.3`.
- `git diff --check -- . ':!dist'` passed for `v3.9.3`; only normal LF-to-CRLF warnings were reported.
- HTTP check passed: `http://127.0.0.1:5179/` returns `Story Maker v3.9.3`.
- In-app browser DOM check on `http://127.0.0.1:5179/` confirmed visible `Story Maker v3.9.3` and no warn/error logs. The page reload after the code update cleared the in-memory API key, so full API QA is waiting for user UI re-entry.
- `node --check src/main.js` passed for `v3.9.2`.
- `npm run build` passed for `v3.9.2`.
- `npm run lint --if-present` passed for `v3.9.2`.
- `git diff --check -- . ':!dist'` passed for `v3.9.2`; only normal LF-to-CRLF warnings were reported.
- HTTP check passed: `http://127.0.0.1:5179/` returns `Story Maker v3.9.2`.
- In-app browser DOM check on `http://127.0.0.1:5179/` confirmed `Story Maker v3.9.2`; selecting long-novel mode populated visible defaults, and browser warn/error logs were empty.
- `node --check src/main.js` passed for `v3.9.1`.
- `npm run build` passed for `v3.9.1`.
- `npm run lint --if-present` passed for `v3.9.1`.
- Browser DOM check on `http://127.0.0.1:5179/` confirmed `Story Maker v3.9.1`, startup defaults visible for the initial `4koma` mode, long-mode click replacing system defaults with long-mode defaults, and a typed custom theme remaining preserved across a later mode click.
- `git diff --check` passed for the touched Story Maker files; only normal LF-to-CRLF warnings were reported.
- `node --check src/main.js` passed for the current v3.8.9 patch.
- Helper-level check passed for `v3.8.6`: bad 8-chapter outlines are replaced with 10 chapters, logline/total/planning metadata is not printed in the header, final clean text has chapter headings 1-10, duplicate header titles are suppressed, and final `縲仙ｮ後疏 remains.
- Helper-level check passed: the long-novel fallback outline now emits 10 distinct chapter beats, and embedded `繧ｿ繧､繝医Ν:` / `繝ｭ繧ｰ繝ｩ繧､繝ｳ:` / `蜈ｨ讒区・:` metadata under the chapter heading is stripped before save.
- `npm run build` passed for the current v3.8.9 patch.
- `npm run lint --if-present` passed for the current v3.8.9 patch.
- `git diff --check` passed for the touched Story Maker files; only normal LF-to-CRLF warnings were reported.
- HTTP check passed: `http://127.0.0.1:5179/` returns `Story Maker v3.8.9`.
- Fresh full API browser QA on `v3.8.9` has not yet been rerun; `v3.8.8` completed and the `v3.8.9` delta is deterministic chapter-title/header normalization.
- `npm run build` passed for `v3.7.8`.
- `npm run lint --if-present` passed for `v3.7.8`.
- HTTP check passed: `http://127.0.0.1:5179/` returns `Story Maker v3.7.8`.
- Pre-patch browser QA on `v3.7.6` saved chapters 1-4, corrected a chapter 4 location contradiction automatically, but was stopped because a `縲仙・遶縺ｮ` admin fragment remained in the visible manuscript after chapter 3.
- Pre-patch browser QA on `v3.7.5` saved chapters 1 and 2, then stopped fail-closed at chapter 3 because the generated chapter replayed chapter 2's completed event and audit repair could not fix the structural contradiction.
- `npm run build` passed for `v3.7.5`.
- `npm run lint --if-present` passed for `v3.7.5`.
- HTTP check passed: `http://127.0.0.1:5179/` returned `Story Maker v3.7.5`.
- Before the patch, `v3.7.3` full-run browser QA reached chapter 10 but stopped fail-closed with: chapter body too short (4006 / 4800) while raw continuation had grown through five retries.
- Prior `v3.7.3` checks: build, lint, diff-check, HTTP 200, and live-status structural check passed.
- Previous user-entered Gemini API manual QA passed on `v3.7.1`: chapter 1 saved, generation continued into chapter 2, and chapter-end pause stopped after chapter 2 at `2 / 11` with `逕滓・繧貞・髢義 visible.

## Remaining Risks / Manual QA
- Full end-to-end generation has not yet been refreshed after the v4.0.1 self-score / story-interest prompt change. Do not ask for or store the API key in chat or files; ask the user to enter it in the UI when needed for future runs.
- Do not deploy v4.0.1 until a fresh API QA checkpoint passes or the user explicitly accepts a local-check-only deploy.
- If the provider keeps returning short prose below the minimum even after continuation attempts, the app should continue to fail closed instead of saving a broken chapter.
- Further literary-quality tuning still needs real generated samples; keep fail-closed body adoption and output-box scroll safeguards in place.

## Next Steps
1. Run a fresh long-novel API QA after the user enters the API key in the UI; confirm the completed manuscript shows the three-item score board.
2. Review the generated prose for stronger choices, costs, reversals, and scene escalation; tune prompts from real samples if needed.
3. Deploy again from Codex only after the next meaningful local checks plus fresh API QA checkpoint.
