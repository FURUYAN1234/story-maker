{
  "name": "story-maker",
  "version": "3.4.2",
  "private": true,
  "description": "AI Story Maker - Gemini API対応 多軸ランダム物語生成ツール",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "version:up": "node scripts/update_version.cjs",
    "predeploy": "node scripts/pre_deploy_check.js && npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.0.0"
  }
}
